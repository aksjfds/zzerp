# 本地开发可留空；生产环境默认通过 Netlify 的 /api 同源代理访问 Render。
VITE_API_BASE_URL=

# Netlify 推荐使用此变量生成代理规则。
RENDER_API_URL=https://your-render-service.onrender.com

# 若 Netlify 已经配置此变量，代理脚本也会将它作为 Render 地址使用。
# 生产页面本身仍统一请求 /api。
# VITE_API_BASE_URL=https://your-render-service.onrender.com

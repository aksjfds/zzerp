# 生产工作台展示与读模型整改方案

## 整改背景

当前生产工作台采用“左侧生产位置卡片、右侧工单大卡片”的两栏结构，不能清晰回答三个核心问题：当前部门有哪些在位物料、每项物料正在进行哪些工艺、这些工艺对应哪些工单及 QC 情况。

当前实现存在以下实际问题：

1. 左侧卡片把 `repository.quantity` 标记为“任务数”，实际该值是当前生产位置的在位数量，不是生产计划任务数。
2. 普通生产部门把加工完成、质检中和返工中等状态统一压缩成“进行中”，丢失了有价值的信息。
3. 左侧按单个 `repository_id` 生成卡片，右侧却按“生产项 + 当前节点 + 来源节点”查询工单；同一生产位置存在多个库存来源行时，左侧会重复出现相同物料，而右侧反复展示同一组工单。
4. 工单使用纵向大卡片展示，工单号、工艺、数量、QC 批次、备注与操作混在同一层级；同一工艺拆成多张工单后难以比较。
5. 装配部把装配任务、输入物料、装配部普通加工和历史记录堆叠在同一列表，缺少稳定的主从层级。
6. 页面使用一个物料 Tag 概括多张工单，但同一项物料可能同时存在加工中、待 QC、返工和已结单工单，单一状态无法准确表达。

本次整改把生产工作台统一为“在位物料 → 工艺加工情况 → 关联工单”的主从视图。整改重点是纠正展示口径、建立精确读模型和重组页面职责，不改变生产执行业务逻辑。

## 整改目标

1. 快速查看当前部门的全部在位物料及可开工情况。
2. 选中一项在位物料后，清晰展示该生产位置的数量、来源和当前节点。
3. 按工艺汇总该生产位置关联工单的加工、QC、合格和异常情况。
4. 使用紧凑工单列表支持同工艺多工单比较，以详情抽屉承载完整 QC 记录和次要操作。
5. 普通生产部门与装配部共享稳定的工作台骨架，装配部单独表达多路输入物料。
6. 数量和状态由后端权威读模型生成，前端只负责展示和交互。
7. 当前页批量聚合概要，所选位置延迟加载详情，避免逐项查询和无意义全量加载。

## 已确认业务口径

### “这批料”的定义

本次不新增物料批次表。“这批料”统一解释为：

> 同一订单生产项 + 当前流程节点 + 来源完成状态对应的在位物料。

实现要求：

- 普通生产位置的业务身份为“生产项 + 当前流程节点 + 来源流程节点”；同一位置下保留一个或多个具体 `repository_id` 开单来源。
- 不同订单、生产项、当前节点或来源完成状态不得合并。
- 同一生产位置的现有库存来源行在工作台聚合展示，但开工单时仍选择具体来源行；不同来源行的数量、上游工单和可选工艺不能丢失。
- 同一来源行的多次到料进入同一个位置余额，本次不提供逐次到料批次追踪。
- 页面和代码不得使用“批次”暗示不存在的独立物料批次身份，统一使用“在位物料”或“生产位置”。

### 数量口径

1. “在位数”取当前生产位置全部库存来源行的实有数量合计。
2. “已占用数”取各来源行被未结工单占用、尚未进入提交结果的数量合计。
3. “可开工数”取各来源行 `max(在位数 - 已占用数, 0)` 的合计，不能先汇总后截断。
4. 生产计划“任务数”只属于生产任务 Tab，生产工作台不得把在位数量称为任务数。
5. 不同工艺可能连续作用于同一项在位物料，不同工艺的开单、合格等数量不能相加为物料总体完成率。
6. 工艺汇总只能在同一工艺范围内聚合同工艺拆分工单。

### 工单关联口径

1. 普通生产位置按“生产项 + 当前节点 + 来源节点 + 所属部门”关联位置范围内工单，并补充当前库存来源明确记录的 `source_work_order_id`；两类结果必须去重。
2. `repository_id` 只作为当前库存来源和未提交工单的执行引用。标准工单整单提交后会清空该字段，不能将其作为历史关联条件。
3. 当前工作台位置来自“现有库存来源 + 当前部门开放工单”的并集；物料已整单送检、库存来源已耗尽时，开放工单仍须在当前工作台显示。
4. 不允许仅凭显示名称、产品名称或工艺名称关联工单。
5. 装配任务按订单产品项和稳定装配节点组织；工单通过装配输出生产项、装配节点及 `work_order_material` 投入快照关联。
6. 装配部所属单路加工仍按普通生产位置规则处理，不混入多路装配输入分组。
7. 已结单工单可以作为当前生产位置的加工记录展示；既无在位数量也无开放工单的位置不进入默认当前列表。

### 保持不变的业务规则

1. 不改变生产计划任务数量口径和产品版本绑定。
2. 不恢复标记、必做工艺或固定工艺顺序。
3. 正常工单仍要求对应产品、版本、物料和车间节点的工艺配置已经确认。
4. 临时工单仍使用配置外工艺，并保持明确临时身份。
5. 标准与装配工单仍按整张工单全部数量处理。
6. 直接确认合格、送检、QC 返回、QC 放行、返工、报废、遗失和结单规则不变。
7. 装配和焊接仍是多路节点，第一张工单消耗输入物料，后续工艺使用节点在制品。
8. 委外加工、QC 工作台、生产任务 Tab、库存和成品仓状态机不在本次修改范围。

## 非目标

- 不新增物料批次表、批号字段或批次拆分合并逻辑。
- 不新增或修改业务表和业务字段。允许为本次已确认查询增加必要索引，但必须同步 SQL/ORM 且不能改变业务约束。
- 不改变开工单、工单占用、生产结果、QC 或物料流转命令。
- 不把生产工作台改造成生产计划进度页。
- 不新增整体加工百分比或伪造不同工艺之间的数量守恒关系。
- 不改变工程部流程图、工艺配置确认和工艺单价功能。
- 不顺便重构与生产工作台无关的部门页面。

## 目标页面结构

### 一、当前在位物料列表

左侧使用紧凑列表替代信息密度低的操作卡片，每项展示：

- 厂编、产品名、物料编号、物料名。
- 当前车间和来源完成状态。
- 在位数、已占用数、可开工数。
- 当前生产位置包含的具体开单来源数量。
- 加工中、待处理结果、待 QC、返工等需要关注的工单数量提示。
- 到达时间或最近操作时间。

交互规则：

- 左侧不再重复放置开工单按钮。
- 不使用一个 Tag 表达全部工单的综合状态。
- 默认按需处理优先级排序：返工、待 QC、待处理结果、加工中、可开工、无可用数量。
- 当前物料和历史位置分开，默认只显示仍有在位物料或有效未结工单的位置。
- 保留关键词、车间和关注状态筛选，不再依赖被压缩的单值状态。

### 二、所选生产位置概况

右侧顶部固定展示：

- 客户订单、产品、产品版本。
- 物料编号、物料名称。
- 当前车间、当前节点、来源完成状态。
- 在位数、已占用数、可开工数。
- 到达时间和工艺配置状态。

“开工单”作为所选位置主操作放在此处。按钮资格继续由后端返回；禁用时只显示简短、准确原因。

同一生产位置包含多个可用 `repository_id` 时，开单弹窗必须先选择具体加工来源，再按该来源展示可选工艺和最大开单数量；不得把多个来源行伪装成一个可以跨来源开单的数量。

### 三、加工情况

按工艺分组展示：

- 工艺名称及已配置/临时身份。
- 工单数量和开单数量。
- 加工中、待 QC、合格、返工、报废和遗失数量。

同一工艺拆分的多张工单可以汇总。点击工艺后，下方只显示该工艺工单，并可恢复“全部工艺”。临时工艺不得反向加入产品版本已确认配置。

### 四、关联工单

工单主体改为紧凑表格或等价行列表，主要列为：

- 工单号、工艺、正式/临时身份、工人。
- 工单数量、当前状态。
- 合格、质检中、返工和异常数量。
- 创建时间和当前可执行主操作。

点击工单打开详情抽屉，展示完整数量、QC 记录、人员、时间、不良原因、备注以及打印、送检、确认合格、返工送检、撤销和取消等合法操作。

按钮可见性继续复用现有工单操作策略，不允许列表和抽屉分别维护状态规则。

### 五、装配部适配

装配任务按“订单产品项 + 稳定装配节点”显示为一个主项。选中后增加始终展开的输入物料区：

- 输入物料及来源完成状态。
- 各来源在位数量、单件用量和已占用数量。
- 当前可支持的装配数量。

下方继续使用统一加工情况和关联工单结构。装配工单详情展示实际投入的各个 `repository_id` 与数量，避免将多个来源误认为普通单一生产位置。

## 第一阶段确认的后端契约

第二阶段必须按以下契约实现，不得继续扩展旧 `RepositoryResponse` 的混合卡片职责。

### 生产位置身份

普通生产位置键由服务端生成并作为不透明字符串返回，其组成字段同时明确返回：

- `production_item_id`
- `flow_node_id`
- `source_flow_node_id`
- `department_code`

同一位置下的具体库存来源以 `sources` 返回，每项包含：

- `repository_id`
- `source_work_order_id`
- `on_hand_quantity`
- `reserved_quantity`
- `available_quantity`
- `available_procedures`
- `procedure_configuration_confirmed`
- `can_create_work_order`

位置级数量分别为各来源对应数量的合计。`available_quantity` 必须合计每项来源截断后的可用数量，禁止使用 `max(总在位 - 总占用, 0)` 替代来源级计算。

装配位置键由以下稳定身份组成：

- `customer_order_item_id`
- `flow_node_id`
- `department_code = assembly`

装配位置不得伪造单个 `repository_id`。其 `input_materials` 按稳定物料身份返回，每项包含物料编号、名称、单件用量、在位数、已占用数、可用数和具体来源列表；`capacity_quantity` 由各必需输入物料可用数按单件用量计算最小值。

### 在位物料概要契约

普通和装配概要使用可判别类型，公共字段为：

- `position_key`
- `position_type`：`standard` 或 `assembly`
- 客户订单、订单明细、产品、版本和厂编身份
- 物料或装配体编号、名称
- 当前流程节点、来源完成节点、车间和部门
- 到达时间、最近活动时间
- 开放工单总数
- 加工中、待处理结果、待 QC、返工工单数量
- 是否具备至少一个合法开单来源

普通概要增加 `on_hand_quantity`、`reserved_quantity`、`available_quantity` 和 `source_count`。装配概要增加 `capacity_quantity`、输入物料是否齐套和输入物料种类数。装配输入数量不能合计成无业务含义的通用“在位数”。

当前列表的集合来源为：

- 当前部门 `repository` 中存在的生产位置。
- 当前部门仍为 `open` 的标准或装配工单所代表的执行位置。

只有历史结单工单、且没有在位来源和开放工单的位置，不进入当前列表。

### 工艺加工汇总契约

工艺分组身份为 `procedure_id + is_temporary`，返回：

- `procedure_id`
- `procedure_name`
- `is_temporary`
- `work_order_count`
- `open_work_order_count`
- `work_order_quantity`
- `processing_quantity`
- `ready_for_result_quantity`
- `pending_qc_quantity`
- `qualified_quantity`
- `rework_quantity`
- `scrap_quantity`
- `lost_quantity`

所有进度字段必须来自 `calculate_work_order_progress()`；装配工单统一使用装配输出单位，分别采用现有输出数量和 `calculate_assembly_output_progress()` 的结果。不同工艺之间不得再次汇总为位置总体进度。

已取消工单保留在关联工单列表中供追溯，但不进入工艺汇总的工单数和任何数量。取消状态不代表在制加工，禁止把其未完成数量计算为加工中数量。

### 关联工单契约

工单列表继续复用 `WorkOrderResponse` 的权威序列化结果，不建立第二套工单进度字段。新增的位置查询范围只负责确定哪些工单属于所选位置：

- 普通位置：标准工单所属部门匹配，且满足“生产项、当前节点、来源节点全部匹配”或“工单 ID 是当前库存来源的 `source_work_order_id`”；后一种情况仍须校验生产项、当前节点和部门，结果按工单 ID 去重。
- 装配位置：装配工单的输出生产项属于同一订单明细，且 `flow_node_id` 匹配；首次多路工单通过 `work_order_material` 保留投入快照，后续工艺通过装配输出生产项关联。

开放工单和结单工单使用同一位置口径。`repository_id` 只用于具体开单来源、未提交工单占用和来源明细，不能作为工单历史查询必填条件；`source_work_order_id` 只补充当前来源的直接生成工单，不递归伪造不存在的完整批次链。

### 字段权威来源

- 在位数：`repository.quantity`。
- 已占用数：`reserved_quantities()`，普通工单使用未完成数量，装配输入使用仍引用来源的开放工单投入数量。
- 可开工数：来源级 `max(repository.quantity - reserved, 0)`。
- 工单阶段：`work_order_stage()`。
- 工单数量进度：`calculate_work_order_progress()`。
- 装配输出进度：`calculate_assembly_output_progress()`。
- 装配输入与来源：`work_order_material` 的生产项、来源节点、来源上游节点、来源工单和数量快照。
- 到达时间：目标部门和位置匹配的最新 `production_movement.created_at`。
- 产品、物料和流程节点显示：订单绑定产品版本的生产流程上下文。
- 工艺配置和开单资格：`standard_execution.configuration_api` 的已确认配置结果以及来源级可用数量。

## 后端架构方案

`planning` 继续拥有跨生产位置、工单和进度的工作台读模型；`production_core` 通过公共或新增窄读取接口提供生产位置占用、工单阶段和聚合数据。Router 不复制查询，`planning` 不访问其他模块私有实现。

读模型分为：

1. 在位物料概要：左侧分页列表。
2. 所选生产位置详情：右侧物料概况和装配输入。
3. 工艺加工汇总：所选位置关联工单按工艺聚合。
4. 关联工单列表：严格按所选位置范围分页查询。

查询原则：

- 候选位置的筛选与排序所需轻量数量和关注状态统一批量读取，禁止逐卡片查询；分页后才加载当前页来源工单、到达流水和已确认工艺等详情。
- 所选位置详情和工单列表延迟加载，不为全部位置加载完整 QC 记录。
- 普通工单按生产项、当前节点、来源节点和部门查询；具体来源行只用于开单，不作为已提交工单的历史关联条件。
- 当前位置概要由现有库存来源与开放工单并集产生，不能因整单送检消耗库存而从工作台消失。
- 装配汇总使用装配节点和投入物料关系计算，不在前端拼接。
- 状态和数量复用现有权威工单进度算法，不新增第二套口径。
- 只有发现明确索引缺口时才允许计划修改 `zzerp.sql`，并须先更新本方案。

第二阶段审查已确认开放工单集合存在索引缺口：现有索引依赖 `repository_id` 或以生产项开头，无法稳定支持送检后来源引用已清空的开放工单按类型、工艺和位置读取。本次增加只覆盖 `status = 'open'` 的工单工作台部分索引，不增加表或字段。

## 前端架构方案

在 `frontend/src/features/production/` 内按职责拆分：

- 在位物料列表组件：筛选结果和选择。
- 生产位置概况组件：所选位置和开单入口。
- 工艺加工汇总组件：展示后端聚合并触发工艺筛选。
- 工单列表组件：紧凑工单行和当前主操作。
- 工单详情抽屉：QC 明细和次要操作。
- 装配输入物料组件：仅由装配工作台装配使用。

普通生产部门和装配部共用页面骨架、工单列表和详情抽屉；装配输入和装配开单保留独立适配层。共享组件不得反向依赖具体部门页面，也不得复制工单操作策略。

## 实施阶段与执行顺序

### 第一阶段：口径与现状闭环

状态：已完成

实施范围：核对生产位置、占用数量、工单关联、工单阶段、装配投入和历史位置实现；将全部展示字段落实为明确后端契约。

完成条件：每个字段都有唯一数据来源；“任务数”误用纳入替换；普通与装配关联身份明确；开放工单不会因库存来源耗尽而丢失；不存在需要物料批次表才能表达的字段。

### 第二阶段：生产工作台后端读模型

状态：已完成

实施范围：以现有库存来源和开放工单并集建立在位物料概要和详情读模型；批量返回在位数、占用数、可开工数及关注状态；移除普通部门状态压缩造成的信息损失。

完成条件：左侧数据由一个权威读模型返回；数量含义明确；当前与历史位置不混淆；当前页聚合不存在逐项查询。

### 第三阶段：精确工单关联与工艺汇总

状态：已完成

实施范围：普通生产位置按生产项、当前节点、来源节点、当前来源工单和部门查询工单，并保留具体来源开单能力；装配按装配输出、装配节点和投入物料关系查询；新增按工艺聚合的加工情况读模型。

完成条件：所选位置不混入其他位置工单；同工艺拆分工单正确汇总；不同工艺不生成伪总体进度；临时工单身份保留。

### 第四阶段：普通生产部门页面整改

状态：已完成

实施范围：替换左侧卡片，增加位置概况和工艺汇总，将工单大卡片改为紧凑列表与详情抽屉，迁移现有开单和工单操作。

完成条件：能够按“在位物料 → 工艺 → 工单”查看；现有合法操作仍可到达；按钮判断只有一个策略来源；不再把在位数显示为任务数。

### 第五阶段：装配部页面整改

状态：已完成

实施范围：统一装配任务主项并增加输入物料区；把装配部普通加工与多路装配映射到统一骨架；展示工单实际投入来源。一个装配主项内明确区分“首次多路输入”和“节点在制品后续工艺”两种合法开单来源：前者按各输入物料来源分配，后者必须选择一个具体节点在制品来源，不再把两者压成一个不可解释的可开工数。首张多路工单继续使用现有 `work_order_material` 投入快照；同一装配节点的后续工艺在该表补齐所选节点在制品来源快照，工作台通过专属只读响应展示物料、数量、来源完成状态和来源工单。该补充不新增表或字段，不改变后续工艺在工单提交时才消耗节点在制品的既有时点。

完成条件：一个装配任务只出现一个主项；输入物料到位和占用清晰；装配、焊接及单路加工的工单关联准确；历史记录不混入当前任务。

### 第六阶段：旧实现清理与闭环审查

状态：已完成

实施范围：删除失去用途的状态压缩、旧卡片、旧部门只读接口和重复前端计算；搜索全部工作台入口、接口类型和状态引用；统一开单命令与工作台的来源占用算法，防止绕过页面或并发请求重复占用同一来源；更新实际修改与审查证据。

完成条件：不存在新旧口径并行；没有无引用组件、重复状态算法或跨模块内部访问；全部完成条件都有代码或只读审查证据；规范、低耦合和高稳定审查无范围内遗留问题。

## 影响范围

预计涉及：

- `backend/src/modules/planning/` 的生产工作台读模型。
- `backend/src/modules/production_core/` 的窄工单查询与聚合接口。
- 对应 Router 和 `backend/src/schemas/` 类型契约。
- `frontend/src/features/production/` 的页面、组件、接口、类型和组合逻辑。
- 普通生产部门与装配部工作台。

原则上不涉及：工程部流程图、生产任务 Tab、QC 命令、工艺配置命令、委外加工、仓库、成品仓、客户订单和数据库表结构。

## 风险与控制

1. **生产位置不等于物料批次**：统一使用“在位物料/生产位置”，不增加批号文案，只按现有稳定身份展示。
2. **工单数量重复计算**：不同工艺不合计总体完成率，只在单工艺内部汇总拆分工单。
3. **工单关联过宽或过窄**：普通生产按稳定位置三元组和部门关联，装配按装配输出与投入快照关联；`repository_id` 仅用于具体开单来源。
4. **查询性能下降**：候选筛选与排序数据批量读取，分页后再加载当前页详情，禁止 N+1 查询；开放工单使用部分索引限制扫描范围。
5. **操作规则重复**：列表与详情复用同一工单操作策略，不自行解释状态机。
6. **普通与装配再次分叉**：共用页面骨架和工单展示，装配只维护输入物料及开单差异适配。

## 代码审查方式

阶段完成前仅使用 `AGENTS.md` 允许的项目内只读检查：

- 搜索全部生产工作台路由、页面、接口和类型引用。
- 对照生产位置、工单、工单投入和 QC 记录的 ORM 所有权与公共接口。
- 审查普通和装配工单查询条件，确认关联范围稳定、可解释。
- 审查数量字段命名和来源，确认任务数、在位数、占用数和可开工数没有混用。
- 审查前端 feature 边界和组件依赖，禁止共享组件反向依赖部门页面。
- 审查查询结构，确认批量聚合、详情延迟加载且不存在明显 N+1。
- 使用只读语法和导入检查排查明显导入、类型或契约冲突；不主动执行构建、项目脚本、服务或数据库操作。

## 最终完成条件

1. 当前部门在位物料、所选位置加工情况和关联工单形成清晰主从关系。
2. 左侧不再把生产位置数量称为任务数。
3. 普通工单与所选业务生产位置精确关联，具体开单来源不丢失；装配工单与多路投入准确关联。
4. 同工艺拆分工单可汇总，不同工艺没有错误总体进度。
5. 工单列表、详情和操作按钮职责清晰，没有重复状态规则。
6. 当前与历史位置不混淆，装配输入不再形成重复主卡片。
7. 不新增物料批次表，不改变已确认生产、QC 和装配业务规则。
8. 不存在新旧展示口径并行、无价值兼容逻辑或本轮范围内未解决问题。
9. 代码规范、低耦合和高稳定审查满足 `AGENTS.md`。

## 实施结果

### 第一阶段实际修改与重要发现

- 完成生产工作台全部现有入口核对。拥有生产位置和工单能力的部门为冲压、CNC、抛光、外协和装配；它们统一经部门 capability 调用 `planning.list_production_cards()` 与 `production_core.list_department_work_orders()`。
- 确认旧 `RepositoryResponse` 同时承担库存来源、装配输入、历史卡片、工艺候选和综合状态，职责过多；第二阶段将新建独立工作台契约，不继续向旧卡片响应堆字段。
- 确认普通生产位置的稳定业务身份为生产项、当前节点、来源节点和部门。同一位置可能因不同 `source_work_order_id` 存在多个库存来源行，必须在位置概要中聚合，同时保留来源级开单资格。
- 确认标准工单整单提交后，`consume_order_source()` 会清空 `work_order.repository_id` 并消耗来源库存；因此历史工单不能按 `repository_id` 查询。方案已改为位置业务身份关联，`repository_id` 只用于具体开单来源和未提交占用。
- 确认当前卡片只从 `repository` 建立主列表。物料全部送检后来源库存可能消失，而工单仍为开放并处于待 QC 或返工状态；第二阶段必须使用“库存位置与开放工单并集”，避免工作台任务消失。
- 确认装配首次开单立即消耗输入库存并清空 `work_order_material.repository_id`，但投入记录持续保存生产项、来源节点、来源上游节点、来源工单和数量快照；装配工作台可在不增加批次表的前提下恢复多路投入上下文。
- 确认普通工单占用由开放工单未完成数量计算，装配输入占用由仍引用库存来源的开放装配投入计算；在位数、占用数和可开工数具备现有权威来源。
- 确认工单阶段和数量进度已有唯一算法：`work_order_stage()`、`calculate_work_order_progress()` 与 `calculate_assembly_output_progress()`；新读模型只能复用，不重新解释状态机。
- 在本文件固化可判别的普通/装配位置概要、来源明细、工艺汇总和关联工单契约，并明确不同工艺不能合并为总体进度。

### 第一阶段完成条件核对

1. **字段唯一来源**：在位、占用、可用、工单阶段、标准进度、装配输出、装配投入、到达时间、显示上下文和工艺配置均已记录权威来源。
2. **任务数误用范围**：已确认旧页面“任务数”来自 `repository.quantity`，第四阶段必须改为“在位数”；生产计划任务数不进入工作台契约。
3. **普通关联身份**：已确认位置业务身份和来源级 `repository_id` 的职责边界，结单工单不依赖会被清空的来源引用。
4. **装配关联身份**：已确认按订单明细、稳定装配节点、输出生产项和投入快照关联，装配输入不伪造单一库存来源。
5. **开放工单连续可见**：已把库存与开放工单并集写入第二阶段契约，覆盖送检后库存来源耗尽的情况。
6. **无批次表依赖**：契约全部可由现有生产位置、工单、装配投入和流水表达，无需新增物料批次表或数据库字段。
7. **方案一致性**：实际发现导致的工单关联范围调整已先同步到本文件；第一阶段完成时没有提前修改后续阶段实现。

### 第一阶段代码审查结论

- 代码规范：契约明确区分任务数、在位数、占用数、可开工数和工单进度，避免继续使用含义不清的通用数量与综合 Tag。
- 低耦合：工作台聚合仍归 `planning`，生产位置、工单与进度算法归 `production_core`；后续通过已有或新增窄读取接口协作，Router 和前端不承担领域计算。
- 高稳定：方案基于当前 ORM 约束、事务命令和实际引用生命周期修正；不新增批次表、不改变执行状态机，也不依赖提交后会被清空的 `repository_id`。
- 只读审查：已搜索部门 capability、工作台前后端引用、SQL/ORM 位置与工单索引，并执行 `git diff --check -- MODIFICATION.md`；未运行构建、项目脚本、服务或数据库命令。

### 第二阶段实际修改与重要发现

- 新增 `production_core.workbench_read_api` 窄读取接口，统一批量返回当前部门开放标准工单、装配工单的精确阶段计数、最近活动时间和开放装配投入快照；`planning` 不复制工单阶段算法。
- 新增 `planning.production_workbench` 权威读模型和独立类型化响应契约，以“现有库存来源 + 开放工单”并集建立普通与装配生产位置，避免整单送检消耗来源库存后任务从工作台消失。
- 普通位置按生产项、当前节点和来源节点聚合多个库存来源，并逐来源调用现有占用算法后计算可开工数；位置级可开工数为来源级截断结果之和。
- 装配位置按订单明细和稳定装配节点聚合，保留现有输入来源与开放工单投入快照。初次多路输入能力和节点在制品继续加工能力分别计算，概要取单次合法开单能力的最大值，不把两类互斥来源错误相加，也不把节点在制品列为输入物料。
- 新契约单独保留加工中、待处理结果、待 QC 和返工工单数量；旧普通生产卡片不再把 `processing_completed`、`qc` 和 `rework` 压缩为 `processing`。
- 新增部门工作台 capability 和只读 HTTP 路由，仅为冲压、CNC、抛光、外协和装配部门注册；Router 只处理权限、参数和响应，不持有读模型逻辑。
- 生产位置候选、库存占用、开放工单及阶段批量读取；只有分页选中的位置才加载来源工单、到达流水和已确认工艺，不存在逐位置数据库查询。
- 审查确认送检后 `repository_id` 会清空，现有索引不能有效覆盖按部门读取全部开放工单的路径；已同步在 ORM 与 `zzerp.sql` 增加仅覆盖 `status = 'open'` 的 `idx_work_order_open_workbench` 部分索引，不新增业务表、字段或约束。

### 第二阶段完成条件核对

1. **权威读模型**：左侧位置数据只由 `planning.list_production_workbench_positions()` 返回，未继续扩展旧 `RepositoryResponse`。
2. **数量含义明确**：普通位置明确区分在位数、占用数和可开工数；装配位置只返回有业务含义的可装配能力和输入物料分项，不伪造通用在位总数。
3. **当前与历史分离**：候选只来自现有库存来源或开放工单；只有已结单历史且无当前来源的位置不会进入当前列表。
4. **开放工单连续可见**：标准与装配开放工单均可独立产生候选位置，不依赖提交后会被清空的库存来源引用。
5. **状态无损**：加工中、待处理结果、待 QC 和返工分别统计及筛选，普通生产卡片的旧状态压缩已删除。
6. **查询边界**：候选所需数据使用固定批量查询，当前页详情批量延迟加载；代码中不存在按位置循环发起数据库查询。
7. **数据库一致性**：开放工单部分索引在 SQL 与 ORM 中名称、字段顺序和条件一致，未改变业务数据结构。

### 第二阶段代码审查结论

- 代码规范：生产位置、库存来源、开放活动和装配投入使用独立命名与可判别响应类型；装配两种互斥开单来源没有被错误合计。
- 低耦合：跨域聚合归 `planning`，工单活动归 `production_core` 窄读取接口，部门层仅组合 capability，Router 仅负责 HTTP；未新增跨模块私有实现访问。
- 高稳定：复用现有 `reserved_quantities()` 和 `work_order_stage()`，不修改生产、装配、QC 或库存命令；开放工单在库存来源耗尽后仍可见，SQL/ORM 索引同步。
- 只读审查：已完成本阶段 Python 文件 AST 语法检查、项目内导入路径检查、未使用导入检查、能力/路由/契约引用搜索、SQL/ORM 索引对照及 `git diff --check`。未运行构建、项目脚本、服务或数据库命令。

### 第三阶段实际修改与重要发现

- 在 `production_core.workbench_read_api` 增加精确位置工单窄读取接口。普通位置固定校验标准工单类型、生产项、当前节点和所属部门，再按来源节点或当前库存来源的直接 `source_work_order_id` 关联并去重；`repository_id` 不作为历史工单条件。
- 装配位置只查询输出生产项属于同一订单明细、且稳定装配节点一致的装配工单；批量 presenter context 同时保留首次多路工单的 `work_order_material` 投入生产项关系，后续工艺继续通过装配输出生产项关联。
- 新增 `planning.production_workbench_orders` 读模型，工单明细继续复用 `WorkOrderResponse` 权威序列化结果；没有建立第二套工单详情字段或按钮状态规则。
- 新增按 `procedure_id + is_temporary` 分组的工艺汇总。普通工单使用现有标准进度单位，装配工单统一换算为装配输出单位；同一工艺拆分工单累加，不同工艺之间不生成位置总体进度。
- 工艺汇总提供工单数、开放工单数、开单量、加工中、待处理结果、待 QC、合格、返工、报废和遗失数量。工单列表支持按工艺 ID 与临时身份过滤，但工艺汇总始终覆盖所选位置全部有效工单。
- 临时工单与普通工单即使引用同一工艺库记录也按 `is_temporary` 分组，临时身份不会被合并或反向污染已确认工艺配置。
- 已取消工单继续出现在关联工单列表供追溯，但不进入工艺汇总，避免把数据库约束下仍存在的任务数量误算为加工中数量。
- 新增类型化响应、部门 capability 方法和只读 HTTP 路由。位置类型、工艺筛选和身份参数由 Router 与领域类型约束，业务关联和汇总不进入 HTTP 层。
- 查询只在用户选中的单个生产位置内加载工单、QC 批次、投入关系和展示上下文；全部关系批量加载，序列化循环不逐工单查询。现有工单位置索引具备所需前缀，本阶段未发现需要增加 SQL/ORM 索引的缺口。

### 第三阶段完成条件核对

1. **普通位置关联准确**：查询同时限制标准工单类型、生产项、当前节点、来源节点/当前直接来源工单和所属部门，不会混入其他位置或其他部门工单。
2. **装配位置关联准确**：查询按订单明细、装配输出生产项和稳定装配节点限定，首次多路投入关系由既有工单投入快照进入权威序列化上下文。
3. **同工艺拆单汇总**：分组键为 `procedure_id + is_temporary`，同组多张有效工单的所有数量逐项累加。
4. **无伪总体进度**：接口只返回工艺分组列表，不返回跨工艺的完成比例或累计总进度。
5. **单位一致**：普通工单沿用物料单位；装配开单量、加工中、待处理结果和合格量使用现有装配输出进度算法，QC 与异常量沿用输出批次数量。
6. **临时身份保留**：响应和工艺筛选均保留 `is_temporary`，普通与临时工单不会因工艺 ID 相同而合并。
7. **权威工单详情**：关联列表直接使用现有 `WorkOrderResponse` 序列化结果，QC 记录、撤销信息和操作资格口径没有复制。
8. **查询稳定性**：所选位置关系批量加载，不存在逐工单数据库查询；未新增业务表、字段、约束或执行状态转换。

### 第三阶段代码审查结论

- 代码规范：位置查询、进度投影、工艺汇总和 HTTP 契约分别承担单一职责；字段名明确表达数量阶段，取消工单规则有显式边界。
- 低耦合：工单 ORM 查询与权威进度投影归 `production_core`，跨域工艺汇总归 `planning`，部门层仅组合 capability，Router 只负责权限和参数；模块边界静态审查通过。
- 高稳定：关联只使用稳定业务身份和当前直接来源，不递归伪造批次链；复用 `calculate_work_order_progress()`、`calculate_assembly_output_progress()` 和 `WorkOrderResponse`，未改变工单、QC、装配或库存命令。
- 只读审查：已完成 9 个相关 Python 文件 AST 语法检查、项目内导入路径与未使用导入检查、模块 descriptor 边界检查、工艺汇总响应键与 schema 对照、关联条件搜索及 `git diff --check`。未运行构建、项目脚本、服务或数据库命令。

### 第四阶段实际修改与重要发现

- 普通生产部门页面已切换到独立生产工作台前端契约，直接调用第二、三阶段建立的在位物料与位置工单接口；冲压、CNC、抛光和外协共用同一页面组合，装配部专属页面未在本阶段提前修改。
- 左侧替换为紧凑在位物料列表，明确展示物料编号与名称、产品、订单、当前车间、来源完成状态、在位数、占用数、可开工数、来源数、加工中、待处理、质检中、返工中、到达时间和最近加工时间；不再显示含义错误的“任务数”，也不再使用一个综合 Tag 代替全部工单情况。
- 所选位置增加独立概况区，展示订单、客户、产品版本、物料、车间、来源完成状态及库存数量。一个位置存在多个库存来源时不自动替用户选择，必须先选择具体来源；开单弹窗使用该来源自己的可开工数和已确认工艺候选。
- 增加工艺汇总区，按后端返回的 `procedure_id + is_temporary` 稳定身份展示已配置/临时工艺、工单数、开放工单数及各阶段数量。点击工艺只过滤关联工单，不生成跨工艺总体进度；接口在同一次请求中持续返回位置级完整工艺汇总，没有重复详情请求。
- 工单大卡片已改为紧凑关联工单列表；点击工单进入详情抽屉查看完整数量、备注和逐批 QC 记录。送检、确认合格、返工送检、撤回、取消和普通/抛光打印操作均已迁移到详情抽屉。
- 工单列表和详情统一复用 `productionWorkOrderCardPolicy`。送检、直接结果、返工送检、撤回和取消的显示条件全部归入同一策略；页面组件不再维护第二套按钮状态判断。
- 开单弹窗对旧整张仓位卡片的依赖已收窄为独立 `WorkOrderCreationTarget`，新旧调用方只提供开单所需字段；没有把生产工作台读模型反向耦合进通用开单弹窗。
- 补充请求竞态和失败边界：切换位置、工艺或工单分页时先清除上一上下文的明细；位置与工单请求使用独立序列和取消控制，失败时不会把上一位置工单展示到新位置；工人和车间基础资料独立加载，单项失败不会丢弃另一项成功结果。

### 第四阶段完成条件核对

1. **主从查看路径**：普通部门页面固定形成“在位物料列表 → 所选位置概况与工艺汇总 → 精确关联工单列表与详情”的主从结构。
2. **操作完整可达**：普通与临时开单、送检、确认合格、返工送检、撤回、取消及打印均有明确入口，仍调用原有命令 API 和操作 composable。
3. **单一按钮策略**：所有工单操作可见性由 `productionWorkOrderCardPolicy` 统一决定；新列表和详情没有复制工单状态机。
4. **数量口径正确**：左侧只显示在位数、占用数和可开工数；相关新页面与组件中不存在“任务数”文案，生产计划任务数量没有进入工作台显示契约。
5. **来源不丢失**：位置可以聚合多个来源，但开单前必须选择具体 `repository_id`；工艺候选、配置确认状态和最大数量均来自所选来源。
6. **工艺汇总无伪总进度**：同工艺拆分工单由后端汇总，不同工艺只并列展示；筛选工艺不改变位置级完整汇总。
7. **普通与装配边界**：本阶段只替换四个普通生产部门共用页面；装配页面继续保留到第五阶段处理，没有引入普通页面对装配输入的猜测。
8. **稳定切换**：异步返回必须同时匹配当前请求序列和未取消信号；切换上下文后旧明细和旧抽屉不会残留。

### 第四阶段代码审查结论

- 代码规范：页面、筛选、位置列表、位置概况、工艺汇总、工单列表/详情、API 契约和页面编排按职责拆分；字段和文案直接表达在位、占用、可开工及工单阶段，没有通用数量或综合状态歧义。
- 低耦合：前端只通过 production feature 内部 API 和领域类型协作；开单弹窗依赖窄开单目标，操作执行复用原 composable，按钮可见性复用唯一 policy；没有复制后端工艺汇总、占用或工单状态算法。
- 高稳定：保留现有生产和 QC 命令接口，多个来源要求显式选择；请求具备竞态隔离和旧上下文清理，详情只在选中位置后加载且工艺筛选不增加重复请求。
- 只读审查：已使用本地 `vue-tsc --noEmit --incremental false` 完成 Vue/TypeScript 类型与导入检查，执行 `git diff --check`，并搜索普通页面旧组件/旧控制器、“任务数”文案、工作台路由引用和按钮条件来源；检查均通过。未运行构建、项目脚本、服务、数据库或网络命令。

### 第五阶段实际修改与重要发现

- 装配部页面已切换到与普通生产部门一致的“在位物料主项 → 所选位置概况与加工情况 → 关联工单列表与详情”骨架。普通单路车间位置继续使用标准生产位置，装配和焊接等多路节点使用装配位置，二者通过可判别类型组合，不再把两套数据压入旧装配卡片。
- 多路装配位置固定按“订单产品项 + 稳定装配节点”生成一个主项。首次多路输入、节点在制品后续工艺、开放工单及其投入关系只补充该主项内容，不再分别生成“未装配、等待物料、历史”等重复主卡片。
- 装配可开工口径拆分为首次多路可开工数和节点在制品后续可开工数。首次区域始终列出每项必要输入物料、单件用量、在位数、占用数、可用数、开放工单已投入数及具体来源；后续工艺必须选择一个明确的节点在制品来源，多个来源时不自动替用户决定。
- 首次多路工单继续使用既有 `work_order_material` 记录实际投入并在开单时消耗各输入来源。同一装配节点后续工艺新增一条投入来源快照，记录所选节点在制品、数量、来源完成节点和来源工单；该快照的 `repository_id` 保持为空，实际未提交占用只由 `work_order.repository_id` 记录，避免同一来源被重复计算占用。
- 后续工艺提交时校验投入快照和当前来源完全一致，再按既有时点消耗节点在制品；取消未提交的后续工单只解除工单占用，不重复恢复从未消耗的来源。首次多路工单原有消耗和取消恢复逻辑保持不变。
- 关联工单响应在工作台专属类型中增加实际投入明细，没有扩张全局 `WorkOrderResponse`。投入物料、数量、来源工单和完成状态由后端批量读取；完成状态复用统一 `material_completion_status` 投影，尚未经过执行节点的普通配件明确显示“初始物料”，前端不再拼接完成状态。
- 装配部普通单路开单继续调用标准工单命令，首次多路和节点后续工艺继续调用装配工单命令。普通/临时工单、工人选择、送检、直接结果、返工送检、撤回和取消仍复用既有对话框、操作 composable 与唯一工单策略，没有在装配页面复制状态机。
- 抽取只负责列表、选择、工艺筛选、分页和请求生命周期的公共工作台读取 composable；普通生产部门和装配部各自保留开单来源与业务操作编排。位置和工单请求继续使用独立取消与序列校验，切换主项时先清空旧工单、工艺汇总和详情，避免异步结果串位。
- `zzerp.sql` 只同步澄清 `work_order_material` 的现有职责说明；本阶段没有新增或修改业务表、字段、约束、索引及生产状态。

### 第五阶段完成条件核对

1. **装配主项唯一**：多路装配只按订单明细和稳定装配节点生成一个位置主项；输入来源、在制品来源和开放工单不会再生成平级重复卡片。
2. **首次输入清晰**：所有必要输入物料始终展开，分别展示单件用量、在位、占用、可用、已投入开放工单和具体来源，首次可开工数由后端按来源级可用量计算。
3. **后续来源清晰**：节点在制品与首次原配件输入使用不同字段和操作入口；后续工艺按具体 `repository_id` 选择并只计算一次占用。
4. **实际投入可追溯**：首次和后续装配工单均存在 `work_order_material` 投入快照；工作台详情显示实际物料、投入数量、统一完成状态和直接来源工单。
5. **装配与焊接一致**：所有多路 `assembly` 节点使用同一位置、容量、投入和工单路径，不依赖节点显示名称；焊接不会形成专用分支。
6. **单路加工不混入多路输入**：装配部所属 `process` 节点作为标准位置展示并调用标准开单与工单操作，装包等单路节点不伪装为装配输入组。
7. **历史不生成当前任务**：当前位置集合仍只来自现有在位来源与开放工单；仅有已结工单的旧位置不生成主项。已结工单只在仍存在的所选位置关联列表内用于追溯。
8. **操作口径唯一**：工单按钮继续由生产或装配工单 policy 决定，页面只按位置类型选择对应 policy，没有新增前端业务状态判断。
9. **请求上下文稳定**：切换位置、筛选、工艺和分页会中止旧请求并清空旧上下文；晚返回结果不能覆盖当前装配位置。

### 第五阶段代码审查结论

- 代码规范：装配位置列表、位置概况、输入区、工艺汇总、工单列表/详情、通用读取状态和装配开单编排职责分开；字段明确区分首次容量、后续容量、实有、占用、可用与实际投入，没有通用数量或综合状态歧义。
- 低耦合：跨域工作台投影仍归 `planning`，生产工单及投入记录读取归 `production_core`，装配命令只通过公开窄接口操作其拥有的记录；前端公共 reader 不包含装配业务，装配页面复用公共展示与既有操作策略，未访问其他 feature 内部状态。
- 高稳定：未改变装配输入消耗时点、工单结果、QC 或流转状态；投入快照与执行占用职责分离，避免重复占用，提交和取消均校验快照不变量；完成状态复用统一后端算法，请求具备竞态隔离和旧上下文清理。
- 只读审查：已对 7 个第五阶段相关 Python 文件完成 AST 语法解析，使用本地 `vue-tsc --noEmit --incremental false` 完成 Vue/TypeScript 类型与导入检查，执行 `git diff --check`，并搜索旧装配入口引用、工作台字段全链路、“任务数”文案、位置类型查询和新增跨模块内部依赖；检查均通过。未运行构建、项目脚本、服务、数据库或网络命令。

### 第六阶段实际修改与重要发现

- 删除旧生产卡片完整后端链：部门 `/repositories` 只读接口、`RepositoryCapability`、旧 `RepositoryResponse` 契约、`planning.current_production_cards`、`historical_production_cards`、`production_card_listing`、卡片过滤与状态压缩实现均已移除。生产工作台只保留类型化 `production-workbench` 位置和关联工单接口。
- 删除旧部门工单列表只读链：部门 `/work-orders` 查询、`WorkOrderCapability`、`WorkOrderListEnvelope`、`list_department_work_orders()` 和前端查询函数均已移除。创建、提交、返工送检、撤回、取消和打印等现行工单命令不受影响，关联工单只从所选生产位置工作台接口读取。
- 冲压、CNC、抛光、外协和装配的前后端部门能力统一改为 `production_workbench`，不再同时声明旧 `repositories` 和 `work_orders` 能力。部门注册、Router 和页面入口现在使用同一能力名称。
- 删除旧前端 `AssemblyGroupCards`、`RepositoryCards`、`RepositoryFilterBar`、`WorkOrderCards`，以及旧普通/装配部门 controller、旧装配分组、旧工单列表、旧仓位状态类型和相关死样式。引用图审查确认 production feature 中除路由页面外不存在无引用文件。
- 原 `departmentRepositories` 混合 API 已按职责拆分：工作台车间列表进入 `productionWorkbench` API，部门工人列表进入 `departmentWorkers` API，结单物料入库候选进入独立 `productionStorage` API；旧 `RepositoryWorkshop` 和 repository 前端接口类型已删除。
- 有效的工单阶段与来源占用算法从旧卡片文件中收敛到 `production_core.work_order_status`。跨模块工作台只通过 `workbench_read_api` 获取权威占用投影，库存与生产核心内部复用同一实现，不再保留旧综合卡片状态算法。
- 多路节点输入物料身份投影从旧卡片辅助文件迁移到职责明确的 `planning.assembly_input_projection`；只保留工作台所需的稳定输入物料键计算，没有保留历史卡片名称、来源名称或兼容字段。
- 开单稳定性审查发现：旧标准开单、首次多路分配和装配节点后续工艺分别计算来源占用，后续装配命令还可能只比较实有量。现已统一复用 `reserved_quantities()`；仓位行锁后再核对来源级可用量，防止绕过页面或并发请求重复占用同一来源。
- 普通与装配工单 policy 删除旧卡片专用的无引用字段，并把送检、直接结果、返工送检、撤回、取消和通用状态文字收敛到单一公共判断源；两类 policy 只保留各自数量指标差异。
- 更新模块边界文档，将旧生产卡片读模型登记替换为 `planning.production_workbench` 与 `production_workbench_orders`。本阶段未新增表、字段、约束、触发器或兼容分支。

### 第六阶段完成条件核对

1. **无新旧口径并行**：旧生产仓位卡片和旧部门工单列表的前端调用、HTTP 路由、部门能力、Schema、读模型及辅助算法均已删除；现行入口只剩工作台位置与位置工单接口。
2. **无无引用旧实现**：production feature 引用图检查通过；被删除的旧组件、composable、类型、样式和 API 名称在前后端引用搜索中均为零。
3. **状态与按钮算法唯一**：工单阶段来自 `work_order_stage()`，进度来自既有标准/装配进度算法；前端全部通用按钮资格和状态文字只在 `workOrderCardPolicy` 定义一次。
4. **占用算法唯一且命令兜底**：工作台、标准开单、首次多路装配和节点在制品后续开单均使用同一来源占用投影；命令在锁定来源后校验可用量，不依赖前端资格判断保证数量安全。
5. **模块边界明确**：跨域工作台归 `planning`，工单与占用投影归 `production_core`，部门层只组合 capability，Router 只处理 HTTP，前端工作台、工人和生产入库 API 按职责拆分；未新增跨模块私有实现访问。
6. **查询结构稳定**：位置候选与开放工单活动批量读取，当前页来源、到达和工艺批量延迟加载，所选位置才读取关联工单；未发现逐位置或逐工单数据库查询。工作台开放工单部分索引在 SQL 与 ORM 中保持同名、同字段顺序和同过滤条件。
7. **数量与历史口径正确**：工作台相关页面不存在“任务数”文案；在位、占用、可开工、首次多路容量和后续在制品容量保持独立。历史结单工单只在仍存在的所选位置内追溯，不生成当前主项。
8. **业务不变量保持**：没有新增物料批次表，没有改变产品版本绑定、工艺配置、整单处理、装配消耗、QC、库存或生产计划规则；本阶段删除的仅为已退出的只读展示链和重复实现。
9. **整体目标闭环**：六个阶段的实施范围、完成条件、实际代码和文档状态一致，本轮目标范围内没有以“后续整改”名义保留的问题。

### 第六阶段及整体代码审查结论

- 代码规范：删除职责混杂的旧卡片和 API，保留的工作台位置、工艺汇总、工单详情、装配输入、工人和入库接口各自职责明确；命名直接表达生产位置和实际数量，没有兼容别名或死代码。
- 低耦合：部门只依赖统一工作台 capability；跨域读模型、生产核心投影、HTTP 契约和前端展示边界清晰；通用 reader、工单 policy 和来源占用算法均为单一复用点，没有循环依赖、跨 feature 内部访问或新增跨模块 persistence 导入。
- 高稳定：来源占用在读模型与写命令中保持一致并受行锁保护；装配投入快照、工单状态、QC 与库存命令保持原有不变量；旧接口是直接删除而非兼容转发，不会继续形成双口径或隐藏分支。
- 只读审查：已完成 276 个后端 Python 文件 AST 解析和项目内导入目标检查、本地 `vue-tsc --noEmit --incremental false` 类型与导入检查、production feature 引用图检查、前端 feature 边界检查、旧接口/组件/类型全局零引用搜索、工作台数量与按钮判断搜索、SQL/ORM 工作台索引对照及 `git diff --check`，结果均通过。当前宿主 Python 未安装项目 SQLAlchemy 依赖，因此运行时模块导入不作为完成证据；未安装依赖、运行项目脚本、构建、服务、数据库或网络命令。

第一至第六阶段已全部完成，整体整改目标与最终完成条件已闭环。

# 生产任务工作台整改方案

## 整改背景

当前生产部门同时存在“生产工作台”和“生产任务”两个入口：前者按当前生产位置展示在位物料和工单，后者按生产计划任务展示任务数量和进度。两个入口的数据口径、层级和操作位置不同，用户需要来回切换，且装配任务还把输入物料作为任务同级行展示，无法快速判断一项任务当前包含哪些加工状态。

本次整改将两个入口收口为一个“生产任务”工作台。一级行只代表生产计划在当前部门、当前车间的任务；二级行只代表该任务下不同的加工状态；开工单使用独立操作列。现有工单创建、工单处理、QC、装配投入和数量规则保持不变。

## 整改目标

1. 每个一级行具有稳定、单一的任务身份，不再把输入物料伪装成任务行。
2. 一级任务下始终展开二级加工状态行，同一状态只出现一行。
3. 二级行按当前可用物料和开放工单生成；同一任务可同时出现“未加工”“某工艺中”“某工艺完”等不同物料状态。
4. 装配输入物料到货明细继续可查，但不占用任务/状态层级。
5. 表格新增独立“操作”列，操作只属于二级状态行：可用物料行开工单，工单处理状态行查看对应工单。
6. “开工单”按钮直接打开原有开工单弹窗；仅在存在多个合法流程位置、来源或装配开单方式时，先显示简洁的来源选择弹窗。
7. 生产计划草稿任务仍可见；没有到位物料或不满足开单条件时，工作台明确展示当前不可开单状态，不伪造可用来源。

## 已确认展示口径

### 一级任务

普通生产部门和装配部的任务身份均为“生产计划明细 + 稳定流程节点”。同一物料多次经过同一车间时必须形成不同任务，不能按车间合并。一级行展示物料、车间、计划任务数、到货数、完成数和备注。

装配任务的投入物料明细通过到货数 Popover 展示，保留物料编号、名称、任务数和到货数；不再作为一级任务下的伪子任务。

### 二级加工状态

二级行按以下当前状态排序展示：

1. `未加工`：当前车间存在尚未被工单占用的到位物料。
2. `某工艺中`：对应工单仍有加工中数量。
3. `某工艺待处理`：工单已完成加工，等待文员直接确认结果或送检。
4. `某工艺质检中`：已送检且 QC 尚未完成结果或去向处理。
5. `某工艺返工中`：QC 返工数量尚未完成后续处理。
6. `某工艺完`：该工艺完成后的物料仍在当前车间且具有可用数量。
7. `某工艺异常`：对应工单存在报废或遗失数量。

同一任务可以同时具有多批当前物料，例如一批未加工、一批粗光中、一批粗光完，因此这些状态行必须同时保留。`未加工`和`某工艺完`只根据当前车间实际可用仓位生成，不能根据历史完成数推断；加工中、待处理、质检中、返工中和异常根据对应工单的权威进度生成。已取消工单不进入状态汇总。

状态行只展示涉及的工艺名称；一级任务的计划任务数、到货数、完成数不复制到二级行。

一级任务的“完成数”统一表示已经完成当前稳定流程节点并实际离开该节点的数量。直接确认合格后仍留在当前节点、送检中、QC 合格后返回当前节点的数量均不计入；QC 放行下一节点、QC 存入仓库，以及装包直接进入成品待入库终点的数量计入。完成数以生产流转记录为权威来源，不按任一工艺合格数的最大值或合计值推断。

### 开工单入口

工单操作只出现在二级状态行的独立操作列：

- `未加工`和`某工艺完`行显示“开工单”，并只允许选择该行携带的具体可用仓位；继续使用已确认工艺或临时工艺创建工单。
- `某工艺中 / 待处理 / 质检中 / 返工中 / 异常`行显示“查看工单”，只展示构成该状态的相关工单；当前任务、当前稳定流程节点的全部工单由一级任务行统一进入。
- 装配任务继续区分首次多路投入和节点在制品后续工艺，沿用现有来源分配规则；两者分别由状态行携带明确开单方式。
- 工艺未确认、物料未到位或无可用数量时不得绕过现有资格判断。
- 多个合法开单来源不得自动选择，必须先由用户在来源选择弹窗中确认。

## 保持不变

- 不修改生产计划、任务数量、产品版本绑定或流程图规则。
- 不修改工单创建命令、物料占用、直接结果、送检、QC、返工和放行状态机。
- 不修改装配多路投入、后续工艺和来源数量分配规则。
- 不修改数据库表、字段、约束和 `zzerp.sql`。
- 不把状态行解释为物料批次或库存余额。

## 非目标

- 不新增物料批次表。
- 不重构 QC 部门页面、库存页面或工程部流程图。
- 不增加新的业务状态。
- 不新增旧数据兼容逻辑。

## 实施阶段

### 第一阶段：任务与状态契约

状态：已完成

实施范围：

- 为生产任务列表补充计划状态、订单明细身份和加工状态汇总契约。
- 明确状态行字段、顺序和数量含义。
- 扩展生产位置查询的精确任务过滤参数，避免按显示名称关联。

完成条件：前后端契约能够唯一识别任务，并能表达二级状态及精确工作台范围。

实际修改与证据：

- 任务列表契约新增 `customer_order_item_id`、`plan_status` 和 `processing_statuses`。
- 状态契约固定为七种展示状态，仅返回状态和涉及工艺名称。
- 生产位置接口新增订单明细、生产项和流程节点精确过滤参数；普通任务以稳定流程节点为任务身份，不再按车间或物料名称合并、关联开单位置。
- 前端类型检查已确认新增字段与接口参数一致。

### 第二阶段：后端状态汇总

状态：已完成

实施范围：

- 从现有工单和批次权威进度计算任务状态汇总。
- 普通生产与装配统一复用纯汇总函数，装配数量保持输出单位。
- 列表查询继续按当前页批量读取，禁止逐任务查询。

完成条件：每项任务返回去重、排序稳定的状态行，已取消工单不计入。

实际修改与证据：

- 新增 planning 内部纯汇总模块，普通生产与装配任务共同复用。
- 工单状态复用权威工单进度算法；装配节点复用装配输出单位算法，返工数量从加工中数量中分离。任务完成数统一从当前流程节点实际放行、入库或直达成品入库终点的生产流转记录计算，不再按工艺合格数推断。
- 当前可开工状态从扣除工单占用后的实际仓位生成；已取消工单被明确排除，无到位物料和开放工单时返回唯一“等待物料”状态。
- 状态随任务列表当前页已有工单和批次数据批量生成，没有新增逐任务查询。

### 第三阶段：任务树表格

状态：已完成

实施范围：

- 重写生产任务表格为一级任务、二级状态的常驻展开树表。
- 增加工艺和独立操作列。
- 装配输入物料改为到货明细 Popover。

完成条件：表格每个一级行均为任务、每个二级行均为不同状态，不再混入输入物料行。

实际修改与证据：

- 任务树改为 `task` 一级行和 `status` 二级行的可判别类型，并使用装配部原有的文件夹目录视觉始终展开。
- 表格新增车间、加工工艺和独立操作列；操作只显示在二级状态行，不展示容易混淆的状态数量或工单数量。
- 装配投入物料改由到货数 Popover 展示，不再生成物料伪子任务。
- 旧物料伪子任务和合并单元格逻辑已删除；仅复用文件夹、文件和分支线视觉表达任务与状态层级。

### 第四阶段：任务行直接开单

状态：已完成

实施范围：

- “开工单”按稳定任务身份载入对应生产位置。
- 复用原有普通和装配开工单弹窗，不增加中间工作台。
- 多来源或多开单方式使用独立、简洁的来源选择弹窗。

完成条件：用户从可开工状态行进入准确的原开工单弹窗；多来源时先明确选择，无显示名称误关联。

实际修改与证据：

- “开工单”从二级可用物料状态行进入原开工单弹窗；草稿计划按钮禁用，普通任务尚无生产项时不执行模糊查询。
- 唯一合法来源自动选定；多个合法来源、流程位置或装配开单方式先进入来源选择弹窗。
- 来源选择只判断实际可用数量；工艺配置与具体开单资格继续由原开单逻辑给出准确结果，不再统一误报“暂无可开工物料”。
- 普通任务按订单明细、生产项和稳定流程节点限定；装配任务按订单明细和稳定流程节点限定；同一任务存在多个合法来源时保留来源选择。
- 普通与装配工单创建仍调用原有 composable 与命令接口，未复制开单资格判断。

### 第五阶段：入口收口与审查

状态：已完成

实施范围：

- 部门默认入口统一命名为“生产任务”，删除重复的独立生产任务 Tab。
- 清理旧任务物料树、无引用参数和重复页面装配。
- 只读审查导入、类型、模块边界、重复逻辑和明显生命周期问题。

完成条件：只保留一个任务工作入口；代码符合规范、低耦合和高稳定要求，范围内无未解决问题。

实际修改与证据：

- 生产部门与装配部默认 Tab 统一为“生产任务”，删除独立进度 Tab 和旧工作台筛选入口。
- 删除无引用的 `ProductionWorkbenchFilterBar.vue`、车间筛选前端类型/API 及对应专用路由。
- 新增共享来源选择弹窗，普通生产与装配共用同一选择界面，并继续复用各自原有开工单弹窗。
- `git diff --check` 无格式错误；277 个后端 Python 文件通过 AST 解析；Vue/TypeScript `--noEmit` 类型检查通过；旧入口和旧物料树关键字搜索无残留。

## 风险与控制

1. 状态分组来自工单加工进度而非库存余额，UI 不展示容易被误解为物料余额的状态数量。
2. 同一物料可能多次经过同一部门或同一车间，任务必须按稳定流程节点拆分；开单同时使用任务节点和来源稳定 ID 过滤，禁止名称匹配。
3. 装配输出单位与投入单位不同，状态汇总必须复用现有装配输出进度函数。
4. 生产计划草稿没有生产位置，任务仍展示，但工作台只能显示无可开工物料。
5. 任务列表不得为每行调用详情接口，避免任务数量增长后产生查询放大。

## 代码审查方式

- 读取并核对实际差异、符号引用和模块依赖。
- 使用 Python AST 只读解析后端文件，检查明显语法与导入名称问题。
- 使用 TypeScript/Vue 只读类型检查能力时，不得生成缓存或构建产物。
- 检查旧双入口、旧物料树和名称关联代码是否清理干净。
- 不启动服务、不连接数据库、不执行构建、格式化或项目脚本。

## 最终结果

状态：已完成

最终结果：

- 生产部门只保留一个“生产任务”工作入口，任务一级行和加工状态二级行职责明确。
- 开工单被限定在所选任务范围内并直接使用原有弹窗，业务命令与状态机未改变。
- 代码规范审查通过：删除旧物料树和无引用筛选实现，新增类型与命名表达实际业务含义。
- 低耦合审查通过：状态汇总归 planning 读模型，工单进度继续由 production_core 权威算法提供，前端复用现有开单与工单操作 composable。
- 高稳定审查通过：使用稳定 ID 限定任务，保持草稿可见、确认后开单、完成计划可继续生产以及装配多路投入规则；未增加数据库变更、兼容分支或名称关联。

## 补充整改：工艺配置取消确认

### 背景与目标

当前工艺配置一经确认便无法恢复编辑，数据库触发器也将确认状态设置为绝对不可逆。实际业务需要在尚未开过工单时允许所属部门取消确认，修正工艺清单后再重新确认。

本次取消以“产品 + 产品版本 + 物料 + 车间流程节点”为完整配置范围。该范围只要存在任意工单记录，包括临时工单、已取消工单或已完成工单，均视为已经开始开单并禁止取消确认。取消成功只清空确认人和确认时间，保留工艺、单价和配置记录。

### 非目标

- 不删除工艺、单价、工单或历史记录。
- 不允许已开过工单的配置通过取消确认改变工艺清单。
- 不修改普通工单、临时工单、QC 或生产流转规则。
- 不增加旧数据兼容分支。

### 实施阶段

#### 第六阶段：受约束的取消确认

状态：已完成

实施范围：

- 复用生产核心现有批量工单引用投影，派生稳定配置范围的可取消状态，不增加重复查询。
- 标准执行模块新增取消确认事务命令，锁定配置并再次校验是否存在工单。
- HTTP 与前端接口补充取消确认命令。
- 工艺配置弹窗对已确认配置显示“取消确认”；已开过工单时按钮禁用，未开单时允许执行。原关闭弹窗按钮明确命名为“关闭”。
- 调整 `zzerp.sql` 的确认保护触发器：仅允许无任何范围工单时从已确认恢复草稿，其他确认字段修改继续禁止。

完成条件：

- 未开过工单的已确认配置可以取消并恢复编辑。
- 任意类型或状态的工单一旦存在，前端取消入口不可执行，后端和数据库仍共同拒绝越权状态变化。
- 前端、后端与数据库使用同一稳定范围口径，不按工艺名称或显示文字关联。
- 范围内代码通过格式、语法、类型、依赖边界和状态约束的只读审查。

### 风险与控制

1. 不能只检查配置清单内工艺是否被引用，否则临时工单会漏判；统一按产品版本、物料来源节点和车间节点判断。
2. 前端可取消状态仅用于展示，取消命令必须在同一事务内加锁并重新检查，避免列表读取后并发开单。
3. 数据库保护只承担非法状态写入底线，面向用户的业务错误由标准执行模块返回。

### 实际修改与审查证据

- 列表复用现有批量工单引用结果返回 `can_cancel`，没有新增逐项或重复工单查询。
- 取消命令复用统一配置范围解析，在事务内锁定产品与配置，并按产品、版本、物料来源节点和车间节点检查全部工单；任意工单存在即返回明确冲突错误。
- 取消只清空 `confirmed_at` 与 `confirmed_by`，工艺、单价和历史记录均保持不变。
- 前端已确认配置均显示“取消确认”；已开过工单时按钮禁用，未开单时二次确认后调用后端命令。弹窗原“取消”改为“关闭”，避免与取消确认混淆。
- `zzerp.sql` 的保护触发器使用相同配置身份判断，仅允许无工单范围撤销确认，其他已确认字段变更仍由数据库拒绝。
- `AGENTS.md` 与 `PROJECT_OVERVIEW.md` 已同步当前业务规则。
- `git diff --check` 通过；277 个后端 Python 文件通过 AST 解析且取消命令符号存在；Vue/TypeScript `--noEmit` 类型检查通过。

### 完成结论

- 代码规范：状态字段和命令命名明确，弹窗关闭与业务取消职责分离，没有临时绕过或重复实现。
- 低耦合：Router 只转发命令，状态转换归标准执行模块，工单引用继续通过生产核心公开只读接口获取，前端只消费后端资格结果。
- 高稳定：前端资格、事务内复核和数据库保护形成三层约束；普通、临时及任意状态工单都不会被漏判，且未改变现有开单、单价和 QC 行为。

## 补充整改：生产任务状态行工单操作

### 背景与目标

生产任务表原先把工单操作放在一级任务行，无法说明按钮实际操作的是哪批物料或哪组工单；同一任务同时存在未加工、加工中和工艺完成物料时，单一任务级按钮会造成来源混淆。

本次把生产操作下放到二级物料状态行。可用在位物料状态负责打开原开工单弹窗；工单处理状态负责从右侧抽屉查看构成该状态的工单。一级任务行只保留“查看全部工单”历史入口。查询按生产计划明细、产品版本、物料来源节点及稳定车间 ID 限定。

### 实施阶段

#### 第七阶段：任务工单抽屉

状态：已完成

实施范围：

- 扩展现有生产进度详情读模型，在同一次查询结果中返回当前任务范围的完整工单列表，不新增重复数据库查询；详情筛选由车间名称改为稳定车间 ID。
- 工单列表补充工艺、车间、临时工单身份和现有数量状态字段。
- 二级行改为当前物料状态，而不是历史工单状态汇总：可用在位物料显示“未加工”或“某工艺完”，开放工单按实际阶段显示“某工艺中 / 待处理 / 质检中 / 返工中 / 异常”。
- 每个状态行返回明确动作和稳定目标：可用物料行携带仓位来源并显示“开工单”，工单处理中状态显示“查看工单”；状态内工单 ID 仅用于状态行稳定身份，不能限制任务工单抽屉的历史范围。
- 新增任务工单抽屉，按当前任务和稳定流程节点以卡片展示全部工单的工单号、工艺、执行人、数量、QC 进度、异常、状态和时间，并复用原工作台工单操作。
- 工单卡片内展示该工单已经存在的真实 QC 批次，包括送检数量、检验结果、返工关联、QC 人员、时间、不良原因和合格品去向；不根据汇总数量伪造批次。
- 工单卡片按现有统一按钮策略显示“送检”和“确认合格”，分别复用普通或装配工单操作 composable、二次确认和原提交命令；操作完成后同步刷新抽屉与生产任务列表。
- 一级任务行只放置“查看全部工单”，不放开单、送检或确认等生产操作；继续保留点击物料名称查看整体生产情况。

完成条件：

- 开工单和具体状态工单操作只出现在二级物料状态行；一级任务行仅显示“查看全部工单”。
- 同一物料同时存在未加工、加工中和加工完成在位数量时，必须同时显示对应状态行，不能用单一汇总状态互相覆盖。
- 抽屉只包含所选任务在当前部门与车间范围内的工单，不按物料名称关联，不串入其他订单、版本或节点。
- 每张工单卡片只展示属于该工单的 QC 批次，批次按稳定 ID 顺序排列；没有批次时不显示空批次区。
- 送检和确认按钮资格与原生产工作台完全一致；前端不得为本抽屉复制另一套资格判断，后端不得为展示按钮新增状态转换。
- 已取消工单继续不参与当前物料状态和完成数量汇总。
- 不增加逐任务查询、工单状态机副本或新的数据库结构。

### 风险与控制

1. “工艺完”不能从历史合格数推断，必须存在当前可用仓位来源；已经流转离开当前车间的历史完成工单不生成可开单状态。
2. 同一工单不能因多个映射路径重复；状态行和抽屉均按工单 ID 去重并稳定倒序。
3. 完整列表与生产情况卡片共享同一工单进度映射，避免数量口径分叉。
4. 抽屉中的工单操作必须复用现有按钮资格、操作 composable 和打印组件，不复制工单状态判断或命令。

### 当前实施记录

- 生产任务响应同时返回稳定 `flow_node_id` 和 `processing_workshop_id`；普通生产与装配任务都按产品计划明细和稳定流程节点区分，进度详情由当前部门、车间 ID 和流程节点共同限定。
- 进度详情从当前任务范围生成顶层 `work_orders`，工单按 ID 去重并倒序返回；完整操作与打印数据由 production core 使用一个共享上下文批量序列化，不逐工单查询。
- 工单进度行与原生产情况卡片共同复用 `build_progress_work_order_row`，加工、送检、完成、返工和异常数量没有形成第二套算法。
- 二级行同时合并当前可用仓位与开放工单进度：未加工和工艺完成仓位显示“开工单”，加工中、待处理、质检中、返工中和异常状态显示“查看工单”；不同状态不会互相覆盖。
- 可开工状态携带精确仓位 ID，查看状态保留构成当前状态的工单 ID 作为稳定行身份；普通生产和装配均复用原开单弹窗与原开单资格判断，不复制状态机。
- 一级任务行提供“查看全部工单”入口；右侧抽屉以占满内容区宽度的单列工单卡片展示当前任务节点全部工单，物料名称仍用于打开整体生产情况。
- 工单卡片内已加入真实 QC 批次卡片；批次复用 production core 的统一序列化，按 ID 排序并展示送检、合格、返工、报废、遗失、待返工、QC 人员、质检时间、不良原因和合格品去向，不存在前端反推批次。
- 工单卡片已接入“送检”和“确认合格”；按钮资格复用 `canSubmitInitialQc` 与 `canSubmitDirectResult`，普通和装配分别调用原操作 composable、二次确认及提交命令，成功后重新加载当前抽屉并刷新生产任务。
- 工单卡片已补回返工送检、撤回最近生产操作、取消工单和打印入口；抛光部门继续使用原特殊打印单，所有操作资格和命令均复用原工作台公共实现。
- 后端将工单提交能力收敛为 production core 公共投影，原完整工单序列化与生产进度读模型共同复用；装配操作另外保留原始工单数量和输出倍数，卡片展示数量不会被重复换算。
- 任务定位和工作台来源查询同时使用稳定流程节点与车间 ID，不再按车间显示名称关联；同一物料重复经过同一车间时保持为独立任务，列表所需仓位、占用、工单和批次仍按当前页批量读取，没有逐任务查询。
- `git diff --check` 通过；278 个后端 Python 文件通过 AST 解析；Vue/TypeScript `--noEmit` 类型检查通过。

### 第七阶段审查结论

- 代码规范：状态行构造、可用来源投影和前端动作分发职责分离，按钮文字和位置直接对应当前行含义，没有父级残留操作或重复实现。
- 低耦合：planning 读模型只返回状态、动作、稳定目标和 production core 提供的提交能力；实际开单、送检和确认合格继续由 standard execution、assembly 和现有 composable 负责，抽屉复用统一工单进度与按钮策略。
- 高稳定：可开工资格来自扣除占用后的当前仓位，不从历史完成数推断；任务、仓位和工单均使用稳定 ID 限定，没有数据库变更、兼容分支或名称关联。

## 补充整改：开单位置查询下推与旧工作台清理

### 背景与目标

生产任务状态行已经携带订单明细、生产项和稳定流程节点，但开单位置读模型仍先读取整个部门的仓位与开放工单，再在内存中过滤当前任务。前端工作台 reader 同时保留旧工作台的工单分页请求，刷新开单位置时会额外加载当前界面不再使用的旧工单列表；对应旧组件、接口和后端查询链已经没有页面入口。

本阶段将稳定任务条件下推到仓位和开放工单数据库查询，并删除已确认无引用的旧工作台工单列表链路。普通生产按“订单明细 + 生产项 + 稳定流程节点”限定；装配按“订单明细 + 稳定流程节点”限定，继续保留该装配节点所需的多路输入与节点在制品，不能误按单一生产项裁剪。

### 实施阶段

#### 第八阶段：查询范围收敛和死代码删除

状态：已完成

实施范围：

- 仓位查询在 SQL 条件中使用订单明细、生产项和稳定流程节点，避免精确任务查询扫描整个部门。
- 开放工单活动读取接口接收相同的可选稳定身份，并在读取工单、批次和装配投入前限定范围。
- 普通精确任务只加载目标生产项展示上下文；装配任务仍加载同一订单明细下的关联生产项，以满足多路物料计算。
- 前端 reader 只负责开单位置读取和选择，刷新不再请求旧工作台工单列表。
- 删除没有调用方的旧工单分页组件、前端接口与类型，以及对应后端路由、能力方法、planning 查询和 production core 序列化代码。

完成条件：

- 精确任务开单位置查询不再先读取整个部门仓位和开放工单。
- 装配首次投入和后续工艺的合法来源保持完整，普通任务不串入同订单其他物料或节点。
- 当前生产任务页面只发出任务列表、精确开单位置和任务工单详情所需请求，不再调用旧工作台工单分页接口。
- 已删除代码不存在剩余导入、路由契约或类型引用；代码规范、低耦合和高稳定审查无本阶段范围内遗留问题。

### 风险与控制

1. 装配节点会涉及多个生产项，不能套用普通任务的单生产项过滤；只按订单明细和稳定装配节点限定仓位与工单。
2. 查询下推只使用稳定数据库字段；车间名称、物料名称和显示文本不得参与身份过滤。
3. 旧工单分页链路仅在确认没有调用方后删除；任务工单抽屉使用的生产进度详情接口与工单命令保持不变。
4. 不修改数据库结构、工单状态机、开单资格、QC 或数量算法。

### 实际修改与审查证据

- 开单位置接口改为必须提供订单明细、车间和稳定流程节点；普通任务额外提供生产项。旧关键字、关注状态和无范围部门扫描入口已删除。
- 仓位查询在读取时直接按部门、订单明细、稳定流程节点及可选生产项限定；开放工单活动通过 production core 公共读取接口使用同一范围，批次和装配投入只读取已限定工单集合。
- 普通任务展示上下文只读取目标生产项；装配任务不传单一生产项，继续读取同一订单明细下的关联物料，首次多路投入和节点在制品计算未被裁剪。
- 前端 reader 已删除旧工单分页、工艺汇总、筛选、分页和工单操作状态，只保留精确开单位置读取与选择；刷新不会再请求已废弃的旧工单列表接口。
- 删除旧工单分页前端 API、类型、六个无引用旧工作台组件、后端路由、部门能力方法、planning 查询模块和 production core 专用序列化实现；对应无引用样式一并删除。
- 全项目关键字与符号搜索确认旧接口、类型、组件和依赖没有残留；`git diff --check` 通过；276 个后端 Python 文件通过 AST 解析；Vue/TypeScript `--noEmit` 类型检查通过。

### 第八阶段审查结论

- 代码规范：reader 只负责开单位置，任务工单历史只由现有任务工单抽屉负责；删除重复分页、汇总和展示代码后职责与命名一致。
- 低耦合：planning 只组合精确任务读模型，生产活动仍通过 production core 窄接口读取；Router 与部门能力仅传递稳定身份，不访问模块内部实现。
- 高稳定：普通与装配使用不同且明确的查询范围，装配多路物料未被单生产项误裁剪；未修改工单命令、QC、数量算法、数据库结构或现有任务工单详情链路。

## 补充整改：本部门物料状态与相关工单

### 背景与目标

生产任务二级行应只表达物料在当前生产部门的加工情况，不展示 QC、仓库或下游部门的内部状态。当前状态汇总仍包含已经失效的“待处理”，把待质检和 QC 已录入但未决定去向混为不透明的“质检中”，并且无当前仓位和开放工单时固定显示“等待物料”，导致本部门已经完成的任务也可能显示错误终态。

当前“查看工单”入口还会丢弃状态行携带的工单身份，任意状态行都打开当前任务节点的全部工单。同一状态可能由多张工单共同构成，同一张工单也可能因 QC 拆分结果同时产生已送检、返工和异常数量；必须保留这种多对多事实，同时避免一次抽屉内重复工单或不同状态按钮展示完全相同内容。

### 已确认口径

- 常规二级状态只包括：等待物料、未加工、某工艺中、某工艺已送检、某工艺返工中、某工艺完、某工艺异常。
- “本部门任务已完成”只在没有当前物料状态且完成数达到任务数时作为终态提示，不表示下游、QC 或仓库状态。
- 状态行按“工艺 + 状态 + 开单方式”合并；一种状态允许关联多张工单。
- 一张工单的 QC 结果可以拆成多个物料状态；同一工单允许出现在多个状态的关联集合中，但一次抽屉内必须按工单 ID 去重。
- 状态数量是当前物料数量合计；状态行同时携带每张相关工单、每个相关 QC 批次对该状态的贡献数量，工单与批次 ID 由该唯一关联投影提供，不再维护重复 ID 集合。
- 点击二级“查看工单”只看所选状态的相关工单和批次；点击一级“查看全部工单”查看当前任务节点完整历史，抽屉内不再切换范围。

### 实施阶段

#### 第九阶段：状态数量、来源与抽屉筛选

状态：已完成

实施范围：

- 重构 planning 状态纯函数，按当前仓位、开放工单和 QC 批次生成互不重复的数量切片；删除不可达的待处理状态。
- 待质检批次以及 QC 已录入但合格品尚未决定去向的数量，在生产部门统一显示为“某工艺已送检”；不暴露 QC 内部阶段。
- 返工按尚未重新送检的返工数量和来源批次计算；异常按报废与遗失数量计算；同一状态合并多张工单并对工单、批次 ID 去重。
- 可用仓位状态补充准确可用数量；装配首次多路投入使用可装配产出数量，装配后续工艺使用扣除占用后的节点在制品数量。
- 状态行把所选状态传入任务工单抽屉并筛选相关工单、突出相关 QC 批次；一级任务行不传状态并展示全部工单，抽屉内不提供范围切换。
- 无状态时根据本部门完成数返回“本部门任务已完成”或“等待物料”，禁止已完成任务回退为等待物料。

完成条件：

- 一种状态可以正确合并多张工单；一次相关工单抽屉内每张工单只出现一次。
- 同一工单存在多种 QC 结果时，各物料状态数量保持真实；一种状态由多张工单组成时，抽屉分别展示每张工单和每个批次对该状态的贡献数量，不能在每张卡片重复显示状态总量。
- 未加工和工艺完成状态继续准确开单；工艺中、已送检、返工中和异常只查看相关工单；全部工单统一从一级任务行查看，终态二级行不重复设置入口。
- 状态列表只表达当前部门，不增加 QC 去向、仓库或下游状态。
- 不修改数据库结构、工单状态机、QC 结果和物料流转规则。

### 风险与控制

1. 不能把工单强制归入单一状态，否则混合 QC 结果会丢失物料数量；状态是数量切片，工单只是来源证据。
2. 状态总量不能简单相加后当作任务完成量，当前仓位、工单在制、QC 暂离、返工和异常各有独立含义。
3. 抽屉筛选仅影响展示，不改变工单操作资格；送检、返工送检、撤回、取消和打印继续复用原策略与命令。
4. 工艺完状态的主操作仍为开工单，不在操作列堆叠来源工单按钮；完整历史通过一级任务行的“查看全部工单”查看。

### 实际修改与审查证据

- 一级任务行补充完整状态数量总览，不再把多种并存状态压缩成一个最高优先级 Tag。草稿计划显示“计划未确认”；已确认或已完成计划按等待物料、可开工、加工中、已送检、返工中、异常、本部门完成分别汇总数量。一级只聚合二级当前状态，并单独使用权威完成数展示本部门完成数量；二级继续解释具体工艺、状态及工单来源。
- 状态纯函数删除“待处理”，将未录入结果的送检批次和已录入但合格品尚未决定去向的数量统一投影为生产部门“某工艺已送检”；QC 内部阶段未进入任务二级状态。
- 当前状态固定为等待物料、未加工、工艺中、已送检、返工中、工艺完、异常；没有当前状态且完成数达到任务数时返回“本部门任务已完成”，不再错误回退到等待物料。
- 普通与装配可用来源均返回扣除工单占用后的数量；装配首次投入按各物料 BOM 用量计算可装配产出数量，后续工艺按节点在制品可用数量计算。
- 同一“工艺 + 状态 + 开单方式”合并多张工单；状态响应使用 `related_work_orders` 和 `related_batches` 分别记录每张工单、每个批次的贡献数量，没有重复维护单纯 ID 集合。
- 同一工单的合格待去向、待返工和异常数量可以分别进入不同状态；各状态总量、工单贡献量和批次贡献量分别累加并保持一致。
- 点击二级状态行后，抽屉仅显示相关工单并按 ID 去重；具有批次来源的状态只展示并突出相关 QC 批次，每张工单和批次显示自身贡献数量。点击一级任务行“查看全部工单”才展示当前任务节点完整历史，抽屉内已删除范围切换按钮。
- 工单操作完成后刷新当前关联工单的最新数据，并保持打开抽屉时由一级或二级入口确定的范围；不得自动混入其他工艺。送检、直接结果、返工送检、撤回、取消和打印仍复用原操作策略。
- `git diff --check` 通过；276 个后端 Python 文件通过 AST 解析；Vue/TypeScript `--noEmit` 类型检查通过；状态旧字段与不可达状态搜索无任务状态残留。

### 第九阶段审查结论

- 代码规范：状态聚合、贡献数量、抽屉过滤和工单操作职责分离；状态总量不会重复显示成每张工单数量，命名直接表达本部门业务含义。
- 低耦合：planning 只生成只读状态和来源贡献，工单进度继续复用 production core 权威算法；前端筛选只消费契约，不复制 QC 或工单状态机。
- 高稳定：支持一种状态多张工单及一张工单多种结果的真实多对多关系；相关模式和全部模式均按工单 ID 去重，未修改数据库、工单命令、QC 结果或物料流转规则。

## 补充整改：QC 质检结果撤回

### 目标与边界

- QC 可撤回已经录入但尚未决定合格品去向的质检结果。
- 已返回当前车间、已放行、已入库，或已经基于该结果产生返工复检批次时禁止撤回。
- 普通与装配工单撤回后保留原送检批次，删除本次质检产生的返工、报废和遗失流水，清空质检结果并恢复为待质检；若工单仅因该结果结单，则恢复为开单状态。
- 委外加工的每次质检结果本身就是独立分批记录，未放行时撤回即删除该次记录，不影响其他分批结果。
- 撤回资格由后端在事务内锁定并复核；前端按钮只提供入口，不承担业务裁决。

### 实施阶段

#### 第十阶段：质检撤回命令与入口

状态：已完成

实施范围：

- 在 production core 提供受约束的批次结果撤回原语，并通过 quality 编排统一处理普通、装配和委外质检。
- 数据库触发器只在当前事务携带匹配批次身份时，允许删除该次结果生成的 QC 流水并把该批次恢复为未质检状态；其他历史修改继续拒绝。
- QC 普通批次卡片和委外分批表格增加“撤回质检”按钮、二次确认、提交状态和成功后刷新。
- 新增单一 QC 撤回接口，不复制各工单类型的前端撤回逻辑。

完成条件：

- 合法撤回后普通/装配批次重新进入待处理，委外累计数量扣除被撤回批次。
- 已决定去向、已有返工复检或存在非本次结果流水的批次由后端明确拒绝。
- SQL 基线、后端事务、接口契约和前端按钮口径一致，不引入兼容分支或绕过历史保护。

### 实际修改与审查证据

- 新增统一 QC 撤回命令，先锁定工单、质检批次、返工复检批次和关联生产流水，再复核结果、去向与后续引用；并发放行或返工复检不会绕过撤回限制。
- 普通与装配质检撤回仅删除该次结果生成的 `qc_rework`、`scrap`、`lost` 流水，保留原 `process` 或 `assembly_output` 送检流水；批次恢复为待质检，因该结果结单的工单恢复为开单状态。
- 委外加工未放行的分批质检撤回后删除该次独立批次；存在任何关联生产流水时拒绝撤回，不影响其他委外分批结果。
- `zzerp.sql` 的历史保护只在事务设置的批次 ID 与实际批次一致、去向未决定且没有返工复检时允许删除结果流水并清空结果；其他 QC 批次和生产流水仍保持不可变。
- 普通 QC 待处理与历史记录均根据服务端资格显示“撤回质检”；委外加工每个未放行分批显示同一操作。两类页面复用一个接口、统一二次确认和后端错误信息。
- `git diff --check` 通过；275 个后端 Python 文件通过 AST 解析；Vue/TypeScript `--noEmit` 类型检查通过。

### 第十阶段审查结论

- 代码规范：撤回资格、事务命令、部门入口和页面交互职责分离，没有用前端隐藏代替后端约束。
- 低耦合：QC 编排只负责权限与事务，production core 维护批次、工单和流水的一致恢复，普通、装配和委外共用单一撤回入口。
- 高稳定：撤回保留原始送检事实，只逆转尚未被使用的质检结果；数据库受约束通道、事务锁和后端复核共同阻止已流转数据被修改。

## 补充整改：QC 扁平工单卡片

### 目标与口径

- 普通/装配 QC 工作区在“生产工单质检”下使用状态选择切换待处理与历史记录，并展示扁平工单卡片列表，不再嵌套物料分组或批次卡片。
- 每张卡片只代表一张工单，订单与物料作为工单上下文字段；首次送检和返工复检作为卡片内独立批次明细，保留编号、来源关系、数量、结果、去向和操作。
- 分页以物料为单位，不能先按批次分页再在前端分组，避免同一物料或工单被截断到不同页。
- 待处理与历史记录继续使用同一结构；同一工单可以同时出现在两个视图，但每个视图只包含符合该视图条件的批次。

### 实施阶段

#### 第十一阶段：QC 分组读模型与扁平工单卡片

状态：已完成

实施范围：

- 将 QC 列表读模型改为按稳定生产物料分页，批量加载该页相关工单、全部批次上下文及当前视图批次。
- 响应明确返回物料、工单和批次三类契约；批次补充工单内绝对序号及返工来源批次序号，前端不得按当前过滤结果猜测编号。
- 将当前页物料响应在前端投影为扁平工单列表，每张工单只生成一张卡片，不增加第二套后端接口或分页口径。
- 卡片结构和视觉与生产部门工单卡片保持一致；批次明细展示送检数、合格、返工、报废、遗失和状态，操作仅使用 QC 的录入结果、决定去向和撤回质检。
- 录入 QC 结果、合格品去向和撤回质检继续调用现有命令，只调整入口归属和加载状态。
- 删除旧批次卡片组件和批次级分页契约，不保留双展示逻辑。

完成条件：

- 同一工单只出现一张顶层卡片，每个匹配质检批次只在所属工单内出现一次；当前页不会截断该物料的工单或匹配批次。
- 返工复检明确显示“第 N 批返工复检（来源第 M 批）”，操作只影响该批次。
- 待处理与历史记录的数量、搜索、分页和空状态准确；没有逐物料或逐工单查询。
- 类型、模块边界、数据库业务规则和现有 QC 命令保持一致。

### 实际修改与审查证据

- QC 列表接口改为按 `production_item_id` 分组并分页，查询先取得当前页物料身份，再批量读取该页符合待处理或历史口径的工单批次；总数表示物料数，不再使用批次数冒充分页总数。
- 每页工单、全部批次上下文、装配投入和展示资料仍按集合批量加载；没有逐物料或逐工单查询。当前视图批次与全部批次上下文分离，既避免返回无关批次，又能计算绝对批次编号和返工来源编号。
- 响应拆分为物料、工单、质检批次三个明确契约。工单内批次返回 `batch_sequence` 与 `rework_source_batch_sequence`，前端不会因待处理/历史过滤而把返工复检错误编号。
- 普通/装配 QC 页面已改为扁平工单卡片列表，物料和订单作为工单卡片上下文，不再保留物料容器、工单分组容器、三级表格或独立批次卡片。
- 生产工单质检删除“待处理 / 历史记录”Tab，在搜索框左侧使用状态下拉选择切换相同后端筛选口径；工单卡片和分页直接位于内容区，不套用额外的 `production-card` 外层卡片。
- 工单卡片沿用生产部门工单卡片的信息层级和视觉；批次作为卡片内明细展示送检数、合格、返工、报废、遗失、状态及 QC 专属操作。
- QC 查询按待处理/历史视图筛选批次，因此工单卡片不把当前视图批次合计冒充整张工单总量；准确数量仅在对应批次明细中展示。
- 工单卡片标题使用“工单WO-...”格式，物料编号与名称和工艺名称在标题下方同一行展示，物料位于工艺左侧；卡片不展示订单字段。批次名称与质检时间使用 `-` 合并为同一标题，下一行展示 QC 工人和状态。QC 去向使用“送回 / 放行 / 存入仓库”短文案，不显示额外的待决定说明或重复返工指标。
- 委外加工质检使用同一工单卡片信息层级：标题为工单号，物料位于供应商和加工工艺左侧，批次由表格改为卡片内明细；仅保留委外的分批录入、单批放行和撤回按钮。
- 委外加工质检内容区同样不套用额外的 `production-card` 外层卡片。
- 首次送检显示“第 N 批首次送检”；返工批次显示“第 N 批返工复检（来源第 M 批）”。录入结果、决定去向和撤回质检继续以具体批次 ID 调用原命令。
- 旧 `QcBatchCards`、批次级响应和旧批次列表接口已删除，没有保留前端分组或双展示兼容逻辑；production core 内部按单张工单读取批次的命令辅助函数不属于列表接口并继续供 QC 状态计算复用。
- `git diff --check` 通过；本阶段改动的 QC 工单卡片和录入弹窗未产生 Vue/TypeScript 类型错误，旧物料嵌套卡片、三级树行和批次表格实现引用搜索无残留。项目级 `vue-tsc --noEmit` 仍报告通用工作台 reader 和装配页面中 2 处本阶段范围外的既有类型问题，本阶段未越界修改。

### 第十一阶段审查结论

- 代码规范：物料分页、扁平工单投影、批次状态和卡片展示职责明确；页面只保留一种工单卡片结构。
- 低耦合：前端只渲染服务端提供的稳定层级与批次关系，质检、去向和撤回仍由原有 QC 命令负责，没有复制状态机。
- 高稳定：同一物料和工单不会被批次分页截断；返工复检即使与来源批次分属待处理和历史视图，也保留工单内绝对编号和来源关系。

#### 第十二阶段：生产任务合并异常二级行

状态：已完成

实施范围：

- 将同一生产任务下的多个异常状态合并为一条二级异常行，并在一级任务 Tag 中同步展示异常总量。
- 异常二级行保留涉及工艺和异常总量，操作文案改为“查看情况”。
- 复用状态读模型已有的关联工单和关联 QC 批次，打开只读异常情况抽屉；抽屉只展示异常所属工单和异常批次，不展示生产执行按钮。
- 保持后端异常计算、报废/遗失数量、工单和 QC 数据不变。

完成条件：

- 每项存在异常的物料只生成一条二级异常行，并在一级任务中生成一个异常 Tag；多个异常工单和批次不丢失、不重复。
- 异常行和普通加工状态共同保持任务二级目录结构，目录末尾判断准确。
- “查看情况”只展示当前物料的全部异常批次及所属工单，不能从只读异常视图执行工单操作。

实际修改与证据：

- 任务树投影将同一任务下全部 `exception` 状态的工艺、工单数量和批次数量按稳定 ID 合并，只生成一个二级异常行；普通状态继续生成二级目录行。
- 一级任务概览使用同一组异常状态汇总生成一个异常 Tag；异常二级行展示涉及工艺和异常总量，操作固定为“查看情况”。
- 异常抽屉复用生产进度详情已有的工单和 QC 批次，只按合并后的关联 ID 过滤；工单与批次分别显示异常贡献量，报废、遗失、不良原因和所属工单关系保持可见。
- 异常模式隐藏返工送检、送检、确认合格、打印、撤销和取消等执行入口，抽屉保持只读；普通状态和全部工单抽屉行为未改变。
- 未修改后端状态汇总、接口、数据库或异常数量口径。`git diff --check` 通过；项目级 Vue/TypeScript 检查未报告本阶段文件错误。检查仍只报告 `useProductionWorkbenchReader.ts` 与 `AssemblyDepartmentView.vue` 中本阶段开始前已存在的两项无关泛型类型错误。

审查结论：

- 代码规范：异常合并集中在纯任务树投影函数，表格复用统一二级状态行，抽屉只根据明确异常模式控制只读视图。
- 低耦合：复用后端已有异常关联工单和批次契约，没有新增接口、复制异常算法或修改生产状态机。
- 高稳定：同一物料只生成一条异常二级行和一个一级异常 Tag，二者均来自同一异常状态集合；关联数量按稳定 ID 合并，只读异常视图不会触发任何生产写操作，普通任务和工单操作路径保持不变。

#### 第十三阶段：临时工单单价配置行

状态：已完成

整改背景：

- 临时工单创建时已经保存独立的 `work_order_pay_detail`，但单价固定为空，当前“工艺与单价配置”只能维护正式配置，导致临时工单始终进入未计价数量。
- 临时工艺不得加入产品版本已确认工艺清单，否则会污染普通工单可选工艺并破坏临时工单与正式配置的边界。

实施范围：

- 在生产核心公开只读接口中提供当前部门临时标准/装配工单的分页引用，不暴露生产核心 ORM。
- 在标准执行模块中读取并更新其拥有的 `work_order_pay_detail.unit_price`，一张临时工单对应一条独立单价配置行。
- 将临时工单单价行与正式工艺配置行放入同一表格和同一分页，并增加“类型”列明确区分“正式”和“临时”。
- 正式行继续使用配置弹窗；临时行展示工单号、产品版本、物料、车间、工艺和当前单价，并允许所属部门保存或清空单价。
- 临时工单单价变更立即作用于该工单的工资计算；不创建 `procedure_price`，不修改正式工艺配置，也不改变其他临时工单。
- 复用现有数据库表和非负单价约束，不修改 `zzerp.sql`。

完成条件：

- 创建临时工单后，所属部门能在“工艺与单价配置”统一列表查到唯一对应行。
- 正式和临时数据使用同一服务端分页口径，页面不得拼接两个独立分页结果。
- 修改一条临时工单单价只更新该工单的计价快照，正式配置和其他工单不受影响。
- 临时标准工单与临时装配工单使用同一接口和界面；取消、结单等工单状态不删除历史计价行。
- 分页查询不产生逐工单数据库查询，跨模块仅通过公开窄接口协作。

非目标：

- 不允许从该行修改工艺、工人、数量或工单状态。
- 不把临时工艺加入产品版本正式工艺配置。
- 不改变正式工艺的单价同步规则。

实际修改与证据：

- 生产核心新增临时标准/装配工单计价引用，只返回工单、产品版本、稳定流程位置、工艺和状态等只读身份；所属部门的工艺 ID 集合在查询入口完成隔离。
- 标准执行模块按页批量读取临时工单引用、工程流程投影和 `work_order_pay_detail`，没有逐工单查询；单价保存使用行锁，只更新指定工单的计价快照。
- “工艺与单价配置”Tab 使用同一表格展示正式配置与临时工单，一张临时工单一行；新增“类型”列，正式行显示“配置”，临时行直接填写非负单价、清空或保存。
- 同一个列表接口统一计算正式配置数与临时工单数，并按“临时工单在前、正式配置在后”的稳定顺序执行一套分页；独立临时列表接口和前端两套列表状态已删除。
- 切换回单价 Tab 时自动重新读取，创建临时工单后无需刷新整个网页即可看到对应行；临时工单取消或结单后行仍保留并显示工单状态。
- 正式工艺配置接口、`procedure_price`、工艺确认规则和普通工单开单资格均未修改；未新增数据表或旧数据兼容逻辑。
- Python 文件语法解析与 `git diff --check` 通过。项目级 Vue/TypeScript 检查未报告本阶段文件错误，仍只报告 `useProductionWorkbenchReader.ts` 和 `AssemblyDepartmentView.vue` 中本阶段开始前已存在的两项无关泛型类型错误；本机 Python 环境未安装 FastAPI，因此未执行应用导入。

审查结论：

- 代码规范：临时工单引用、计价应用流程、HTTP 契约和页面交互职责分开，命名明确，没有把临时工单塞入正式配置分支。
- 低耦合：标准执行只通过生产核心 `reference_api` 获取工单身份，只修改自身拥有的 `work_order_pay_detail`；前端使用独立类型和接口，不复制工资算法。
- 高稳定：一张工单唯一计价记录的数据库约束继续生效，单价非负约束继续生效；更新使用工单与工艺双重匹配及行锁，不能跨部门或误改其他工单。

#### 第十四阶段：装配任务计划数量口径

状态：已完成

整改背景：

- 装配部生产任务当前直接使用装配计划项的 `planned_production_quantity`；该字段由订单需求展开形成，不能表达业务部填写普通配件计划数量后计算出的产品计划数。
- 生产计划对外使用的产品计划数由同一订单产品下全部普通配件计划数量通过 `planned_product_quantity` 统一计算。

实施范围：

- 装配任务按“产品计划数 × 当前装配节点产出件数”计算任务数量。
- 同一任务的列表任务数、装配到货明细和加工状态汇总共用该任务数量，禁止仅修改前端显示。
- 保持订单数量、生产计划保存规则、BOM 展开、库存和工单数量规则不变。

完成条件：

- 业务部计划数量高于订单需求时，装配部任务数随产品计划数增加，不再停留在订单数量。
- 一个装配节点每次产出多件时，任务数保持装配体件数口径；到货明细仍正确换算所需输入物料。
- 查询按当前页订单产品批量计算计划数，不产生逐任务查询。

实际修改与证据：

- 装配任务查询对当前页涉及的订单产品一次性读取全部生产计划项，并复用生产计划既有的 `planned_product_quantity` 算法计算业务部保存的产品计划数。
- 每个装配任务按“产品计划数 × 装配节点 `output_pcs`”生成装配体件数口径的任务数；当前节点及其后续装配部工序共用相同数量。
- 装配到货明细、加工状态汇总和一级任务行均使用同一个 `task_quantity`，没有新增前端换算或第二套数量算法。
- 未修改订单、生产计划写入、BOM、库存、工单或数据库结构。Python 语法解析与 `git diff --check` 通过。

审查结论：

- 代码规范：数量换算在装配任务读模型中集中完成，变量明确表达产品计划数和装配体任务数。
- 低耦合：直接复用 planning 模块既有产品计划数量算法，前端和其他模块不重复计算。
- 高稳定：当前页只增加一次批量计划项查询；任务展示、到货换算和状态数量保持同一口径，订单与工单状态机不受影响。

#### 第十五阶段：装配等待物料明细

状态：已完成

实施范围：

- 多路装配工艺的二级“等待物料”行逐项展示全部投入物料的到货数和任务数。
- 复用装配任务已有 `material_arrivals` 契约，不新增接口、查询或前端数量换算。
- 单路任务及可开工、加工中、质检中、返工中、完成和异常状态行保持原样。

完成条件：

- 等待多种物料时每一种投入物料都可见，不再只显示汇总等待数量。
- 未到、部分到货和已到齐具有明确但简洁的视觉区分。
- 展示数量与一级任务到货明细使用同一服务端数据。

实际修改与证据：

- 二级“等待物料”状态行直接遍历任务已有的 `material_arrivals`，逐项展示物料名称及“到货数/任务数”。
- 到货数为零显示灰色、部分到货显示黄色、达到任务数显示绿色；同一多路节点的所有投入物料同时可见。
- 仅当状态为未开始、操作为不可开单且存在装配投入明细时展示，不影响其他二级状态和普通生产任务。
- 扩展树形分支线以适应明细增加后的行高，避免多项物料导致目录连接线中断。
- 未修改后端接口、查询、数量算法或数据库。`git diff --check` 通过；项目级 Vue/TypeScript 检查未报告本阶段文件错误，仍只存在两个此前已记录的无关泛型类型错误。

审查结论：

- 代码规范：等待条件和到货颜色由两个明确的小函数负责，模板只负责逐项展示。
- 低耦合：完全复用装配任务既有到货契约，没有新增前端推算或跨模块依赖。
- 高稳定：一级到货 Popover 与二级等待明细来自同一数据源；单路和其他生产状态行为保持不变。

#### 第十六阶段：QC 实际送检批次表格

状态：已完成

整改背景：

- 当前普通/装配 QC 工作区按生产物料分页，并把工单和质检批次嵌套在卡片中；页面顶层对象不是实际到达 QC 的送检批次，批次状态和可执行操作不便直接核对。
- 仅在前端把现有物料分页结果摊平成表格会造成每页行数和总数失真，因此列表契约必须同步改成质检批次口径。

实施范围：

- 将普通/装配 QC 列表改成服务端按 `work_order_batch` 分页，一行只对应一个真实送检批次；搜索、待处理/历史筛选和总数使用同一批次口径。
- 批次行一次返回物料、工单、工艺、批次绝对序号、返工来源、送检与质检结果、去向和操作资格，前端不自行拼接物料或推导返工关系。
- QC 工作区改为单层表格，操作列按批次当前状态提供“录入”“查看”“送回”“放行”“存入仓库”“撤回质检”；不可执行的操作不展示。
- “查看”使用只读详情弹窗展示该批次完整结果和去向；“录入”继续使用现有录入弹窗，去向决定和撤回继续调用现有后端命令。
- 删除旧物料/工单嵌套卡片契约和组件，不保留双展示或旧接口兼容逻辑。
- 委外加工质检保持独立页面和分批规则，不纳入本次普通/装配 QC 表格。

完成条件：

- 表格每一行与一个 `work_order_batch.id` 一一对应；同一工单多次送检或返工复检分别显示，且不会重复或被物料分页截断。
- 待处理、历史、搜索、分页总数都按实际批次数计算；查询不产生逐批次数据库访问。
- 未录入批次可录入；已录入批次可查看；符合资格时可决定去向或撤回，按钮资格仍由后端返回并在写命令中复核。
- 类型契约、模块依赖和现有 QC 状态机保持一致，无临时兼容分支。

实际修改与证据：

- 普通/装配 QC 查询已改为直接按 `work_order_batch.id` 倒序分页，总数、待处理/历史筛选和关键字搜索均在同一个批次查询上完成；同一工单的首次送检和返工复检各自返回一行。
- 当前页批次、所属工单、全部批次上下文、物料和流程展示资料均按集合批量读取；全部批次上下文只用于计算工单内绝对批次序号、返工来源、撤回资格和可选去向，没有逐批次查询。
- 后端响应改为单层 `QcInspectionBatchRowResponse`，明确包含物料、工单、加工工人、工艺、批次数量、质检结果、返工来源、去向和操作资格；旧物料/工单嵌套响应和旧 `/qc/material-work-orders` 接口已删除，不保留兼容分支。
- QC 生产工单质检工作区已改为单层 `ElTable`，一行对应一个稳定批次 ID；列宽使用项目统一的浏览器持久化能力，表格展示批次、物料、工单、工艺、送检及结果数量、QC 工人、质检时间和状态。
- 操作列根据服务端状态提供录入、查看、送回、放行、存入仓库和撤回质检。查看弹窗只读展示完整批次、返工来源、加工/QC 工人、质检结果、不良原因和去向；所有写操作继续调用原有 QC 命令并由后端事务复核。
- 委外加工质检页面、分批录入规则和接口未修改。旧普通 QC 工单卡片组件及前端嵌套类型已删除。
- 全部后端 Python 文件通过 AST 解析；前端 Vue/TypeScript `--noEmit` 检查通过；旧接口、旧嵌套类型和旧卡片引用搜索无残留；`git diff --check` 通过。

审查结论：

- 代码规范：批次查询、类型契约、表格展示、录入弹窗和只读详情各自职责单一，页面不再维护嵌套卡片投影。
- 低耦合：列表只通过 quality 公共 API 和部门能力暴露，前端只消费扁平契约；去向、撤回和质检状态机没有复制到新组件。
- 高稳定：分页对象与页面行对象完全一致；返工关系基于服务端全部批次上下文计算，按钮资格由读模型提示并由原写命令最终裁决，现有 QC 数据和数据库约束未改变。

#### 第十七阶段：QC 表格信息层级收敛

状态：已完成

实施范围：

- QC 表格只保留物料、工单号、工艺、加工工人、状态和操作等定位信息。
- 批次序号、返工来源、送检数量、质检数量、QC 工人、质检时间、不良原因及去向明细统一放入“查看”弹窗。
- 每一行继续绑定唯一质检批次，录入、去向决定和撤回操作仍使用当前行的批次 ID，不改变分页口径或 QC 业务规则。

完成条件：

- 表格不再直接铺开质检批次和质检结果明细。
- 查看弹窗能够完整识别当前批次并展示所有质检信息。
- 多个批次即使属于同一工单，也保持独立行和独立操作身份。

实际修改与证据：

- 表格已删除批次序号、返工来源、送检、合格、返工、报废、遗失、QC 工人和质检时间列，只保留物料、工单号、工艺、加工工人、状态和操作。
- “查看”弹窗继续展示当前行对应的批次序号、返工来源、送检数、全部质检结果、加工/QC 工人、质检时间、不良原因、合格品去向、决定时间和决定人。
- 行键、分页数据和所有操作仍使用唯一 `work_order_batch.id`，没有改变批次级查询、操作资格或写命令。
- 前端 Vue/TypeScript `--noEmit` 检查与 `git diff --check` 通过。

审查结论：

- 代码规范：列表承担快速定位，详情弹窗承担完整信息展示，职责和信息层级清晰。
- 低耦合：仅调整现有表格投影，没有修改接口、状态机或跨模块依赖。
- 高稳定：同工单多批次仍是不同数据行和不同操作目标，隐藏明细不会改变任何业务身份。

#### 第十八阶段：QC 工单记录右侧抽屉

状态：已完成

实施范围：

- QC 表格“查看”改为从右侧打开抽屉，不再使用单个批次描述弹窗。
- 抽屉按生产部门工单卡片的信息层级展示当前行所属完整工单，以及该工单全部首次送检和返工复检记录。
- 从生产部门现有抽屉提取纯展示工单记录卡片，由生产部门和 QC 共同复用；生产执行按钮继续由生产部门通过插槽提供，QC 抽屉保持只读。
- 新增 QC 工单详情只读接口，按工单 ID 返回现有统一工单契约和车间名称；接口只允许查询普通/装配 QC 工单，不复制工单进度或质检算法。
- QC 表格中的录入、送回、放行、存入仓库和撤回质检继续保留在操作列，不迁入查看抽屉。

完成条件：

- 点击任意 QC 行的“查看”后，从右侧完整显示该行所属工单和该工单全部 QC 批次。
- 工单数量、加工数量、送检数量、完成数量、异常数量及批次结果均来自统一工单序列化结果。
- 生产部门现有工单操作、相关状态筛选和异常只读模式保持不变；QC 抽屉不出现生产执行按钮。
- 共享组件不依赖具体部门写操作，接口和前端不存在重复工单状态算法。

实际修改与证据：

- QC 表格“查看”已改为打开右侧 `ElDrawer`，抽屉按工单加载并展示该工单全部 QC 批次；原单批次描述弹窗和组件已删除。
- 新增 QC 工单详情只读接口，只接受普通/装配工单 ID，并通过 QC 部门能力暴露；响应直接使用现有 `ProductionProgressWorkOrderResponse`，不建立第二套工单详情契约。
- 生产工单展示行的数量换算与状态能力计算已从 planning 内部提取到 production core 纯展示函数；生产进度查询和 QC 详情共同调用同一实现，装配输出数量口径保持一致。
- 工单卡片和 QC 批次记录已提取为 `WorkOrderRecordCard`。生产部门通过插槽保留相关状态标签、返工送检、送检、确认合格、打印、撤销和取消入口；QC 抽屉只传入只读工单记录，不出现生产写操作。
- QC 的录入、查看、送回、放行、存入仓库和撤回质检仍由表格操作列负责；打开查看不会改变当前批次或工单状态。
- 全部后端 Python 文件通过 AST 解析；前端 Vue/TypeScript `--noEmit` 检查通过；旧详情弹窗与临时详情契约引用搜索无残留；`git diff --check` 通过。

审查结论：

- 代码规范：工单记录展示、生产专属操作、QC 列表操作和详情加载职责分开；已删除被替代的弹窗实现。
- 低耦合：生产与 QC 复用纯展示卡片和统一工单记录计算，QC 只通过 quality 公共 API 获取详情，没有导入 planning 内部实现或复制状态算法。
- 高稳定：生产部门原有操作通过原函数和策略继续执行，装配数量继续使用统一输出口径；QC 抽屉只读且按稳定工单 ID 加载全部批次，单行批次身份和表格写操作不受影响。

#### 第十九阶段：委外加工质检表格与工单抽屉

状态：已完成

实施范围：

- 委外加工质检由工单卡片列表改为单层表格，一行对应一张唯一委外工单；表格展示物料、工单号、供应商、工艺、状态和操作。
- 表格操作列提供“录入”和“查看”；委外分次质检仍从工单维度创建新批次。
- “查看”从右侧打开委外工单抽屉，复用生产/QC 的工单记录卡片结构，展示该工单累计质检情况和全部分次 QC 批次。
- 单批放行和撤回质检保留在对应批次记录上，防止同一工单存在多个待处理批次时操作目标不明确。
- 抽取工单记录组件所需的稳定纯展示契约，并允许委外工单替换指标区；不改变普通/装配工单展示和操作。

完成条件：

- 委外质检主页面不再直接铺开工单和批次卡片，定位信息与详细记录分层清晰。
- 每张开放委外工单可继续分次录入；查看抽屉展示全部已有批次，每个批次的放行和撤回仍准确使用其批次 ID。
- 委外累计质检、累计合格、返工、报废、遗失、剩余待合格、待放行和已放行口径保持不变。
- 普通/装配生产工单和 QC 抽屉继续使用原数据与操作策略，不受委外展示适配影响。

实际修改与证据：

- 委外加工质检主页面已由工单卡片改为 `ElTable`，一行对应一张开放委外工单；表格只展示“编号 物料名”、工单号、供应商、工艺、状态和操作，并使用统一列宽记忆能力。
- 操作列保留委外工单级“录入”和“查看”。录入继续创建一次独立分批 QC 记录，没有改变累计合格上限或工单结单条件。
- 新增委外工单右侧抽屉，通过共享 `WorkOrderRecordCard` 展示工单上下文、委外专属累计指标和全部分次质检批次。
- 放行和撤回质检位于抽屉内各自批次记录下；操作完成后重新读取同一委外任务并刷新抽屉，最终合格且放行导致工单结单时关闭已不再属于待处理列表的抽屉。
- 工单记录组件改为依赖最小纯展示契约，并提供指标插槽。普通/装配仍传入原 `ProductionProgressWorkOrder` 和默认指标；委外只替换指标区，没有复制批次展示。
- 前端 Vue/TypeScript `--noEmit` 检查通过；旧委外工单/批次卡片模板和样式搜索无残留；全部后端 Python 文件通过 AST 解析；`git diff --check` 通过。

审查结论：

- 代码规范：委外主列表、录入弹窗、详情抽屉、累计指标和批次操作职责清楚，被替代的大段卡片模板与样式已删除。
- 低耦合：三类工单共用最小展示契约和同一批次卡片，委外通过指标插槽保留其独立累计口径，没有影响普通/装配状态算法。
- 高稳定：委外仍按工单分次录入、按具体批次放行或撤回；刷新后按稳定工单 ID 同步抽屉，工单结单从开放列表移除时不会保留失效操作入口。

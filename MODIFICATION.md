# 核心大模块职责拆分整改方案

## 整改背景

当前模块依赖、表所有权、SQL/ORM 一致性和前端边界检查能够通过，但以下核心实现文件仍然过大：

- `planning/production_workbench.py`：同时承担查询编排、上下文加载、候选位置计算和响应组装。
- `inventory/warehouse_api.py`：同时承担仓库查询、幂等写入、冲销、操作状态机和请求校验。
- `production_core/persistence.py`：集中声明生产物料、工单、QC 批次、流转和撤回等全部 ORM。
- `standard_execution/price_configs.py`：同时承担工艺配置查询、正式/临时价格组装和配置命令。

这些文件虽然仍位于正确业务模块内，但内部职责边界不够清晰，修改局部规则时需要理解大量无关代码，增加回归、循环依赖和状态口径混合风险。

## 整改目标

1. 在各自业务模块内部按稳定职责拆分，不新增顶层 `services/`、`repositories/` 或 `models/` 层。
2. 保留现有公开 API、函数签名、响应结构、ORM 表名、字段、约束、索引和业务行为。
3. 查询、候选计算、响应组装、命令、幂等状态机和 ORM 领域分组分别归属明确文件。
4. 原公开导入路径继续作为兼容当前代码的模块门面，但门面只负责显式导出，不保留业务实现。
5. 同步扩展只读架构检查，使拆分后的模块内 ORM 文件仍参与 SQL/ORM 和表所有权核对。

## 非目标

- 不修改数据库表、字段、约束、索引或 `zzerp.sql`。
- 不修改生产、装配、QC、仓库、工艺配置的任何业务规则和数量口径。
- 不修改 HTTP 路由、请求响应契约或前端页面。
- 不引入旧数据兼容、运行时动态导入、全局 Service/Repository/Model 层。
- 不顺便处理认证、部署或数据库迁移策略。

## 实施阶段

### 第一阶段：生产核心 ORM 分组

状态：已完成

- 将物料与仓位、工单与 QC、流转与撤回模型分别放入模块内独立持久化文件。
- 将 `production_core/persistence.py` 收敛为稳定 ORM 导出门面。
- 调整只读 SQL/ORM 检查，使模块内 `persistence*.py` 均纳入扫描。

完成条件：所有 ORM 类只定义一次；原导入路径继续有效；表、字段、约束和索引定义不变。

实际修改与证据：生产物料/仓位、工单/QC、流转/撤回模型已分别迁移到三个模块内持久化文件；原 `persistence.py` 仅显式导出全部模型。只读架构检查已改为扫描模块内全部 `persistence*.py`，不因拆分丢失 SQL/ORM 核对范围。

### 第二阶段：生产工作台读模型拆分

状态：已完成

- 分离工作台内部数据类型、数据加载、候选位置计算和响应组装。
- `production_workbench.py` 只保留查询事务和分页编排。

完成条件：公开函数签名和返回顺序不变；拆分文件之间依赖单向且不存在循环。

实际修改与证据：工作台数据类型、批量上下文查询、候选位置计算和响应序列化已分别进入独立文件；原 `production_workbench.py` 从 1058 行收敛为 115 行事务编排，公开函数签名及 `planning.api` 导入路径保持不变。拆分后文件语法解析通过，依赖方向为类型 → 查询/计算 → 序列化 → 编排。

### 第三阶段：临时仓库服务拆分

状态：已完成

- 分离仓库请求契约、读取查询、入出库命令、冲销命令及共享幂等支持。
- `warehouse_api.py` 收敛为公开门面。

完成条件：幂等键、操作状态、外部写入不确定态、冲销限制和返回快照逻辑保持原样。

实际修改与证据：仓库请求契约、库存/操作查询、入出库命令、冲销命令和共享幂等支持已拆分；原 `warehouse_api.py` 从 1048 行收敛为 38 行显式导出门面。原函数体按职责机械迁移，事务入口、网关调用、状态转换、异常类型和快照生成均未改写或复制。

### 第四阶段：工艺与单价配置拆分

状态：已完成

- 分离配置范围解析、查询响应和配置修改命令。
- `price_configs.py` 收敛为模块内部稳定门面。

完成条件：正式/临时工艺、确认/取消确认、单价修订记录和权限范围保持原样。

实际修改与证据：配置范围解析与修订记录支持、正式/临时价格查询、配置修改命令已分别迁移；原 `price_configs.py` 从 811 行收敛为 21 行显式导出门面。原公开函数签名、事务边界、配置锁定、工单引用保护和修订记录调用保持不变。

### 第五阶段：引用与重复实现清理

状态：已完成

- 核对公共 API、descriptor 和现有调用方。
- 删除拆分后重复的导入、定义和无价值转发。
- 保证外部模块仍只通过已声明 API 或既有窄接口协作。

完成条件：不存在重复类或函数实现，不新增跨模块内部依赖和循环依赖。

实际修改与证据：所有模块外调用仍指向 `planning.production_workbench`、`inventory.warehouse_api`、`production_core.persistence` 或 `standard_execution.price_configs` 原门面；新增职责文件仅被所属模块内部引用。四个原文件的 85 个类/函数 AST 与拆分后定义逐项一致且无重复，新增模块依赖和循环依赖检查通过；拆分产生的无用导入已清理。

### 第六阶段：完整只读审查

状态：已完成

- 检查 Python 语法、模块依赖、循环依赖、表所有权、SQL/ORM 名称和前端边界。
- 检查差异格式、公开符号和四个原大文件的最终职责。
- 记录实际结果及未完成事项。

完成条件：代码规范、低耦合和高稳定三类审查均有实际代码或只读检查证据，范围内不存在未解决问题。

实际修改与证据：后端全量 Python 语法解析通过；模块依赖、循环依赖、Router 边界、表所有权、SQL/ORM 字段/约束/索引及前端 feature 边界静态检查通过；拆分模块全局符号检查通过；`git diff --check` 通过。四个原文件的 85 个类/函数与拆分后 AST 逐项一致，未发现缺失、重复或实现变化。目标实现文件均已控制在 481 行以内，四个原入口分别收敛为 115、38、30、22 行。

## 执行顺序

第一阶段 → 第二阶段 → 第三阶段 → 第四阶段 → 第五阶段 → 第六阶段。

## 影响范围

- `backend/src/modules/production_core/`
- `backend/src/modules/planning/`
- `backend/src/modules/inventory/`
- `backend/src/modules/standard_execution/`
- `scripts/check_backend.py`
- `MODIFICATION.md`

## 风险与控制

1. 机械移动过程中可能遗漏隐式依赖：通过显式导入、Python 语法和符号检查控制。
2. ORM 拆分可能影响 SQLAlchemy 元数据注册：门面必须同步导入全部模型，既有应用导入路径不变。
3. 私有辅助函数拆分可能形成循环：数据类型置于无业务依赖文件，查询、计算、展示按单向依赖组织。
4. 仓库幂等逻辑不可被复制：共享状态机辅助函数只保留一份，命令模块统一调用。
5. 不以文件变小为理由改变事务边界、查询次数或业务条件。

## 代码审查方式

- 搜索四个原文件的类、函数和调用方，确认定义唯一。
- 执行项目只读架构检查的静态部分及 Python 全量语法解析。
- 核对 SQL/ORM 表、字段、约束和索引名称一致性。
- 检查模块依赖图、循环依赖、跨模块 persistence 访问和前端 feature 边界。
- 执行 `git diff --check` 并审查最终文件规模与导出门面。

## 最终结果

状态：已完成

六个阶段已按顺序完成，范围内不存在未解决问题。

- 代码规范：查询、计算、序列化、命令、幂等支持和 ORM 分组均有明确文件职责；原入口仅保留稳定导出或事务编排；新增文件具有职责说明且未保留无用导入。
- 低耦合：拆分全部发生在原业务模块内部，外部调用继续使用原公共路径；新增职责文件没有被其他业务模块直接依赖，模块依赖图和循环检查通过。
- 高稳定：事务边界、公开签名、类和函数实现、ORM 定义及数据库结构均未改变；85 个定义通过 AST 等价核对，SQL/ORM 与全量语法审查通过，没有兼容分支、动态导入或临时转发逻辑。

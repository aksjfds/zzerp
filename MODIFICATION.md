# zzerp v6 规范、解耦与稳定性整改方案

## 一、文档用途

本文档记录 2026-08-22 基于当前真实工作区完成的项目复审结果，以及下一轮大型整改方案。

本次只完成以下事项：

- 读取当前 `AGENTS.md`、项目说明、数据库基线和真实源码；
- 复审后端依赖、模块所有权、查询实现、前端边界、接口契约和数据库职责；
- 记录已经确认的问题、整改顺序、风险和完成门槛；
- 覆盖旧的整改记录，取消与当前代码不一致的“已完成”结论。

本次没有执行整改，没有运行构建、项目脚本、数据库命令或外部服务操作。

## 二、复审范围与方法

### 2.1 已检查范围

- 根目录规范与说明：`AGENTS.md`、`README.md`、`PROJECT_OVERVIEW.md`；
- 后端：`backend/src/domain`、`modules`、`departments`、`routers`、`schemas`；
- 前端：`frontend/src/api`、`features`、`shared`、`stores`、`permission`、`router`；
- 数据库：`zzerp.sql` 的表、约束、索引、函数、触发器和初始化数据；
- 架构声明：模块 descriptor、`modules/ownership.py`、模块说明文档；
- 当前 Git 差异、文件结构、跨模块导入、局部导入、超长职责文件和无边界查询。

### 2.2 使用的检查方式

- 文件和符号搜索；
- Python AST 只读依赖图分析；
- SQL 与 ORM 表所有权人工对照；
- 前后端响应字段人工对照；
- 查询分页、批量加载和生命周期代码路径审查；
- `git diff --check` 只读检查。

未运行构建、类型检查、项目检查脚本和数据库验证，因此本文档只记录代码规范、低耦合和高稳定审查结论，不把未执行的运行结果写成已验证。

## 三、当前符合规范的部分

1. Router 基本只负责 HTTP 参数衔接、权限依赖和响应模型，没有直接操作数据库。
2. 顶层旧 `services/`、`repositories/`、`models/` 源码层已经删除，ORM 表定义位于各自模块的 `persistence.py`。
3. `modules/ownership.py` 当前声明的 28 张表与物理 ORM 定义的模块归属一致，未发现同一表由两个模块重复定义。
4. 未发现业务模块直接导入其他模块的 `persistence.py`。
5. 前端 `shared` 未反向依赖业务 feature；普通跨 feature 引用大多经过目标 feature 的 `index.ts`，路由和部门注册表作为组合根直接装配页面符合约束。
6. `zzerp.sql` 中现有业务触发器主要承担并发守恒、关联一致性和历史不可变底线，没有解析流程图或替代完整后端业务状态机。
7. 生产流水、库存流水和已完成 QC 历史的数据库不可变保护有明确索引支撑，不建议迁回后端形成第二套弱约束。
8. 工程部编辑流程图与其他部门只读流程图已经物理分开，没有发现共享改动直接覆盖工程部编辑交互。

上述结论不代表对应模块已经完全规范，只表示这些具体边界当前无需重构。

## 四、确认存在的问题

### P0：模块依赖图仍存在大型循环

AST 依赖图确认以下 10 个业务模块处于同一个强连通分量：

`assembly`、`engineering`、`inventory`、`planning`、`production_core`、`purchasing`、`quality`、`sales`、`standard_execution`、`workforce`。

确认存在的双向依赖包括：

- `assembly ↔ quality`；
- `purchasing ↔ quality`；
- `quality ↔ standard_execution`；
- `quality ↔ workforce`；
- `engineering ↔ inventory`；
- `engineering ↔ production_core`；
- `engineering ↔ sales`；
- `engineering ↔ standard_execution`；
- `inventory ↔ planning`；
- `inventory ↔ production_core`；
- `planning ↔ production_core`；
- `planning ↔ sales`；
- `production_core ↔ sales`；
- `production_core ↔ workforce`。

主要根因不是单一导入写法，而是命令编排、跨域查询和展示模型没有分开：

- `quality.inspections` 曾反向选择装配、采购和普通生产的 QC 路由；该直接反向依赖已在第二阶段解除，其他命令编排循环仍按后续阶段处理；
- `sales.orders` 同时协调工程、计划和生产初始化；
- `planning.plan_api` 同时协调库存占用和生产初始化；
- `production_core` 同时承担核心状态、跨域展示、计划读取、库存协调和人员读取；
- `engineering` 为判断可编辑性反向查询库存、生产、销售和工艺配置；
- `inventory` 在自己的库存命令中直接协调生产移动。

该问题直接违反 `AGENTS.md` 的“禁止循环依赖”和明确依赖方向要求。仅把导入移入函数不能解决循环。

### P0（第一阶段已解决）：系统账号契约违反明确业务规则

数据库已经正确使用 `users.is_system = TRUE` 且系统账号 `department IS NULL`，但应用层又把系统账号伪装成 `sys` 部门：

- `modules/identity/authentication.py` 曾将系统账号序列化为伪部门；
- `schemas/common.py` 和 `frontend/src/types/auth.ts` 曾把伪系统身份当成部门代码；
- `authorization.py`、生产、装配、QC、工艺单价和部门编排代码曾使用伪部门判断系统权限；
- 用户响应没有暴露真实的 `is_system` 字段。

这与 `AGENTS.md` 第 12 条核心业务规则直接冲突，也让身份概念在数据库、后端和前端形成三套口径。

### P0：descriptor 不能真实表达依赖，局部导入正在隐藏循环

- `quality/descriptor.py` 与 QC 执行路由的真实依赖曾不一致；第二阶段已将策略选择移到应用编排层并同步 descriptor，其余局部跨模块导入仍按后续阶段处理；
- `inventory/reservation_api.py`、`inventory/api.py`、`planning/plan_api.py`、`production_core/inventory_api.py` 存在六处函数内部跨模块导入；
- descriptor 当前只能描述依赖，不能阻止未声明依赖、循环或违规内部入口。

因此“descriptor 已更新”不能作为低耦合完成依据。

### P1：跨模块 ORM `model_api` 已从例外变成常规依赖

当前业务模块中存在约 71 处跨模块 `model_api` 导入。并非所有跨表只读 join 都错误，但现状已经超出“确有必要的例外”：

- 命令路径和读模型路径没有区分；
- 模块经常直接依赖多个外部 ORM 类型；
- owner 的数据读取语义、过滤条件和稳定 DTO 没有封装；
- 大量外部 ORM 引用持续支撑模块循环。

整改时必须逐项分类：命令路径不得读取或修改外部 ORM；跨域只读 join 只能集中在明确的读模型模块，并在 descriptor 中登记理由。

### P1：进度与列表查询仍按全部历史规模消耗资源

旧方案把若干接口标记为“性能整改完成”，但真实代码仍有以下问题：

1. `production_core/work_order_queries.py:list_qc_batches` 在有关键字时加载全部待检或历史批次、全部关联上下文，在 Python 过滤后分页。
2. `planning/department_progress.py` 流式扫描所有草稿和已确认计划项，对每项解析流程图、生成车间任务，扫描完后才能得到 `total`。
3. `planning/part_progress.py` 的计算型筛选先读取全部候选订单 ID，再按 50 个一批完整计算；它降低了峰值内存，但总查询与计算成本仍随全部历史订单增长。
4. `planning/part_progress.py` 同时实现 PMC、普通生产和装配进度，重复加载流程、移动、工单和 QC 上下文。
5. `standard_execution/price_configs.py:list_procedure_prices` 遍历全部当前产品与流程图生成候选项，再在 Python 中分页。

这些实现不一定立即报错，但数据增长后会稳定变慢，不能继续作为“已解决”保留。

### P1：库存与成品查询存在全历史回放和 N+1

1. `inventory/api.py:list_transactions` 只限制普通库存流水 200 条，但成品流水 `_finished_order_transactions` 会读取全部成品移动，Python 重放余额、合并排序后再截取。
2. 成品流水使用负数 movement ID、`inventory_stock_id = 0` 和“系统记录”伪造统一流水身份，响应模型不能表达真实来源。
3. `list_stocks` 为每个库存项单独加载产品流程以计算完成节点名称。
4. `list_outbound_plans` 在循环中逐项读取 BOM，并逐项加载流程。
5. `finished_goods_api.list_finished_order_stocks` 按订单产品分组后逐组读取订单、订单项和发货汇总。
6. 订单自动结单判断对每个订单项重复查询成品批次和已发货数量。

写命令中的逐行锁不能盲目批量化，但纯列表和汇总查询必须改为稳定的集合查询或显式批量上下文。

### P1：生产与计划文件仍混合多个职责

- `planning/part_progress.py`：1336 行，同时负责候选查询、PMC 聚合、生产部门任务、装配任务、发货汇总、流程解析和响应映射；
- `production_core/cards.py`：当前卡片、历史卡片、状态筛选、来源命名和查询混合；
- `production_core/work_order_presenters.py`：批量上下文、物料命名、装配命名、QC 批次和整张工单序列化混合；
- `assembly/work_orders.py`：装配开单、物料分配、提交、取消恢复和返工复检混合；
- `planning/plan_api.py`：计划命令、序列化、库存拆解、完成校验和流程占用递归混合；
- `production_core/order_status_view.py`：跨域加载、节点状态算法和响应映射仍集中；
- `planning/production_progress_detail.py`：上下文读取、节点卡片和状态计算混合。

纯 ORM 定义和完整的纯算法不因为行数大就拆分；上述文件的问题是职责和依赖同时混合，而不是单纯行数。

### P1：前后端接口契约不完整

`schemas/pmc.py:PmcPartProgressRow` 返回的字段中，前端 `features/admin/domain/pmcPartProgress.ts` 缺少：

- `part_no`、`order_date`、`shipped_quantity`、`outstanding_quantity`；
- `completion_date`、`remark`；
- `department_workshops`、`department_workshop_arrivals`、`department_workshop_completions`。

前端还把 `order_status` 写成普通 `string`，没有复用客户订单状态类型。当前页面只使用字段子集不代表契约正确；接口升级时这种不完整类型会静默漏字段或漂移。

### P1：前端身份恢复会把服务异常静默当成未登录

`stores/auth.ts:refreshUser` 捕获所有错误后统一清空用户并返回 `null`。因此网络中断、超时或服务端 500 与真实 401 的表现相同，用户会被静默送回登录页。请求层已经能区分 401、403、网络错误和超时，Store 不应抹掉这种区别。

### P1：全局表格列宽功能存在全局 DOM 副作用

`shared/table/columnWidthPersistence.ts`：

- 对整个 `document.body` 建立永久 `MutationObserver`；
- 在每次节点加入时扫描后代所有 `.el-table`，保存时再次查询全页面所有表格；
- 以“路由 + 列标题 + 同签名表格出现顺序”作为身份，动态显隐或表格顺序变化时可能恢复到错误表格；
- 最多连续 60 帧轮询未就绪表头。

该功能是用户明确需要的，但实现方式不符合低副作用和稳定身份要求。必须保留“浏览器记住手动列宽”的行为，改为表格显式接入和稳定业务键。

### P1：只读流程图的数据刷新会重复适应画布

`shared/process-flow/useProductionFlowCanvas.ts` 把流程拓扑、节点进度和边进度放在同一个深度 watch 中；每次 `render()` 最终都会执行 `fitFlowToView()`。这会使仅进度数值变化也重置用户当前视野。应区分“首次加载或流程拓扑改变”和“只更新进度样式”。

### P2：状态常量和展示口径仍分散

- 后端已集中工单状态，但订单、计划、库存、流水等状态仍大量以字符串散落在命令和查询中；
- `schemas/common.py` 又维护一套 Literal；
- 客户订单状态标签和 Tag 颜色在业务部与 PMC 页面重复；
- 生产计划页面存在上下文专属文案，但没有与通用订单状态展示明确分层。

数据库 CHECK 约束保留字符串是合理的；问题在于应用层多个文件各自维护同一集合和展示映射。

### P2：部分前端页面与 composable 职责过重

- `PmcPartProgressView.vue` 同时处理筛选、分页、路由聚焦、流程图懒加载、展开状态和整页展示；
- `useProductionFlowCanvas.ts` 同时负责实例生命周期、平移、Popover、缩放持久化、渲染、动画和自动适应；
- `PolishWorkOrderPrintDialog.vue` 同时承担打印数据映射、打印窗口基础设施和整张打印模板。

应按控制器、纯展示、交互和基础设施拆分，不按行数机械拆组件。

### P2：磨房打印存在硬编码和历史截断

- 打印单固定显示“制单人：董凤”，没有使用真实工单创建人快照；
- QC 记录固定映射“首批 + 返修 1 至 6”，超过 7 个检验批后会静默不显示；
- 打印 iframe 创建、样式复制和销毁逻辑由业务组件自己实现，后续其他打印页面容易重复。

这属于明确硬编码和静默丢数据风险，不能作为模板细节忽略。

### P2：静态检查脚本和文档说明不一致

`AGENTS.md`、`PROJECT_OVERVIEW.md` 将 `scripts/check_backend.py` 描述为“后端静态一致性检查”，实际脚本只导入 FastAPI 应用并列出路由。它不会检查未声明依赖、模块循环、跨模块 persistence、ownership、局部导入或 SQL/ORM 漂移。文档不能继续把“能导入应用”表述成完整架构审查。

### P2：业务说明与真实任务可见性存在文字冲突

真实查询将 `draft` 和 `confirmed` 生产计划都纳入部门生产任务，符合此前“生产计划创建后即可看到任务”的已确认需求；`PROJECT_OVERVIEW.md` 又写成“确认计划后发布生产任务”。本轮不擅自改变现有行为，应把文档修正为真实口径，并明确草稿任务与确认后库存占用的区别。

## 五、整改目标

1. 消除 10 模块强连通循环，不用局部导入、万能公共模块或第二套状态机掩盖依赖。
2. 系统账号统一使用 `is_system`，彻底删除伪 `sys` 部门口径。
3. 命令编排、模块拥有者命令、跨域读模型和展示映射职责分开。
4. 列表和进度接口的主要成本随当前页或明确增量增长，不再扫描或回放全部历史。
5. 前后端类型完整表达真实接口，状态与展示映射只有一个明确拥有者。
6. 保留现有客户订单、生产计划、工单、QC、装配、库存和发货业务行为。
7. 让静态检查名称、能力和文档一致，能够发现本轮问题再次出现。
8. 最终复审范围内不保留“以后再处理”的计划内遗留项。

## 六、非目标

- 不改变订单绑定产品版本、BOM 展开、任务数、工单整单处理、QC 返回/放行、多路装配消耗、库存身份、发货结单和计划手动完成规则；
- 不恢复必做工艺、固定工艺顺序、替代来源或旧数据兼容；
- 不引入微服务、消息队列、独立缓存服务或顶层 `services/`、`repositories/`、`models/`；
- 不为了减少行数拆散纯算法或同一表族的 ORM 定义；
- 不主动运行构建、项目脚本、数据库命令或外部服务操作；
- 不顺便修改无关页面视觉和业务文案。

若某阶段发现必须改变已确认业务规则，必须先更新本文档并向用户确认，不能通过重构名义自行决定。

## 七、必须保持的业务基线

1. 订单、计划、生产任务、工单、库存和流水始终使用订单绑定产品版本。
2. 业务部填写普通配件计划数量，任务数取生产计划数量，不取客户订单数量。
3. 客户订单确认后创建草稿计划；现有生产部门任务可见性保持“计划创建后可见”，计划确认才执行库存占用和正式初始化动作。
4. 一张工单只对应一个车间节点和一个工艺，同一工艺可拆多张工单。
5. 工单按整单全部数量直接填写结果或送检，不支持部分处理；直接填写结果后不能再次送检。
6. QC 合格后由 QC 决定返回当前节点或放行下一节点。
7. 装配、焊接等多路节点首张工单消耗输入，后续工艺使用节点在制品，不重复消耗。
8. 客户订单发货满足需求后自动结单，生产计划不自动停止或完成。
9. 库存按产品、版本、物料节点和完成节点区分，生产与库存历史保持不可变。
10. 正式流程节点 ID 是产品版本内稳定身份，已引用版本不得原地破坏。

## 八、目标依赖方向

1. `identity`、`organization`、稳定 `domain` 类型位于基础层。
2. `sales`、`engineering`、`planning`、`production_core`、`inventory`、`workforce` 各自只拥有自己的状态和命令。
3. 跨所有者写事务由现有 `departments` 应用编排层按具体用例协调，并把同一 Session 传给 owner 的事务 API。
4. QC 路由策略由应用编排层选择并注入，`quality` 不反向识别装配、采购或普通生产实现。
5. 跨域报表和进度放在明确的 `planning` 读模型中；核心写模块不得为页面展示反向依赖报表模块。
6. 外部 ORM join 只允许存在于登记过的跨域只读模型；命令路径使用标量、不可变 DTO、Protocol 或 owner API。
7. Router 继续只调用公开模块 API 或部门门面，不承担业务编排。

## 九、实施阶段

### 第一阶段：统一系统身份与授权契约

状态：已完成

实施内容：

- 用户响应增加真实 `is_system`；系统账号的 `department` 保持 `null`；
- 从后端、前端类型中删除 `sys` 部门代码；
- 在 identity/authorization 边界提供统一的系统账号和部门访问判断；
- 替换生产、装配、QC、工艺配置和部门编排中的伪部门判断；
- 保持数据库现有约束，不新增兼容字段。

完成门槛：项目内不再将系统账号序列化为 `sys`；`sys` 只允许存在于权限名称；普通账号仍引用真实部门；前后端用户契约一致。

实际修改：

- 用户序列化与前后端响应类型增加真实 `is_system`，并允许系统账号的 `department` 为 `null`；
- 删除后端与前端部门类型中的伪 `sys` 代码，系统账号默认路由改为依据 `is_system`；
- 在稳定 `domain.identity` 边界集中提供部门访问判断，授权层、工单编排、标准生产、装配、QC、撤回、取消和工艺单价入口统一传递并使用真实系统身份；
- 保持 `users` 表、ORM 和 `zzerp.sql` 现有系统账号约束及初始化数据不变，没有增加兼容字段或迁移分支。

重要发现：

- 数据库和 ORM 原本已经一致表达“系统账号无部门、普通账号必须绑定部门”，本阶段问题仅存在于应用层序列化和授权口径；
- `sys:user:add` 是权限名称，不是部门代码，按完成门槛保留。

未完成事项：无。

审查结论：

- 代码规范：系统身份与部门身份字段含义明确，未保留用户名、角色或伪部门硬编码判断；
- 低耦合：跨模块只依赖无 ORM、无副作用的稳定身份规则，部门访问算法只有一个实现；
- 高稳定：普通账号部门约束和现有权限字符串未改变，前后端可空性一致；Python 源码 AST 解析及 `git diff --check` 通过，未运行构建、项目脚本或数据库命令。

### 第二阶段：解除 QC 与执行模块的反向依赖

状态：已完成

实施内容：

- 定义最小、无 ORM 的 QC 路由策略协议和结果 DTO；
- 将标准生产、采购、装配路由策略的选择移动到明确的部门应用编排入口；
- `quality` 只校验和记录 QC 结果，并调用注入策略，不再导入三个执行模块；
- 执行模块可单向调用 quality 的批次 owner API；
- 拆分 `inspect_batch` 的加载、规则校验、结果持久化和路由执行职责，保持一个事务和原调用顺序。

完成门槛：消除三组 QC 双向依赖；descriptor 与真实依赖一致；QC 守恒、返回、放行、返工和结单规则不变。

实际修改：

- 增加无 ORM 的 `QcRoutingStrategy` 协议，以及合格品和返工路由的不可变结果 DTO；
- 新增 QC 部门应用编排入口，由该入口持有唯一事务并按工单类型选择标准生产、采购或装配路由策略；
- `quality` 命令改为“准备检验上下文”和“执行检验结果”两个事务内端口，并进一步拆分工人校验、锁定加载、数量规则校验、生产上下文加载、路由执行和结果持久化职责；
- `quality` 不再导入或识别 `assembly`、`purchasing`、`standard_execution`，三个执行模块通过各自 `qc_api.py` 单向实现注入策略；
- 三种路由统一返回明确 DTO，不再以无名称元组或裸整数表达目标节点和目标部门；
- 删除重复的 QC 批次上下文兼容出口，统一复用 `production_core` 的无 ORM 结构协议，并同步 quality descriptor 的公开协作端口。

重要发现：

- 原逻辑的事务边界位于 `quality.inspect_batch`；本阶段将事务边界上移到具体 QC 应用用例，但仍保持一次事务、同一 Session 和最外层统一提交/回滚；
- 原锁顺序为先锁工单、再锁送检批次，本阶段保持该顺序，未因策略上移改变并发行为；
- QC 工人、批次、工单状态、数量守恒和生产上下文校验仍先于路由执行；合格、返工、报废、遗失、检验记录和工单结单刷新顺序未变。

未完成事项：无。

审查结论：

- 代码规范：策略选择、检验规则、路由执行和结果持久化职责已明确拆分，目标结果使用具名不可变 DTO；
- 低耦合：AST 依赖审查确认 `quality` 不再直接依赖三个执行模块，三个执行模块只单向依赖 quality 公共协作端口，descriptor 与真实直接依赖一致；
- 高稳定：QC 权限、错误口径、锁顺序、事务范围、守恒、返回、放行、返工及结单流程保持不变；后端 Python 源码 AST 解析和 `git diff --check` 通过，未运行构建、项目脚本或数据库命令。

### 第三阶段：收敛订单、计划、生产与库存命令编排

状态：已完成

实施内容：

- 按用例建立业务部、工程部、生产和库存的窄应用编排入口，不建立万能 workflow 模块；
- 订单确认/取消、计划确认/取消/完成、库存占用/出库、生产初始化和成品结单分别明确事务拥有者；
- `sales` 不再直接创建 planning/core 记录；`planning` 不再直接修改 inventory/core 记录；
- `production_core` 不再为命令反向读取 planning、workforce 或 inventory 内部模型；
- 工程版本可编辑性由编排层聚合各 owner 的“是否被引用”结果；
- 在 `work_order` 由 production_core 持久化真实创建人快照，所有开单入口统一写入，并同步 owner ORM 与 `zzerp.sql`，不保留无创建人的兼容分支；
- 为消除 production_core 对 workforce 的反向展示依赖，工单同时保存开单时已选择工人的姓名快照；工人身份和车间归属仍由 workforce 在应用编排入口校验，既有工单展示口径不变；
- 移除函数内部跨模块导入；命令 API 只传稳定标量、不可变 DTO 或 Protocol，同一跨域操作保持单事务。
- 复审确认仅移动写命令不足以满足本阶段“依赖图无强连通分量”的门槛；因此先把造成反向边的现有跨域展示组合移出底层 owner，归入 planning 读入口或具体应用编排层，但本阶段不建立第四阶段的路线任务投影，也不实施查询性能改造。

完成门槛：业务模块依赖图不存在强连通分量；不使用局部导入掩盖循环；每张表仍只有一个写入 owner；事务、锁顺序、修订号和错误编号保持明确。

实际修改：

- 新增业务部、工程部、QC、工单、库存出库和仓库结余的窄应用编排入口；Router 只调用对应应用入口或 owner 的只读 API，没有建立万能 workflow 模块；
- `sales` 通过 `SalesPlanningPort`、`SalesEngineeringPort` 和 `SalesProductionPort` 接收稳定协作者，不再导入 planning、engineering 或 production_core 的具体实现；订单确认、取消和计划确认仍由业务部用例持有一个 `SessionLocal.begin()` 事务，订单先按修订号加锁，计划在同一 Session 内加锁并调用各 owner 命令；
- `planning` 的计划确认/取消改为通过 `PlanExecutionCollaborators` 调用库存占用/释放和生产初始化/取消；planning 不再直接调用 inventory 或 production_core 的写命令，计划状态、修订号和数量校验仍由 planning owner 修改；
- 库存出库由 `departments/inventory_orchestration.py` 持有单事务，按“锁库存占用与库存记录 → inventory 扣减 → planning 更新已出库数量 → production_core 注入生产流转 → inventory 写流水”的顺序执行；各 owner 只修改自己的记录；
- 成品发货达到订单数量后的结单改为调用 sales 的事务内窄命令，inventory 不再直接修改订单状态；结余转库存仍由 inventory owner 完成，整个发货、结余和结单保持一个事务；
- 工程版本可编辑性和工艺单价引用检查改为 `EngineeringCollaborators` 聚合，由 `departments/engineering_orchestration.py` 注入 sales、production_core、inventory 和 standard_execution 的引用查询与复制命令；engineering 不再反向依赖这些实现模块；
- 生产卡片跨域组合从 production_core 移至 planning 读入口；仓库结余跨域编排从 production_core 移至部门应用层；production_core 的命令路径不再反向读取 planning、inventory 或 workforce；
- 工单新增非空 `created_by` 创建人快照和可空 `worker_name` 工人姓名快照；标准生产、采购、外购、装配和焊接开单入口均传入真实当前账号及已校验工人姓名，ORM、响应契约、前端类型和 `zzerp.sql` 同步，不保留旧记录兼容分支；
- QC 工人引用改为稳定的不可变 `WorkerReference`，库存出库上下文改为不可变 DTO；移除全部函数内部跨业务模块导入；
- descriptor 已与 12 个业务模块的真实直接依赖同步。AST 依赖图复核结果为强连通分量 `0`，未声明依赖和陈旧声明均为 `0`。

事务与稳定性核对：

- 订单命令继续先锁订单并校验 `revision`；计划确认继续锁计划并校验计划 `revision`，库存占用及生产初始化在相同事务中完成，任一步异常统一回滚；
- 库存出库继续对占用记录和库存记录使用行锁，计划项更新与生产流转使用同一 Session，流水只在数量变更成功后写入；
- 工程修改继续先锁产品并校验产品 `revision`，跨 owner 引用检查只读同一事务快照；
- 成品发货继续先锁订单项、订单和成品批次，全部发货判定成立后先转结余再由 sales owner 结单；
- 原有业务错误编号、数量校验和状态约束未改为静默回退；新增协作者为必传依赖，没有默认兼容实现。

代码审查记录：

- 代码规范：命令 owner、跨域编排、只读引用和 DTO/Protocol 职责分开；未新增顶层 service/repository/model 或万能编排模块；
- 低耦合：真实模块导入图无强连通分量，descriptor 与真实依赖一致，函数内部跨模块导入为 0，sales、planning、production_core 和 engineering 的反向命令依赖已解除；
- 高稳定：工单创建人字段已同步 ORM、SQL 和接口类型；事务、锁、修订号及错误编号均有明确归属；后端 Python 源码 AST 解析和 `git diff --check` 通过；
- 按 `AGENTS.md` 权限限制，未运行构建、项目脚本、数据库命令、服务或外部请求。

未完成事项：无。第四阶段的跨域读模型持久化和查询性能整改未提前实施。

### 第四阶段：建立明确的跨域读模型边界

状态：已完成

执行前核对（2026-08-24）：

- 当前普通生产与装配任务均先遍历全部草稿/已确认计划项，再逐项解析正式流程 JSON 后进行部门、车间、关键字筛选和 Python 分页，确实无法满足本阶段数据库直接筛选、计数和分页的门槛；
- `production_plan_item` 已保存物料事实和计划数量，新增投影只保存它与正式流程节点、车间、部门之间的确定路线映射；任务数、到货数、完成数、工单和 QC 状态继续读取各 owner 的真实记录，禁止复制第二套数量状态；
- 投影在计划重建后以同一事务确定性重建；计划数量修改不改变路线，但仍显式同步投影以校验其与绑定产品版本正式流程一致；计划确认不重建路线；
- 普通生产与装配列表先以投影完成数据库过滤、计数和分页，再仅为当前页加载生产、移动、工单和 QC 上下文；第五阶段的其他历史规模性能整改不提前扩入本阶段。

实施内容：

- 将 PMC、部门任务、订单流程、工单卡片和工资投影分别归到明确读模型；
- `planning` 负责跨域生产进度，`production_core` 只保留核心数量算法和自身记录查询；
- 对外部 `model_api` 逐项分类：命令违规删除，只读 join 集中并登记；
- 将计划的物料—流程节点—车间—部门任务展开持久化为 planning 拥有的路线任务投影；
- 投影在草稿计划创建/修改时同步更新，计划确认只增加库存占用和正式执行状态；
- `zzerp.sql` 和 planning ORM 同步增加投影表、唯一约束、外键和索引，不保留兼容结构。

完成门槛：写模块不依赖跨域展示模型；外部 ORM 读取都有明确只读用途；部门任务可直接数据库筛选、计数和分页；投影可确定性重建且不是第二套业务事实。

实际修改：

- 新增 planning 所有的 `production_route_task` 路线任务投影，并同步 `planning.persistence`、`modules/ownership.py` 与 `zzerp.sql`；投影只保存计划项、正式流程节点、节点类型、车间和路线顺序，部门继续由车间归属确定；
- 投影通过 `(production_plan_item_id, route_flow_node_id)` 唯一约束保证同一计划物料的节点身份唯一，通过计划项组合外键和 `ON DELETE CASCADE` 保证生命周期一致，并增加计划分页和车间查询索引；
- `rebuild_plan_route_tasks` 只读取订单绑定产品版本的正式流程图，使用同一物理路线算法确定性生成投影；草稿计划创建、重算和数量修改均在原事务内同步重建，计划确认不再解析或重建路线；
- 普通生产任务改为先按路线投影、部门、计划状态、订单状态、物料类型和关键字在数据库中分组、计数和分页，再仅为当前页加载流程、生产移动、工单和 QC 上下文；同一计划物料在同一车间的多个流程节点仍合并为一行，保持原展示口径；
- 装配任务改为按装配部路线投影直接数据库过滤、计数和分页；装配节点及其后属于装配部的焊接等工艺节点继续逐节点显示，物料到货、完成数量和目录结构仍读取生产真实记录；
- 将订单生产汇总、发货数量汇总和订单流程状态从 `production_core` 移至 planning 的 `order_progress.py`、`order_status_view.py` 与 `sales_progress_api.py`；业务部应用编排改用 planning 读模型，production_core 不再拥有订单展示模型；
- PMC、部门任务、订单生产流程、生产卡片、任务详情、工单查询和工资投影的 owner 与用途已在 module descriptor 和 `modules/README.md` 中明确登记；生产卡片使用 production_core 暴露的纯计算 `card_read_api.py`，不再导入其内部实现；
- 所有跨 owner ORM 读取均改用 descriptor 声明的 `model_api.py`；移除 planning 对 production_core/inventory `persistence.py` 的直接引用，并将库存身份规则改为明确的 `identity_api.py`；
- 复审命令路径时发现并修复三类外部 ORM 写入：assembly 不再直接构造或修改 `WorkOrderMaterial`/`WorkOrder`，standard_execution 不再直接更新工单数量，quality 不再直接构造或更新 `WorkOrderBatch`；这些写入分别通过 production_core 的 `ownership_api.py`、`transaction_api.py` 和 `qc_api.py` 完成；
- 生产撤销恢复客户订单状态改为调用 sales owner 的事务内命令；成品结单仍通过 sales owner 命令，跨 owner 模块不再直接修改订单记录；
- QC 路由改为显式接收本次合格处置，不再为路由判断提前写入未完成检验批，从而避免查询自动 flush 时触发检验批完整性约束；最终 QC 结果仍一次性由 production_core owner 写入。

业务口径与投影边界：

- `production_plan_item.planned_production_quantity` 仍是唯一任务数量来源；路线投影不保存任务数；
- 到货、加工完成、工单、送检、异常和 QC 状态仍分别来自 production movement、repository、work order 和 work order batch，不写入路线投影；
- 投影绑定计划项已锁定的 `product_id + product_version + flow_node_id`，不会切换到产品最新版本；
- 草稿和确认计划继续可见，取消计划与取消订单继续从任务查询中排除；
- 普通生产按“计划物料 + 车间”合并，装配按具体装配/焊接流程节点展示，保持第四阶段前的响应字段和页面口径。

完成条件核对：

1. 实施范围：PMC、部门任务、订单流程、生产卡片、任务详情、工单和工资读模型均有明确 owner；路线任务投影、同步入口、SQL/ORM/所有权和查询入口均已落实。
2. 阶段门槛：部门任务的候选过滤、`total` 和分页均由数据库基于路线投影完成；流程 JSON 只为当前页结果加载；投影能够在草稿计划事务内确定性重建且不复制数量状态。
3. 整体目标：业务模块依赖图仍无强连通分量，descriptor 与真实直接依赖一致；第三阶段确立的事务和单表 writer 边界保持成立。
4. 代码规范与低耦合：没有新增万能读模型、顶层 service/repository/model、旧数据兼容或第二套状态算法；跨模块只使用声明的 API、Protocol 或 `model_api.py`。
5. 只读证据：后端 Python AST 解析通过；新增投影 ORM 与 SQL 的 7 个字段顺序和名称一致；30 张 ORM 物理表与 `TABLE_OWNERS` 完全一致；跨模块 persistence 导入为 0、外部 ORM 构造为 0、descriptor 偏差为 0、业务模块强连通分量为 0；`git diff --check` 通过。

审查结论：

- 代码规范：投影生成、数据库候选查询、当前页上下文加载和状态计算职责清晰；新增命名表达真实业务含义；
- 低耦合：planning 统一拥有跨域生产进度，production_core 只提供生产事实、工单 owner 查询和纯计算接口；所有跨 owner 写入均回到 owner API；
- 高稳定：产品版本绑定、任务数量、草稿可见性、普通生产合并、装配逐节点、QC 路由、锁与事务口径保持；数据库约束和索引与 ORM 同步，无兼容分支或静默回退；
- 按 `AGENTS.md` 权限限制，未运行构建、项目脚本、数据库命令、服务或外部请求。

未完成事项：无。第五阶段约定的 PMC 全历史筛选、QC 搜索、库存流水和其他查询性能整改未提前实施。

### 第五阶段：修复进度、QC、工艺配置和库存性能

状态：已完成

实施内容：

- QC 搜索先在数据库筛选、计数、分页，再加载当前页上下文；
- PMC 关键字、部门、异常和未完成筛选基于路线任务与聚合查询，不扫描全部订单；
- 普通生产、装配和工艺单价列表基于路线任务投影分页，不遍历全部流程 JSON；
- 库存完成节点名称按产品版本批量加载；BOM、订单和发货汇总使用集合查询；
- 成品流水改为数据库集合查询与窗口余额，或建立 inventory 拥有的真实成品流水投影；禁止全历史 Python 回放和负数伪 ID；
- 自动结单所需发货汇总一次性按订单所有产品聚合；写命令保持确定锁顺序。

完成门槛：分页接口不在 Python 中读取全部历史后切片；查询次数不按当前页每行线性增长；`total` 与过滤口径一致；不引入近似总数、跨请求缓存或前端二次口径。

执行前核对（2026-08-24）：

- QC 关键字查询仍会读取全部符合历史状态的检验批，并在 Python 中序列化、过滤和分页；
- PMC 组合筛选仍会取得全部候选订单 ID，按批加载完整上下文后在 Python 中累计 `total`；
- 工艺单价列表仍读取全部当前产品正式流程 JSON，再在 Python 中展开、过滤和切片；
- 库存列表逐条加载同一产品版本流程，成品库存列表与自动结单逐订单产品查询发货汇总；
- 成品流水仍读取全部相关生产移动、用 Python 回放余额，并以负数移动 ID 和 `inventory_stock_id = 0` 伪装库存流水身份。

本阶段先消除上述真实查询路径，不改变订单版本绑定、任务数、QC 决策、库存身份、发货结单或生产计划生命周期口径。

实际修改（2026-08-24）：

- QC 关键字条件下沉到工单、订单、产品、BOM 与正式物料路线的数据库条件；数据库先完成精确计数和分页，再为当前页批量加载工单展示上下文；
- PMC 关键字、部门、异常和未完成条件改为订单/产品、`production_route_task`、生产移动、仓位、工单与 QC 批次上的集合条件；先取得符合条件的订单页和精确 `total`，再只计算当前页进度；
- 普通生产与装配生产任务继续使用第四阶段建立的 `production_route_task`；工艺单价新增 engineering 拥有的 `product_route_task` 正式流程投影，正式流程保存时同事务重建，列表直接在数据库筛选、计数和分页，不再遍历全部当前产品流程 JSON；
- 库存完成节点按产品版本批量复用流程上下文；生产计划库存出库的 BOM 与订单上下文改为集合加载；成品待入库/发货列表一次性联查订单和订单项，并直接汇总受锁保护的订单成品数量；
- 新增 inventory 拥有且数据库级不可变的 `finished_goods_transaction`，在成品入库、库存转入订单、客户发货和订单结余转库存时记录真实来源、真实 ID、操作人及前后余额；流水接口只读取限定数量的普通库存流水和成品流水后合并，不再回放全部生产移动，也不再伪造负数 ID、零库存 ID或“系统记录”；
- 自动结单使用一次按订单全部产品分组的 `finished_order_stock.shipped_quantity` 汇总；成品确认、发货、结余与库存占用写命令统一按订单、订单项、成品批次或库存 ID 的确定顺序加锁，批量加载关联生产对象。

完成条件核对与只读证据：

1. 阶段实施范围：QC、PMC、普通/装配任务投影、工艺配置、库存上下文、成品流水、自动结单及写锁顺序均已逐项落实；
2. 分页与 `total`：QC、PMC 和工艺单价均由同一数据库候选查询计数和分页，当前页上下文加载不再决定 `total`；目标代码搜索未发现原全量 QC、50 单批扫 PMC、Python 工艺候选切片或成品全历史回放路径；
3. 查询增长：库存流程按唯一产品版本加载，BOM、订单、订单项、生产对象和发货/成品数量均使用集合查询，不按当前页每行执行查询；写路径保留必要行锁但采用确定顺序；
4. 数据结构：只读比对确认全部 ORM 表与 `zzerp.sql` 表集合一致；`product_route_task`、`finished_goods_transaction` 的 ORM 与 SQL 字段集合分别一致，owner 与 descriptor 已登记；
5. 静态审查：全部后端 Python 文件 AST 解析通过；模块依赖强连通分量为空；未发现跨模块直接导入其他 owner 的 `persistence.py`；`git diff --check` 通过；
6. 代码规范：查询候选、批量上下文和响应映射边界明确，没有新增前端二次口径、跨请求缓存、近似总数、兼容分支或无业务意义包装；
7. 低耦合：工程正式路线投影归 engineering，计划路线投影归 planning，订单成品流水归 inventory；其他模块只通过公开 model/read API 做跨域读组合，写入仍由 owner API 完成；
8. 高稳定：订单版本绑定、计划任务数、QC 决策、库存身份、发货自动结单和计划手动完成规则未改变；流水历史具备数据库不可变保护，SQL/ORM 约束和查询索引同步；
9. 按 `AGENTS.md` 权限限制，未运行构建、项目脚本、数据库命令、服务或外部请求。本地宿主未安装项目 SQLAlchemy 依赖，因此未把运行时模块导入作为完成证据；完成判断只采用上述允许的静态代码审查证据。

未完成事项：无。

### 第六阶段：拆分生产与计划的混合职责文件

状态：已完成

实施内容：

- `part_progress.py` 按 PMC、部门任务、装配任务、纯状态计算和映射拆分；
- `cards.py` 按候选查询、当前卡片、历史卡片和映射拆分；
- `work_order_presenters.py` 分离批量上下文、物料身份/名称和响应序列化；
- `assembly/work_orders.py` 分离创建、物料分配、提交、取消恢复和返工命令；
- `plan_api.py` 分离命令、库存展示、BOM 校验和计划映射；
- `order_status_view.py` 与 `production_progress_detail.py` 分离加载、纯计算和映射；
- 保留稳定公开入口，通过内部组合迁移，不复制算法。

完成门槛：每个文件职责单一；状态算法只有一个实现；查询与纯计算边界明确；不建立无业务含义的薄包装层。

执行前核对（2026-08-24）：

- `part_progress.py` 仍同时包含 PMC 订单页、普通部门入口、装配任务查询、跨部门单元格计算和发货汇总；
- `production_cards.py` 仍同时加载当前仓位卡片与历史工单卡片；
- `work_order_presenters.py` 仍同时构造批量上下文、解析物料显示身份并序列化批次/工单；
- `assembly/work_orders.py` 仍同时实现创建、物料分配、提交、取消恢复和返工复检；
- `plan_api.py` 仍同时实现计划命令、库存展示/拆解、BOM 产出验证和响应映射；
- `order_status_view.py`、`production_progress_detail.py` 的数据库加载、状态计算和响应映射仍未分层。

本阶段保持现有公开 API 名称和调用方不变；拆分后的原入口只负责导出或编排，不复制算法，不增加兼容分支。

实际修改（2026-08-24）：

- `part_progress.py` 缩减为稳定公开入口；PMC 候选分页与批量上下文归入 `pmc_progress.py`，单行响应映射归入 `pmc_progress_mapper.py`，装配部门任务归入 `assembly_department_progress.py`，共享数量计算与路线解析分别归入 `progress_calculations.py` 和 `progress_routes.py`；
- 原生产工作台卡片实现拆为 `production_card_listing.py`、`current_production_cards.py`、`historical_production_cards.py`、`production_card_filters.py` 和 `production_card_support.py`，分别承担入口编排、当前仓位、历史工单、候选过滤及共享物料/节点映射；删除已经失去职责的旧 `production_cards.py`，没有保留双实现；
- `work_order_presenters.py` 保留稳定序列化入口；批量读取上下文、物料展示身份和响应序列化分别迁入 `work_order_presenter_context.py`、`production_item_presenters.py`、`work_order_serializers.py`；单张工单与分页工单共用同一上下文构造和映射算法；
- `assembly/work_orders.py` 缩减为稳定公开入口；创建、物料分配、提交、取消恢复和返工分别迁入 `create_work_order.py`、`material_allocation.py`、`submit_work_order.py`、`cancel_work_order.py`、`rework.py`；物料稳定身份集中到 `domain/material_identity.py`，普通生产、装配和计划展示不再各自复制算法；
- `plan_api.py` 缩减为稳定公开入口；访问与修订校验、命令事务、库存展示与拆解、BOM 产出校验、库存占用和响应映射分别迁入对应 `plan_*` 模块；
- 客户订单生产状态拆为 `order_production_context.py`、`order_production_calculations.py`、`order_production_mapper.py` 和入口 `order_status_view.py`；部门生产明细拆为上下文入口、到货计算、卡片状态、路线/组织映射和响应映射模块；
- 清除进度明细入口中残留的第二份到货算法，统一调用 `calculate_progress_arrivals`；装配明细加载 BOM 时补上订单绑定的 `product_version` 条件，防止同产品不同版本物料混入；
- 移除生产撤回命令中用于隐藏依赖的局部导入；修复本轮静态审查发现的 `product_reference_api.py` 缺少 `dataclass` 导入、`production_core.persistence` 缺少 `String` 导入，以及成品库存接收路径未显式传入分配协作者的问题。

完成条件核对与只读证据：

1. 阶段实施范围：七组目标文件均已按已记录职责完成物理拆分，旧公开入口只保留稳定导出或必要编排；原大文件内算法没有复制保留；
2. 单一实现：关键公开算法及命令名称全项目定义位置唯一；函数体归一化审查未发现重复实现；路线标准目标、物料稳定身份和生产明细到货计算均只有一个实现；
3. 查询与计算边界：PMC、订单状态和生产明细均把批量加载、纯数量/状态计算和响应映射分开；计划命令、库存展示、BOM 校验和占用递归不再混在公开入口；
4. 公开入口：现有调用方继续通过 `part_progress.py`、`plan_api.py`、`assembly/work_orders.py`、`work_order_presenters.py` 和 planning 公开 API 使用原名称；静态符号审查未发现不存在的本地导入；
5. 代码规范：全部后端 281 个 Python 文件 AST 解析通过；目标文件未发现未使用业务导入、未解析全局名称、局部模块导入或无业务意义的兼容包装；`git diff --check` 通过；
6. 低耦合：模块级和文件级依赖强连通分量均为空；未发现跨 owner 直接导入 `persistence.py`；展示读取、领域计算和命令写入保持各自职责；
7. 高稳定：订单绑定产品版本、计划任务数、工单整单处理、QC 返回/放行、多路装配消耗、库存和发货规则未改变；本轮发现的缺失导入、协作者传递和跨版本 BOM 风险均已修复，没有静默回退或旧逻辑兼容分支；
8. 按 `AGENTS.md` 权限限制，未运行构建、项目脚本、数据库命令、服务或外部请求；完成判断仅使用允许的静态代码审查证据。

未完成事项：无。

### 第七阶段：统一接口类型、状态展示和前端生命周期

状态：已完成

执行前核对（2026-08-24）：

- PMC 前端行类型仍缺少后端响应中的订单日期、出货、欠货、完成日期、备注及车间分组字段，订单状态仍为普通字符串；
- `refreshUser` 仍捕获所有异常并清空用户，网络、超时和服务端错误会被静默当成未登录；
- 表格列宽仍通过全局 `document.body` 的 `MutationObserver` 自动扫描全部 Element Plus 表格，表格身份依赖页面顺序；
- 只读流程图仍需核对拓扑刷新与进度刷新是否共用自动适应行为；
- PMC 页面仍同时承担筛选、分页、展开、流程图加载和整页展示；
- 磨房打印仍需核对制单人来源、QC 批次完整性和打印 frame 生命周期。

实施内容：

- 补齐 PMC 前端类型全部字段，并复用 `CustomerOrderStatus`；
- 按业务 owner 集中订单、计划、工单、库存状态类型；
- 业务部导出通用订单状态展示，PMC 复用，计划上下文文案独立保留；
- `refreshUser` 只在真实未认证时清空用户，网络、超时和服务错误保留明确状态；
- 表格列宽改为显式 directive/composable，每张表提供稳定业务 key，删除全局 body observer；
- 拆分只读流程图生命周期、交互、Popover 和显示配置；仅首次、容器首次就绪或拓扑变化时适应，进度刷新只更新样式；
- 拆分 PMC 页面控制器与订单/流程展示；
- 提供共享打印 frame 基础设施；打印使用工单真实创建人快照；超过固定首屏的 QC 批次生成续页或完整附表。

完成门槛：前后端字段、可空性和状态一致；非 401 不再被当成退出；表格宽度不串表；进度刷新不重置视野；打印无人员硬编码和批次截断。

实际修改（2026-08-24）：

- PMC 前端契约补齐 `part_no`、订单日期、出货数量、欠货数量、完成日期、备注、部门车间及车间到货/完成数量字段；订单状态改为复用业务部公开的 `CustomerOrderStatus`，查询参数同步使用同一类型；
- 在客户订单 owner 中新增统一订单状态展示映射，客户订单列表和 PMC 复用同一标签与 Tag 颜色；计划页面保留计划上下文专属文案；生产计划、工单、生产卡片和库存相关状态改为各自 owner 中的具名类型，删除重复工单状态文案；
- `refreshUser` 只在 HTTP 401 时清空用户；网络、超时和服务端错误继续抛出，由路由守卫显示明确错误并中止当前导航，不再静默跳转登录页；
- 表格列宽能力改为全局注册、页面显式使用的 `v-table-column-widths` directive；项目当前 20 张 Element Plus 表格全部声明稳定业务键，按列名保存宽度；删除全局 `MutationObserver`、全页面表格扫描、路由加出现顺序身份和 60 帧轮询；
- 只读流程图拆出实例生命周期、画布拖动、节点 Popover 和显示设置模块；流程拓扑/车间映射变化才重新渲染并自动适应，节点与连线进度变化只更新展示属性和动画，容器尺寸变化只调整画布尺寸，不再重置用户视野；
- PMC 页面拆为 `usePmcPartProgress` 页面控制器、`PmcOrderProgressCard` 订单/矩阵/流程展示组件和轻量页面组合入口；跨 feature 数据继续只通过公开 `index.ts` 入口协作；
- 新增共享 `printElement` iframe 打印基础设施，统一创建、错误处理、打印后销毁和超时回收；磨房计件单改用当前工单的 `created_by` 快照，前七批保持原版面，超过七批的全部 QC 记录生成完整附表，不再静默截断；通用工单打印同步展示真实制单人；
- 补回 Vue 文件类型声明并保留既有 Vite 环境字段；修复静态类型审查发现的流程节点属性收窄、自动连接候选可空、工艺保存数组索引可空及部门页头必填属性问题，没有改变对应业务行为。

完成条件核对与只读证据：

1. 前后端契约：逐字段对照 `schemas/pmc.py` 后 PMC 行、订单分组、部门进度与查询状态类型完整一致；生产计划、客户订单、工单和库存状态均有明确 owner 类型；
2. 身份恢复：代码路径审查确认仅 Axios 401 清空用户；其他错误保留原用户状态、展示请求错误并取消导航；
3. 表格列宽：源码搜索确认 20 个 `<ElTable>` 均存在显式稳定键，数量一一对应；未发现 `MutationObserver`、全局 DOM 表格扫描或无参全局初始化旧入口；
4. 流程图生命周期：拓扑 watcher 与进度 watcher 已物理分开；`fitFlowToView` 只由首次/拓扑渲染和用户点击适应触发，进度更新和普通 ResizeObserver 回调均不调用适应；
5. PMC 与打印职责：页面控制器、展示组件和共享打印 frame 已分离；源码搜索未发现固定制单人、七批标签数组、旧打印 iframe 标记或 QC 批次截断；
6. 代码规范：103 个 TypeScript 文件语法解析无错误，58 个 Vue SFC 解析与 script setup 编译无错误；`vue-tsc --noEmit --incremental false` 通过；`git diff --check` 通过；
7. 低耦合：前端 feature 依赖强连通分量为空，`shared` 未依赖任何业务 feature；新增跨 feature 类型和组件调用均通过目标 feature 公共入口；
8. 高稳定：订单、生产计划、工单、QC、流程图和打印业务数据口径未改变；没有静默回退、全局 DOM 观察器、人员硬编码、历史截断或表格顺序身份；
9. 未运行构建、项目脚本、数据库命令、服务或外部请求；上述命令均为不写入项目的静态代码审查。

未完成事项：无。

### 第八阶段：数据库/ORM 一致性、检查工具和文档收口

状态：已完成

实施内容：

- 核对本轮路线任务投影、工单创建人审计字段及相关索引在 `zzerp.sql` 与 owner ORM 中完全一致；
- 保留现有守恒和历史不可变触发器，不把业务状态机移入数据库；
- 让 `scripts/check_backend.py` 至少检查应用导入、表 owner、跨模块 persistence、descriptor 未声明依赖、局部导入和模块循环；
- 增加不连接数据库的 SQL/ORM 表、字段、约束和索引名称一致性审查；
- 增加前端 `shared -> feature` 和 feature 内部越界导入的只读规则；
- 更新项目文档中的任务可见性、模块边界和检查能力；
- 删除无用入口、旧导出和重复状态映射，不保留兼容分支。

完成门槛：文档、descriptor、ownership、SQL、ORM 和真实导入一致；静态检查能报告本轮架构违规；无未登记循环、局部导入掩盖、跨 owner 写入或伪 `sys` 部门。

实际修改（2026-08-24）：

- 将 `scripts/check_backend.py` 从单纯导入应用并打印路由，改为只读架构检查入口：检查应用导入、表 owner、跨模块 persistence、descriptor 依赖和公开 API、函数内局部导入、模块循环、SQL/ORM 表/字段/具名约束/索引，以及前端 shared 和 feature 边界；检查过程不连接数据库；
- 校正 `identity` 与 `workforce` descriptor 的真实表所有权，并登记装配模块实际使用的 `planning.execution_api` 窄接口；ownership、descriptor、owner ORM 三者采用相同表名；
- 将工程流程校验器和生产撤回入口的局部导入提升为显式模块依赖，删除依靠执行路径延迟暴露依赖的写法；
- 核对 `product_route_task`、`production_route_task`、`work_order.created_by`、`finished_goods_transaction` 及相关索引在 `zzerp.sql` 和 ORM 中的表、字段、具名约束与索引名称一致；既有 QC/装配数量守恒、流水历史不可变和更新时间触发器保持原职责，未向数据库加入业务状态机；
- 将生产卡片共享过滤器从带私有名称的跨文件导出改为明确公共名称，删除无意义的私有导出形式，没有增加兼容别名；
- 更新 README、项目介绍和 AGENTS：明确草稿计划创建时生成路线任务并让部门可见，计划确认负责库存占用和执行资格；补充模块边界、静态检查能力及数据库不连接边界。

完成条件核对与只读证据：

1. 数据库/ORM：源码集合比对确认 SQL 与 ORM 表名、具名约束和索引名称无差异；全部 ORM 表均存在唯一 ownership 且 owner 与 persistence 所在模块一致；descriptor 已覆盖 owner 的全部真实表；路线投影、工单创建人和成品流水重点对象逐项一致；
2. 后端边界：AST 依赖审查确认 descriptor 未声明依赖和未登记公开 API 均已归零，模块依赖图无循环，模块内不存在函数级局部导入，未发现跨模块 persistence 导入；
3. 前端边界：源码导入审查确认 shared 不依赖业务 feature；除明确作为组合根的 departments 注册功能外，跨 feature 调用均经过目标 feature 公共入口；
4. 身份与数据库职责：源码搜索未发现伪 `sys` 部门判断或初始化数据；`sys:user:add` 仅为权限代码，不是部门；数据库函数未解析流程图，守恒和不可变触发器未被拆除或扩展为业务状态机；
5. 检查器稳定性：283 个后端及脚本 Python 文件通过 AST 语法解析，检查器自身语法有效；应用只读导入在当前宿主环境因未安装 FastAPI 运行依赖而无法完成运行时加载，该环境缺失不属于代码冲突，检查器会在完整开发环境中将此情况明确报告为失败而非静默跳过；
6. 代码规范：检查职责按依赖、循环、schema、前端边界和应用导入分函数组织；公共过滤器命名准确；`git diff --check` 无格式错误；
7. 低耦合：依赖只经过 descriptor 登记的公共或窄 API，表 owner 单一，前端 shared 无反向业务依赖，没有局部导入或兼容导出掩盖边界；
8. 高稳定：未改变订单、计划、生产、QC、库存和发货业务规则；未连接数据库、未修改触发器状态机边界、未启动服务，也未运行构建、格式化、数据库命令或项目脚本。

未完成事项：无。

### 第九阶段：最终零遗留复审

状态：已完成

执行内容：

- 按第二节相同范围重新生成依赖图、搜索边界违规、核对接口和数据库基线；
- 逐条回查第四节所有问题和每阶段完成门槛；
- 检查是否新增循环、重复实现、隐藏副作用、全量查询或静默回退；
- 新问题立即写入对应阶段并继续处理，不改写成“后续优化”；
- 只有范围内问题归零后，记录最终代码规范、低耦合和高稳定结论。

完成门槛：本文档未完成事项为空；所有阶段有具体修改和审查证据；不以“基本完成”“主要完成”“不影响主流程”替代未解决问题；无法处理的问题必须标记“受阻”，不能将整体整改标记完成。

复审发现与实际修改（2026-08-24）：

- 重新生成 12 个业务模块的 AST 依赖图和强连通分量，确认模块循环、函数内局部导入、跨 owner persistence、descriptor 未登记依赖和未登记公开 API 均为零；
- 扩展 `scripts/check_backend.py`：增加 Router 越过模块公开 API、旧顶层 `models/repositories/services` 复生以及跨模块 `model_api` 模型直接构造检查；将 departments 的跨 feature 页面装配例外收紧为各部门 `index.ts` 组合根；
- 删除源码目录中全部 `__pycache__` 和陈旧字节码，其中包含早已删除的旧 persistence、cards、work_orders 和 read_access 模块缓存；没有删除源码或业务数据；
- 删除无人引用的 `planning.model_api` 及 descriptor 声明，不保留兼容入口；
- 可达性复审确认旧 PMC 配件进度链已不再由任何页面使用，当前 PMC 已使用业务部进度明细表；删除旧 `/pmc/part-production-progress` Router、schema、planning 读模型/映射器以及对应前端页面、组件、composable、接口和类型，避免继续维护两套进度口径；
- 同步删除旧客户订单 `include_progress/product_progress` 可选链、逐订单项全量生产进度算法和未使用展示组件；客户订单列表继续使用现有分页数据，PMC 和业务部进度均使用当前进度明细表，不改变订单、任务数、发货或结单规则；
- 更新模块边界文档中的已登记读模型，移除已删除入口。

逐项回查与只读证据：

1. 第一至第三阶段：系统身份只使用 `is_system`，源码与初始化数据无伪 `sys` 部门；权限文本仅由 identity 解析；命令编排位于 departments，Router 未直接访问数据库或 persistence；QC 路由继续由编排层注入；
2. 第四至第五阶段：产品与计划路线投影仍绑定订单产品版本；部门任务查询、QC 列表、工艺单价和当前进度均先在数据库稳定分页再批量加载上下文；旧 PMC 全历史进度链和客户订单可选全量进度链已删除；
3. 第六至第七阶段：后端大职责文件拆分结果仍可达且无悬空导入；前端 154 个实际源码文件全部从 `main.ts` 可达，未发现孤立组件；shared 无 feature 反向依赖，跨 feature 内部访问仅存在于明确的 departments 组合根；表格、只读流程图和打印职责保持拆分；
4. 第八阶段：SQL、ORM 和 ownership 表集合一致且 owner 无差异；具名约束和索引集合一致；descriptor 覆盖所有 owner 表；数据库守恒、不可变和更新时间触发器未被改为业务状态机；
5. 导入与结构：276 个 Python 文件通过 AST 语法解析，全部内部导入符号存在；业务模块无强连通分量、局部导入、跨 owner persistence、外部 model 构造或孤立源码模块；Router 边界和旧顶层源码层检查归零；
6. 前端：本地与别名导入目标均存在，源码可达图无孤立文件；当前宿主环境未提供 Node 命令，因此未执行前端编译器语法解析，这不作为本轮代码审查的替代证据或运行通过声明；
7. 清理：源码搜索未发现旧 PMC 入口、`include_progress/product_progress`、已删除 planning model/order progress、测试脚本、旧 persistence/read_access 引用、TODO/FIXME/HACK 或陈旧字节码；
8. 格式与副作用：`git diff --check` 通过；未运行构建、项目脚本、数据库操作、服务或外部请求。

最终审查结论：

- 代码规范：职责和命名与当前真实用途一致，旧入口、孤立文件、缓存产物和重复进度算法已经删除，没有兼容别名或无价值代码；
- 低耦合：模块依赖图无循环，Router、部门编排、owner API、读模型和 persistence 边界明确，前端 shared/feature 方向正确；
- 高稳定：订单绑定版本、生产计划任务数、整单工单、QC 返回/放行、多路装配、库存身份、发货结单和计划手动完成业务基线未改变；SQL/ORM/ownership 一致，无静默回退、伪身份或重复状态机。

未完成事项：无。

## 十、执行顺序

1. 第一阶段先统一身份，避免后续继续传播伪部门判断；
2. 第二阶段解除 QC 反向路由，缩小最危险生产循环；
3. 第三阶段重建命令依赖方向和事务边界；
4. 第四阶段在稳定写边界上建立读模型和路线任务投影；
5. 第五阶段使用投影解决全历史扫描和 N+1；
6. 第六阶段在依赖稳定后拆分大文件；
7. 第七阶段统一前端契约、生命周期和展示基础设施；
8. 第八阶段同步数据库、静态检查和文档；
9. 第九阶段重新完整审查，未归零不得结束。

前一阶段未满足完成门槛时，不得把状态改为“已完成”并进入下一阶段。

## 十一、主要风险与控制

### 11.1 跨域事务被拆断

编排入口持有一个事务，owner 提供接收当前 Session 的事务 API；提交和回滚只在最外层发生。

### 11.2 路线任务投影成为第二套事实

投影只由绑定版本流程和计划项确定性生成；计划与投影同事务更新；唯一约束阻止重复；不允许人工编辑。

### 11.3 查询优化改变分页口径

每个接口先明确一行或一组的稳定身份；排序包含唯一主键；过滤、count 和 page 使用同一候选关系。

### 11.4 去除外部 ORM 导致查询爆炸

跨域只读模型允许登记后的集合 join；命令与读模型区别治理，不机械禁止全部外部 ORM。

### 11.5 公共前端能力影响所有页面

先建立显式 API，再逐页迁移；迁移完成后一次删除旧全局实现，不长期保留双实现。

### 11.6 SQL 与 ORM 再次漂移

每个数据库阶段同时修改 SQL、owner ORM、ownership 和静态规则，不建立旧结构兼容。

## 十二、阶段记录与硬性规则

每个阶段执行时必须记录：状态、实际修改、重要发现、未完成事项、代码规范、低耦合和高稳定审查结论。

状态只能使用：待执行 / 进行中 / 已完成 / 受阻。

硬性规则：

1. 计划范围内仍有问题时，阶段不得标记“已完成”。
2. 新发现的问题必须进入当前方案，不能只在对话中提到后遗漏。
3. 不允许用兼容分支、局部导入、静默回退或重复实现换取表面通过。
4. 如果实际修改需要改变依赖方向、数据库范围或业务口径，先更新本文档；涉及业务选择时先询问用户。
5. 最终报告必须明确说明修改、原因以及代码规范、低耦合和高稳定结论，不得隐瞒未完成项。

## 十三、当前状态

- 方案复审：已完成；
- 第一至第八阶段：已完成；
- 第九阶段：已完成；
- 当前整改全部阶段已完成；
- 当前未运行构建、项目脚本或数据库操作；
- 下一步：无。

# 装配部单路与多路车间职责调整方案

## 整改背景

当前项目以“车间所属部门是否为装配部”判断流程节点是否为多路，并把装配部所有默认工艺统一设置为 `multiple`。这导致装包车间也只能作为装配节点，无法表达“装包接收一个上游物料并产出成品”的业务。

用户已明确：装配部新增多路工艺“装配”；焊接仍为多路；装包改为非多路工艺，负责把一个上游物料打包成成品。

`zzerp.sql` 是开发数据库唯一重建基线，不维护旧字段或旧流程图兼容逻辑，数据库由用户自行重建。

## 目标

1. 将单路/多路属性归属到车间，而不是由部门代码推断。
2. 装配部新增“装配车间”和默认工艺“装配”，其流程节点为多路装配节点。
3. 焊接车间继续作为多路装配节点。
4. 装包车间改为单路工艺节点，使用标准工单执行，并可经 QC 放行到发货节点形成待入库成品。
5. SQL、ORM、后端契约、流程编辑器和生产工作台使用同一车间输入模式口径。

## 非目标

1. 不改变工单整单送检、直接填写结果、QC 返回/放行/入库等规则。
2. 不修改装配多路物料消耗和后续工艺使用在制品的规则。
3. 不自动迁移或改写现有正式流程图；用户重建数据库后按新车间类型重新配置。
4. 不新增旧 `procedure.input_mode` 兼容字段或按车间名称判断的临时分支。

## 业务基线

1. 流程节点代表车间，车间决定节点是单路 `process` 还是多路 `assembly`。
2. 具体工艺属于车间，不再单独保存与车间重复的输入模式。
3. 装配车间、焊接车间是多路；装包车间是单路。
4. 装包节点必须只有一个上游，并按正常规则连接 QC；QC 放行到发货节点后登记待入库成品。
5. 装配部页面同时承载多路装配任务和装配部所属单路加工任务，复用现有装配工单与标准工单边界。

## 影响范围

### 数据库与组织模型

- 在 `workshop` 增加 `input_mode` 及检查约束。
- 从 `procedure` 删除 `input_mode`，消除重复业务口径。
- 更新装配部车间、工艺和示例人员初始化数据。

### 后端

- 调整组织 ORM、read API、transaction API 和 schemas。
- 工艺解析只校验车间归属；工单类型由流程节点类型保证。
- 生产卡片按车间输入模式筛选工艺。
- 删除依赖 `procedure.input_mode` 的工单工厂重复校验。

### 前端

- 流程编辑器根据 `workshop.input_mode` 创建单路或多路节点。
- 节点列表的形状、颜色和“单路/多路”说明按车间输入模式展示，不再按装配部门一刀切。
- 生产工单接口继续接收按车间投影出的可用工艺，不增加前端业务判断。

### 文档

- 更新 `AGENTS.md` 和 `PROJECT_OVERVIEW.md` 的装配、焊接与装包规则。

## 实施阶段与状态

### 第一阶段：车间输入模式成为唯一口径

状态：已完成

实施范围：修改 SQL、ORM、组织 read/transaction API 和 schemas，将 `input_mode` 从工艺迁移到车间。

完成条件：数据库与 ORM 只在车间保存输入模式；组织接口可稳定返回车间输入模式；不存在 `procedure.input_mode` 访问。

### 第二阶段：工单与生产卡片适配

状态：已完成

实施范围：简化工艺解析和工单工厂校验；生产卡片按车间模式选择工艺；保持标准与装配执行边界。

完成条件：单路车间只进入标准工单入口，多路车间只进入装配工单入口；没有部门代码或车间名称硬编码。

### 第三阶段：流程编辑器与初始化数据调整

状态：已完成

实施范围：流程编辑器按车间模式创建节点；新增装配车间/装配工艺，焊接设为多路，装包设为单路；同步默认人员和文档。

完成条件：工程部节点列表能明确区分装配部的装配、焊接多路节点与装包单路节点；SQL 初始化口径一致。

### 第四阶段：只读一致性审查

状态：已完成

实施范围：搜索旧字段和部门硬编码，审查 Python 语法、项目内导入、前端本地导入、SQL/ORM 字段和差异。

完成条件：范围内不存在旧口径、重复口径、明显导入/类型/约束冲突；代码规范、低耦合和高稳定审查通过。

## 风险与控制

1. **流程节点类型与工单入口不一致**：节点类型只由车间 `input_mode` 创建，后端仍校验节点类型与车间归属。
2. **工艺模式重复存储导致漂移**：删除 `procedure.input_mode`，工艺天然继承所属车间的输入模式。
3. **装包无法形成成品**：保持 process → QC → shipping 的现有放行路径，复用标准工单与成品待入库逻辑。
4. **装配部单路任务页面缺失**：复用装配页面现有 `processItems` 和标准工单弹窗，不新增重复页面。
5. **旧流程图仍把装包保存为 assembly**：不增加兼容逻辑；开发数据库按新 SQL 重建并重新配置流程。

## 代码审查方式

- **代码规范**：输入模式只保留一个数据所有者，不使用名称判断、默认回退或兼容字段。
- **低耦合**：组织模块拥有车间模式，工程流程和生产执行只消费公开投影；标准执行与装配执行职责不混合。
- **高稳定**：SQL、ORM、schemas、前端类型同时修改；成品流转继续使用现有 QC 放行与成品待入库链路。

## 最终结果

状态：已完成

### 实际修改

1. 将 `input_mode` 从 `procedure` 迁移到 `workshop`，SQL 和 ORM 只在车间保存单路/多路属性。
2. 组织模块的车间读模型、HTTP schema 和前端车间类型同步返回 `input_mode`；工艺读模型不再暴露或保存重复输入模式。
3. 工艺解析根据所属车间校验工单所需模式；删除失去职责的 `production_core/work_order_rules.py` 及其重复工艺模式校验。
4. 工程流程保存时校验节点类型必须与车间模式一致；流程编辑器按车间模式创建 `process` 或 `assembly` 节点，不再按装配部门代码推断。
5. `zzerp.sql` 新增多路“装配车间/装配”与示例工人，焊接保持多路，装包明确设为单路并增加装包示例工人。
6. 装配部现有页面继续按节点类型分流：装配和焊接进入多路分组及装配工单；装包进入单路任务及标准工单。装包经 QC 放行到发货节点后复用现有待入库成品逻辑。
7. 更新 `AGENTS.md` 和 `PROJECT_OVERVIEW.md`，明确车间是输入模式唯一所有者及三类车间职责。

### 只读审查证据

1. 对 265 个后端 Python 文件执行只读语法编译，全部通过。
2. 静态解析后端项目内模块导入和前端本地导入，引用目标均存在。
3. 对照 `zzerp.sql` 与组织 ORM，`workshop`、`procedure` 字段完全一致。
4. 搜索 `Procedure.input_mode`、旧工艺输入模式约束和已删除工单规则文件，运行源码与 SQL 中均无残留。
5. 初始化数据明确为：装配 `multiple`、焊接 `multiple`、装包 `single`，并分别绑定装配、焊接、装包工艺。
6. `git diff --check` 通过，未发现空白错误。

### 代码审查结论

- **代码规范**：单路/多路只有车间一个数据所有者；没有按名称判断、部门级默认覆盖或旧字段兼容分支。
- **低耦合**：组织模块拥有车间模式，工程流程、标准执行、装配执行和前端编辑器只消费明确契约；未混合标准工单与多路装配工单职责。
- **高稳定**：流程保存、开单和工艺配置均校验车间模式；装包沿用已存在的标准工单、QC 放行和成品待入库链路，没有新增重复状态机。

未启动服务、未执行构建、未运行项目脚本，也未连接或重建数据库；数据库由用户使用更新后的 `zzerp.sql` 自行重建。

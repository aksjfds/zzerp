# 物料加工状态与仓库续接整改方案

## 整改背景

当前临时仓库仅用 `completion_status` 文本表示物料加工情况，并通过该文本反查流程节点。现有“粗光1完”中的序号代表流程图同名节点出现次序，不代表工单工艺“粗1”；同一车间内完成不同正式或临时工艺、直接结果、QC 送回和 QC 放行会被压缩为相同状态，可能合并本应从不同节点继续的库存。

## 整改目标

1. 使用不可变、结构化的物料加工状态表达工艺履历、QC 去向和下次继续节点。
2. 不新增物料批次表；未来生产行为完全相同的数量共享状态定义，不同工艺履历或继续节点不得合并。
3. 仓库外部业务字段继续保留可读的状态文字，后端不得解析显示文字决定流转。
4. 初始、标准工单、临时工单、装配工单、委外 QC、QC 送回和 QC 放行均使用统一状态规则。
5. 生产计划取用库存时依据结构化继续节点注入生产，不再由“完成状态”猜测节点。
6. 保持工单数量、QC 数量、库存账实和成品入库规则不变。

## 非目标

- 不增加物料批次或库存批次表。
- 不恢复必做工艺或固定工艺顺序。
- 不改变成品仓库存身份和成品入库流程。
- 不兼容旧数据库、旧完成状态或旧库存数据；开发数据库由 `zzerp.sql` 破坏性重建。
- 不执行数据库重建、项目构建或服务启动。

## 业务基线

- 初始物料状态显示“未加工 · 待某节点”。
- 直接填写结果后追加实际工单工艺，物料留在当前节点。
- QC 送回与直接结果可具有相同继续节点，但 QC 状态必须不同。
- QC 放行后的继续节点为流程下游节点。
- 装配首次工单生成新的装配体身份并从该装配工艺开始履历；装配后续工艺继续追加履历。
- 委外加工工艺使用工单自由文本快照进入履历。
- 加工中、质检中、返工、报废、遗失及 QC 待决定去向不进入仓库状态。
- 相同产品、版本、物料、工艺履历、QC 状态和继续节点才允许共享加工状态。

## 实施阶段

### 第一阶段：数据库与 ORM 状态基线

状态：已完成

- 增加不可变 `material_processing_state` 状态定义表。
- 生产仓位、工单输入快照和仓库操作保存状态引用。
- 更新 `zzerp.sql`、ORM、模块所有权和 descriptor。

完成条件：数据库与 ORM 字段、约束、索引及表所有权一致，状态定义不保存数量。

实际修改与证据：

- 新增 `material_processing_state`，只保存产品版本、物料身份、工艺履历、QC 去向、完成节点、继续节点、显示文字和稳定签名，不保存数量。
- `repository`、`work_order`、`work_order_material`、`warehouse_operation`、`production_warehouse_storage_line` 均保存必要的状态引用；工单及装配投入通过组合外键锁定来源仓位和来源状态。
- 状态历史由数据库触发器禁止更新和删除；签名、上下文查询和业务关联均具备约束或索引。
- `zzerp.sql`、ORM、`modules/ownership.py` 和 `production_core` descriptor 已同步；只读列对照结果为 `SQL_ORM_COLUMNS_OK`。

### 第二阶段：统一状态领域服务

状态：已完成

- 集中生成状态签名、显示文字和状态转换。
- 支持初始、直接结果、QC 送回、QC 放行及装配身份重置。

完成条件：其他模块不拼接或解析加工状态文字。

实际修改与证据：

- 新增 `production_core/material_state_api.py`，统一负责初始状态、工单履历追加、装配身份重置、QC 送回、QC 放行和 QC 入仓状态。
- 状态签名由完整结构化内容生成，并以数据库唯一约束和冲突安全插入保证并发下复用同一状态。
- 显示文字仅由领域服务生成；临时工艺带有“临时”标识，避免与同名正式工艺形成不可区分的库存状态。
- 全项目搜索未发现通过 `completion_status` 拆分、替换或后缀判断来反推流程节点的实现。

### 第三阶段：工单与 QC 状态流转

状态：已完成

- 标准、装配和委外工单记录输入状态。
- 直接结果及 QC 去向生成正确输出状态。

完成条件：正式和临时工艺均进入履历，QC 动态处理中数量不生成可入库状态。

实际修改与证据：

- 标准工单记录来源状态，直接结果追加工艺后留在当前车间；QC 送回和放行分别生成不同状态。
- 装配首张工单保存每一路投入状态并重置为新装配体履历；后续工艺从装配体来源状态继续追加，取消时按快照恢复。
- 委外工单在 QC 放行或入仓时写入供应商加工工艺快照，不引入工艺主数据依赖。
- 加工中、待质检、返工、报废和遗失继续由工单及 QC 批次状态表达，只有形成可用生产仓位或执行入仓时才创建可库存化状态。

### 第四阶段：生产节点入库

状态：已完成

- 入库候选读取仓位状态定义。
- 仓库入库按状态显示文字分库存放，并在本项目操作记录保存状态引用。

完成条件：不同继续节点或不同履历不会合并，页面能显示清楚的加工情况。

实际修改与证据：

- 可入库位置按“生产物料 + 加工状态 + 当前节点 + 来源节点”分组，不再把不同履历数量合并。
- 入库请求和本项目仓库操作记录携带状态引用；实际外部仓库仍只保存业务可读的加工状态文字。
- 生产仓位消耗明细保存状态快照，受限冲销按原状态和原位置恢复，不重新计算历史。
- 生产入库接口和页面统一使用“加工情况”字段，返回结构包含工艺履历、QC 状态及继续节点。

### 第五阶段：生产计划库存取用与续接

状态：已完成

- 计划库存读取只接受本项目已登记的加工状态。
- 出库操作携带状态引用，生产仓位按结构化继续节点恢复。

完成条件：不解析显示文字、不通过完成节点推测去向；未知状态明确拒绝自动取用。

实际修改与证据：

- 计划库存只把外部库存状态文字与当前产品版本已登记的结构化状态进行精确匹配；未知状态不参与自动可用量。
- 同一物料存在重复显示文字时明确报错，不进行不确定分配。
- 仓库出库操作逐行保存实际状态引用，并将状态引用和继续节点传给生产核心。
- 生产注入校验产品、版本、BOM/装配体身份和来源节点后，直接按 `resume_flow_node_id` 恢复，不再解析显示文字或推测完成节点。

### 第六阶段：接口与前端展示

状态：已完成

- 接口返回加工履历、QC 状态、继续节点和显示文字。
- 物料入库、计划库存和仓库页面使用统一展示口径。

完成条件：用户能区分未加工、留在当前车间、QC 送回和 QC 放行。

实际修改与证据：

- 生产入库响应增加类型化工艺履历、QC 状态、完成节点、继续节点和统一加工情况。
- 生产工作台、生产任务、装配任务和业务进度明细批量读取状态定义，以完整履历判断“未开始/当前节点已加工”，不再使用上一张工单名称代替物料状态。
- 生产计划库存界面改用“加工情况”口径；生产入库前后端字段已统一为 `processing_status`。
- 业务进度中，到货计算传入工单索引，物料位置投影传入批量加载的 `processing_states`；两个读模型的输入契约已分开。
- 生产工单详情统一使用工单保存的工人名称快照，删除旧 `workers` 查询和已失效的参数传递，避免点击“查看工单”时的调用契约错误。

### 第七阶段：旧逻辑清理与完整审查

状态：已完成

- 删除通过 `completion_status` 反推流程节点的旧逻辑和“流程节点名 + 次序 + 完”生成规则。
- 搜索残留并执行只读语法、边界、SQL/ORM 和差异审查。

完成条件：七阶段范围无遗留，代码规范、模块低耦合和稳定性审查均有实际证据。

实际修改与证据：

- 删除 `production_core/completion_status.py` 及流程 API 中旧的同名节点编号、显示文字生成和文字反查入口。
- 后端 278 个 Python 文件 AST 解析通过；本轮关键跨模块调用签名检查结果为 `TARGET_CALL_SIGNATURES_OK`，本地导入符号检查结果为 `LOCAL_IMPORT_SYMBOLS_OK`。
- 前端 `vue-tsc --noEmit` 检查通过；`git diff --check` 通过。
- 旧映射函数、旧完成节点字段及加工状态文字解析搜索无代码残留；仓库表中的 `completion_status` 仅作为外部业务字段和显示快照保留。

## 影响范围

- `zzerp.sql`
- `domain/warehouse.py`、`domain/production_inventory.py`
- `modules/production_core` 的仓位、工单、QC 与库存注入
- `modules/inventory` 的临时仓库操作
- `modules/planning` 的库存展示、分配与计划确认
- 生产、业务和仓库前端的加工状态展示

## 风险与控制

1. 状态转换集中在 `production_core`，库存模块只保存外部库存和操作结果，避免复制生产状态机。
2. 状态定义使用稳定签名和唯一约束，避免并发创建重复状态。
3. 工单输入保存状态快照引用，撤回和取消沿既有仓位快照恢复，不重算历史。
4. 未登记的外部状态不得静默映射或自动取用。
5. 不维护旧状态兼容分支，避免开发阶段长期双轨逻辑。

## 代码审查方式

- 核对 SQL、ORM、领域契约和所有调用签名。
- 搜索旧完成状态映射、字符串解析和跨模块内部导入。
- 使用只读 AST、TypeScript `--noEmit` 条件允许时审查语法；至少执行 `git diff --check` 和引用搜索。

## 最终结果

状态：已完成

七个阶段的实施范围和完成条件均已落实。

- 代码规范：加工状态生成、校验和转换集中于单一领域服务；接口字段和前端类型含义一致；未增加兼容分支或文字解析规则。
- 低耦合：`production_core` 拥有状态定义和生产流转，`inventory` 只管理仓库库存及操作，`planning` 通过公开窄接口读取状态并编排计划，部门层承担跨模块事务编排。
- 高稳定：状态不可变、输入快照可追溯、库存取用按稳定引用恢复；未知或歧义状态明确拒绝；SQL/ORM、调用签名、Python 语法、TypeScript 类型和差异格式审查均通过。
- 按非目标约束，未重建数据库、未启动服务、未执行项目构建。

# zzerp v6 项目介绍

## 1. 项目定位

zzerp v6 是一套面向五金制造业务的模块化 ERP，覆盖从产品工程配置、客户订单、生产计划，到车间工单、质量检验、库存、成品发货和人员计价的完整业务链。

系统的核心目标不是单纯记录订单，而是把以下对象放在同一条可追踪的数据链中：

> 客户订单 → 产品版本 → BOM 与生产流程 → 生产计划 → 物料流转 → 工单与 QC → 库存与成品 → 发货与结单

当前项目处于开发阶段，数据库以 `zzerp.sql` 作为破坏性重建基线，不承担旧结构和旧数据兼容。

## 2. 技术架构

### 后端

- Python 3.12
- FastAPI
- SQLAlchemy
- PostgreSQL
- Uvicorn

后端采用模块化单体结构。业务模块拥有自己的数据和规则，通过公开 API 或部门能力门面协作；不同生产部门共享统一的生产库存、物料流转、工单和 QC 状态模型。

### 前端

- Vue 3
- TypeScript
- Vite
- Element Plus
- Vue Router

前端按业务功能拆分，每个功能目录管理自己的页面、组件和接口类型；跨功能复用内容放在 `shared`，部门入口通过部门注册表统一组织。

### 数据库与部署

- `zzerp.sql`：唯一的破坏性重建基线，统一维护完整表结构、声明式完整性约束、并发底线触发器和开发初始化数据。
- SQLAlchemy ORM：与 `zzerp.sql` 保持同一表、字段、约束和索引口径，用于应用访问，不作为第二套数据库结构来源。
- 后端领域模块负责工单类型、流程节点、送检资格、QC 路由和状态转换；数据库函数不解析流程图，也不维护完整业务状态机。
- 数据库触发器只保留并发数量、QC 结果与流水守恒、生产/库存流水及已完成 QC 历史不可变等底线，每个触发器保持单一职责。
- 初始化数据先创建部门，再创建账号、车间、工艺和默认人员；示例客户、产品、BOM、流程图、订单和示例人员集中在可删除区段。
- Docker Compose：本地容器化开发。
- Render：后端测试部署配置。
- Netlify：前端测试部署配置。
- Neon PostgreSQL：测试环境数据库。

## 3. 主要使用部门

### 业务部

负责客户订单、生产计划和订单进度明细。

- 创建、修改、确认、取消和查看客户订单；
- 订单确认后处理系统创建的草稿生产计划；
- 根据 BOM 净需求和库存参考填写普通配件计划数量；
- 查看订单、任务、出货和欠货情况；
- 在生产计划确认后按计划任务创建唯一委外加工工单，填写本单供应商和加工工艺快照；
- 在生产条件满足后手动完成生产计划。

### 工程部

负责产品、产品版本、BOM 和宏观生产流程图。

- 维护产品和客户基础关联；
- 配置产品版本；
- 配置 BOM 配件；
- 在流程图中配置车间、QC、委外加工、装配和入库等节点；
- 维护装配关系和系统生成的稳定装配体身份；
- 判断产品是否已经配置完整并可用于创建订单。

工程部只配置宏观车间流程，不负责配置车间内具体要开的工艺工单。

### 生产部门

包括冲压、CNC、抛光及其他标准生产部门。各部门共享生产工作台，但只处理本部门负责的车间节点。

- 生产计划确认后查看本部门生产任务；
- 按实际需要选择工艺并开工单；
- 对整张工单填写加工结果或送检；
- 查看本部门当前物料和工单进度；
- 将停留在本部门、尚未执行当前节点加工且未被工单占用的物料存入仓库。

### 装配部

负责装配、焊接和装包。装配与焊接是多路加工节点；装包是装配部单路加工节点，负责将一个上游合格物料打包成成品，并可直接进入入库终点。

装配工单需要同时考虑多个输入配件或装配体的可用数量。生产任务以装配体为父项、所需物料为子项展示各输入物料的到货情况。

### 外协部

负责蚀字、电镀等外协工单以及对应外协单位。

外协部使用普通车间、工艺和标准工单；它不负责业务部创建的独立委外加工工单。

### QC 部门

负责标准/装配整张工单质检，以及委外加工工单的分批质检。

QC 合格后决定物料返回当前车间继续加工，还是放行到流程图下一节点；不合格物料进入返工路径。QC 记录与原工单、检验批和物料流转保持关联。

委外加工不预先登记到货或送检。QC 按实际检验批次直接填写合格、返工、报废和遗失数量，每批合格品再独立放行到正式下一节点；委外返工只保留结果历史，不建立返工复检。

### 仓库

部门代码为 `warehouse`，负责查看临时仓库中的非成品配件、装配体库存，以及本项目执行的仓库操作记录。生产计划确认时由系统直接扣减库存，不由仓库人员再次手工出库。

### 成品部

部门代码为 `finished`，负责成品入库和客户订单发货。入库与发货在不同页面 Tab 中处理；生产计划创建后，其计划产品即按产品和版本显示在成品入库表中，表格同时展示汇总计划数量和累计到货数。实际成品仍须到达入库终点并由成品部确认后才增加库存。

## 4. 核心业务对象

### 产品与产品版本

产品保存厂编、产品名、客户等基础信息。产品版本冻结订单执行时使用的 BOM 和正式生产流程。客户订单一旦绑定产品版本，后续生产计划、工单和进度计算都应基于该版本。

### BOM

BOM 描述生产一个成品所需的普通配件及用量。业务部填写生产计划时只填写普通配件数量，装配体和成品由系统内部根据 BOM 与流程图计算和追踪。

同一逻辑配件不使用替代来源模型；BOM 中每一行都是独立且稳定的工程物料身份。

### 生产流程图

生产流程图描述物料在不同车间、委外加工、装配、QC 和入库节点之间的宏观流转关系。

- 工程部使用可编辑流程图；
- 业务部及其他有权限部门使用只读进度流程图；
- 装配和焊接节点支持多路输入，装包节点只允许一个上游输入，并允许“上游 QC → 装包 → 入库”；
- 委外加工是独立节点，不引用车间，只允许“普通配件 → 委外加工 → QC”；
- 装配体编号从 `-81` 开始由系统生成，并在产品版本内保持稳定。
- 正式流程图的节点 ID 是产品版本内的稳定业务身份，计划项、仓位、工单、QC、库存和流水均使用这一身份。
- 产品版本被订单、生产或库存历史引用后，BOM 和正式流程冻结；流程调整必须创建新版本，旧订单不会切换到新版本。
- 仅存在工艺单价配置时仍可调整节点坐标和显示内容，但被配置引用的节点不能删除、换号或改变类型、车间及 BOM 归属。

### 生产计划

客户订单确认后，系统生成草稿生产计划。业务部结合系统净需求和库存参考填写普通配件生产数量，再确认计划。

确认计划后：

- 系统按订单绑定版本和结构化加工状态直接扣减非成品临时仓库存，并按状态记录的继续节点把物料注入生产流程；
- 对统一成品仓只建立计划占用，不扣减成品实存，也不需要仓库人员手工出库；
- 计划内配件、装配体和成品分别进入生产跟踪。

草稿生产计划只供业务部填写和确认，不向生产部门发布任务。计划确认后，系统才按订单绑定版本发布路线任务投影，同时扣减非成品库存、占用成品库存并允许后续执行。

客户订单结单不会停止生产计划。业务部可以手动完成已确认计划，操作前必须确认提示；计划完成后已有物料仍可继续开工单。生产节点物料是否可直接入库不依赖计划完成状态。

### 车间工艺

车间节点内部的工艺由所属部门管理。工艺身份由产品、产品版本、物料、车间节点和工艺共同确定。

系统没有用于完成判定的必做工艺，也没有固定工艺顺序。所属部门先按产品版本、物料和车间节点配置工艺并确认；确认后工艺清单不可修改，单价仍可补充和调整。该配置范围从未创建过任何工单时可以取消确认并恢复编辑；普通、临时、已取消或已完成工单只要存在，均不允许取消确认。普通工单只能选择已确认配置中的工艺；需要配置外工艺时创建临时工单，临时工艺写入车间工艺库但不加入已确认配置，单价快照为空。

### 工单

一张工单只对应一个车间节点和一个工艺。同一工艺可以拆成多张工单，同一批物料也可以在同一车间连续执行多张不同工艺工单。

工单按整单处理，不支持部分数量送检或部分数量填写结果。完成时可以：

- 直接填写整单结果，物料留在当前车间，可继续开下一张工艺工单；
- 整单送检，由 QC 决定返回当前车间或放行到下一节点。

委外加工工单是独立例外：业务部在计划确认后按“计划配件 + 稳定委外节点”只能创建一张，数量固定等于计划任务数，供应商和加工工艺为本单自由文本快照。开单不消耗库存，也不产生到货或送检记录。QC 可分次录入完整结果，累计质检可因异常超过任务数，但累计合格不得超过任务数；每批合格品单独放行，已放行合格数达到任务数后工单结单。

### 库存与物料流转

临时仓库按品号、产品版本、物料类型、加工状态和仓库区分非成品库存。加工状态由系统保存为不可变的实际工艺履历、最近 QC 去向和继续节点；仓库业务字段保留可读文字，但系统不解析该文字决定流转。成品部统一成品仓只按产品和产品版本区分，相同产品版本可供不同订单和生产计划共享。

库存系统分别记录：

- 临时仓库非成品库存与统一成品实存；
- 生产计划确认时的非成品直接扣减和成品库存占用；
- 生产节点之间的物料移动；
- QC 合格品入库，以及生产节点尚未加工物料的直接入库；
- 成品整批入库、客户发货和统一不可变流水。

## 5. 主要业务流程

### 产品投入使用

1. 工程部创建产品和版本。
2. 配置 BOM。
3. 配置并正式保存生产流程图。
4. 系统确认配置完整后，产品状态变为可用。
5. 业务部只能用可用产品版本创建订单。

### 订单到生产

1. 业务部创建并确认客户订单。
2. 系统创建草稿生产计划。
3. 业务部填写普通配件计划数量。
4. 系统校验 BOM 可满足的成品数量。
5. 草稿计划不发布生产任务；业务部确认计划后，相关部门才能看到生产任务，同时系统直接扣减可用非成品库存并注入生产流程，对可用成品库存只建立占用。
6. 未使用库存的计划数量由生产部门从初始物料开始生产。

### 工单到节点流转

1. 部门在当前车间节点选择物料和工艺开工单。
2. 工单按整单填写结果或整单送检。
3. 直接填写结果时，物料留在当前车间。
4. 送检时，QC 记录检验结果。
5. QC 合格后选择返回当前车间或放行下一节点。
6. 多路节点收齐所需输入后生成装配体或后续物料。
7. QC 放行或装包直接完成到“入库”终点后形成待入库批次，成品部整批确认后进入统一成品仓。

委外加工分支不套用上述整单送检步骤：业务部开出唯一委外工单后，QC 按实际检验批次直接录入结果并逐批放行；异常数量只保留历史，不生成后续供应商任务或返工复检。

### 成品入库、发货与结单

1. 生产计划创建后，成品部的成品入库表即按产品和绑定版本显示汇总计划数量；到货数累计该产品版本已经形成的全部成品待入库记录，已确认入库后不会从到货数中扣除。计划数据不提前形成待入库记录或成品库存。
2. 成品部整批确认 QC 放行或装包完成形成的待入库批次，实存与入库流水在同一事务增加。
3. 成品部按客户订单发货；发货优先消耗该订单占用，再使用未占用库存，并且只在发货时扣减实存。
4. 发货达到订单数量后客户订单自动结单，剩余成品仍留在统一成品仓，不做订单间转移。
5. 客户订单结单不停止或完成生产计划。
6. QC 合格的非成品可选择入库；到达当前节点但尚未加工、未被工单占用的配件或装配体可由所在生产部门直接入库，不要求生产计划完成，也不重复 QC。
7. 业务部手动完成已确认生产计划；完成不会停止后续生产。

### 业务撤回与更正

系统对误操作采用“受限撤回”而不是直接覆盖历史：只有相关数量尚未被下游工单、QC、库存、发货或其他流转使用时才允许撤回，无法安全恢复时返回明确原因。

- 未产生质检和下游流转的委外加工工单可以取消。
- 未开工、未送检且未形成下游记录的生产计划可以撤回确认；仓库领料以反向仓库记录退回，成品占用同步释放。已完成计划在完成后尚未办理有效生产节点物料入库时可以恢复为生产中。
- 普通、装配及装包工单的最近一次提交可以按生产操作快照撤回；装包形成的待入库记录必须仍未确认。QC 合格品去向在尚未被下游使用时可以撤回，存入仓库的数量同步冲销。
- 非成品生产节点入库、成品确认入库和客户发货均使用引用原记录的反向流水；原成功流水保持不可修改，同一原操作只能冲销一次。发货冲销同时恢复成品库存、计划占用、订单出货数量和订单状态；成品入库冲销重新生成待入库记录。
- 工人未被工单或 QC 引用时可以删除；被引用后只能更正名称，历史工单继续使用开单时的姓名快照。
- 正式及临时工艺单价的实际变更写入追加式历史，既有工单的单价快照不随主数据变化。

## 6. 项目目录

```text
zzerp/
├── AGENTS.md                 AI 与 Agent 协作规则、最终业务约束
├── MODIFICATION.md           大型整改方案、阶段状态和验证记录
├── PROJECT_OVERVIEW.md       项目定位、架构和业务介绍
├── README.md                 项目入口、开发与部署提示
├── main.py                   根级后端启动入口
├── zzerp.sql                 PostgreSQL 完整重建基线
├── package.json              根级开发脚本
├── backend/
│   ├── requirements.txt      后端运行依赖
│   ├── src/
│       ├── main.py           FastAPI 应用和路由装配
│       ├── auth_dependencies.py  登录身份依赖
│       ├── authorization.py  权限检查
│       ├── config.py         环境配置
│       ├── database.py       数据库会话基础设施
│       ├── departments/      部门门面与部门能力组合
│       ├── domain/           跨模块领域类型、错误和稳定常量
│       ├── modules/          按业务域划分的核心模块
│       ├── routers/          HTTP 路由入口
│       └── schemas/          请求与响应结构
├── frontend/
│   ├── package.json          前端脚本与依赖
│   └── src/
│       ├── api/              通用接口基础设施
│       ├── features/         按业务功能拆分的页面和组件
│       ├── permission/       权限常量与路由守卫
│       ├── router/           前端路由
│       ├── shared/           跨功能共享组件和工具
│       ├── stores/           全局状态
│       ├── styles/           全局样式
│       ├── types/            跨功能类型
│       └── views/            通用页面入口
└── scripts/
    ├── check_backend.py      应用导入、架构边界及 SQL/ORM 静态一致性检查
    ├── reset_database.py     开发数据库破坏性重建
```

## 7. 后端模块分工

`backend/src/modules` 中的主要业务模块包括：

- `identity`：账号、会话与身份认证；
- `organization`：部门、车间和工艺基础数据；
- `engineering`：产品、版本、BOM 和生产流程；
- `sales`：客户订单和业务进度；
- `planning`：生产计划、需求展开和跨域进度读模型；
- `production_core`：生产物料、工单、节点流转和共享生产规则；
- `standard_execution`：标准生产部门执行能力；
- `assembly`：装配和多路物料执行；
- `quality`：检验批、QC 操作、返回和放行；
- `supplier_processing`：业务部委外任务、唯一开单和委外分批 QC 编排；
- `inventory`：临时仓库、直接出入库操作、成品库存和流水；
- `workforce`：工人、计价和工资相关数据。

模块自己的 ORM、查询和事务实现位于对应模块目录。旧顶层 `models`、`repositories`、`services` 源码层已经删除，不属于当前架构。模块应通过公开接口协作，不应直接读取其他模块内部表结构来复制业务判断。

## 8. 前端功能分工

`frontend/src/features` 中的主要功能包括：

- `process-designer`：工程部产品、BOM 和流程图编辑；
- `customer-orders`：业务部订单、生产计划、进度明细和委外开单；
- `customers`：客户查询和客户公共类型；
- `production`：生产任务、工作台、工单和生产情况；
- `inventory`：库存和库存流水；
- `departments`：部门页面注册和部门专属入口；
- `workers`：工人和工资相关页面；

工程部流程图编辑器与其他部门的只读进度流程图具有不同职责。共享代码只能复用纯展示或纯计算能力，不能让只读交互修改编辑器行为。

跨 feature 调用必须经过目标 feature 的 `index.ts` 公共入口。`router` 和部门注册表负责具体页面装配；`shared` 只保存跨功能公共能力，不能依赖业务 feature。

`scripts/check_backend.py` 以只读方式核对应用能否导入、ORM 表所有权、跨模块 persistence、descriptor 依赖与公开 API、局部导入和模块循环，并比对 `zzerp.sql` 与 ORM 的表、字段、具名约束和索引名称；同时检查前端 `shared -> feature` 反向依赖和 feature 内部越界导入。该检查不连接数据库，也不替代数据库重建。

## 9. 文档边界

- 项目是什么、有哪些模块、业务如何流转：阅读本文件。
- AI/Agent 能做什么、修改时必须遵守什么：阅读 [`AGENTS.md`](AGENTS.md)。
- 大型整改为什么做、执行到哪一阶段：阅读 [`MODIFICATION.md`](MODIFICATION.md)。
- 开发入口和环境提示：阅读 [`README.md`](README.md)。
- 数据库真实结构：以 [`zzerp.sql`](zzerp.sql) 和当前后端模型共同核对，不能只依赖说明文档。

## 10. 已确认的详细业务与界面规则

本节记录当前项目已经确认的详细业务规则和业务界面规则。

### 10.1 产品、BOM、版本与流程图

- 工程部产品列表只标注“可用/不可用”。业务部不能选择不可用产品创建订单，页面直接显示“不可用”，不暴露工程内部缺项原因。
- 客户订单绑定明确的产品版本；生产计划、BOM 展开、流程图、工单和进度始终使用该版本，不能自动切换到产品最新版本。
- 同一客户订单禁止重复选择相同产品版本。
- 同一逻辑配件不支持双来源；BOM 中每条物料使用独立且稳定的身份。
- 装配体编号按正式流程图装配顺序从 `-81` 开始生成，坐标顺序为从左到右、从上到下。
- 装配体名称按参与物料顺序组合，并保持稳定身份，不能因重新渲染或临时改名生成新身份。
- 工程部负责产品版本、BOM 和宏观流程图。工艺与装配节点表示车间，节点名就是车间名；委外加工节点不表示车间。工程部不配置车间内具体工艺，也不填写委外供应商或加工工艺。
- 同一车间节点内部可以连续开多张工艺工单，不需要为每道工艺创建流程节点。
- 装配和焊接多路节点允许多个上游输入；装包单路节点只能有一个上游输入，并可直接连接入库终点。
- 流程图编辑器不按部门边界强制插入 QC；实际物料跨执行节点仍由 QC 放行控制，唯一例外是上游已合格的物料完成装包后可直接进入入库终点。

工程部流程图编辑器与其他部门的只读进度流程图分开维护。业务部版本是其他有权限部门只读流程图的基准，修改只读版本时同步其他部门，但不得改变工程部编辑器。

流程图当前交互规则：

- 禁用鼠标滚轮平移；工程部使用滚轮键拖动画布、左键框选节点。
- 节点拖动时自由移动，松开后吸附网格；当前网格为 X 轴 500px、Y 轴 250px，修改前仍需核对真实代码。
- “适应”只调整视口以显示全部节点，不重新布局。
- 节点大小、字体大小和连线粗细调整需要持久化；箭头大小不随线宽变化。
- 节点文字不可拖选、不可溢出，不显示输入光标。
- 只读节点详情使用 Popover，不常驻展示“入/出”。
- 连线默认不穿过节点，优先减少拐弯；需要两次拐弯时在较长轴距离的中点附近转折，不能贴近起点或终点。
- 节点移动后按同一规则重新计算连线。
- 自动连接按节点真实空间顺序逐个连接，不能跳过 BOM 起点或错误直连首尾节点。

### 10.2 客户订单与生产计划

- 业务部“客户订单”和“生产计划”为两个独立 Tab。
- 创建或保存订单成功后离开编辑界面。
- 确认客户订单后，系统创建草稿生产计划。
- 已确认但尚未创建或启动计划的订单取消后仍可查看和编辑；所有取消订单保留查看入口。
- 发货达到订单需求数量后客户订单自动结单。
- 订单结单不停止、也不自动完成生产计划，计划可以继续生产。
- 业务部可手动完成已确认生产计划，确认提示必须说明完成后现有物料仍可继续生产；生产节点物料入库不以计划完成为前提。

生产计划填写规则：

- 业务部只填写 BOM 普通配件计划数量，装配体和成品不作为可填写明细。
- 系统内部可以保留装配体和成品计划项用于计算与执行。
- 普通配件计划数量必须大于等于系统净需求。
- 系统按 BOM 用量校验配件可满足的成品数量不低于订单需求。
- 库存仅供填写时参考，按产品分组展示配件、装配体和成品。
- 每个产品只提供一个库存换算入口，用 Popover 展示装配体和成品折算后的数量，不为每条库存项单独放按钮。
- 计划填写时只读取库存参考；确认时重新读取，非成品库存直接扣减并进入对应生产流程，成品库存只建立占用。
- 仓库不提供计划手工出库；计划取消分别按非成品既有约束和成品未发货占用释放规则处理。

数量口径：

- “任务数”只表示生产计划数量，不使用客户订单数量回退。
- 业务部进度明细表任务数读取生产计划内部成品项的计划生产数量。
- 生产部门删除订单数量相关字段和值，只使用生产计划任务数。
- 生产任务表展示 `任务数量 / 到货数量`、完成数量和欠交数量；欠交数量 = 任务数量 - 完成数量。
- 加工完成不能清除已经发生的到货数；完成数使用清晰的亮绿色。

### 10.3 车间工艺、工单与 QC

工艺业务身份为：

> 产品 + 产品版本 + 配件/装配体 + 流程节点（车间） + 工艺

- 工艺与单价由节点所属部门管理。
- 单路/多路是车间属性，具体工艺继承所属车间的输入模式，不重复保存该属性。
- 不存在用于完成判定的必做工艺或固定工艺顺序，文员按实际情况开单。
- 一张工单只选择一个工艺；同一工艺可以拆成多张工单。
- 普通工单只能选择当前范围已确认的配置工艺；没有已确认配置时不能进入开单弹窗。
- 已确认配置的工艺清单不可新增、删除或更换，单价仍可补充和调整；仅当该范围从未创建过任何工单时可以取消确认并恢复编辑。
- 配置外工艺通过临时工单创建；临时工艺进入车间工艺库，但不加入当前产品版本配置，工单单价快照为空。
- 已被工单引用的工艺禁止物理删除，未引用工艺可删除；不设置停用属性。
- 空单价不阻止开工单，但工资结算必须明确处理，不能静默使用错误价格。

工单按整张工单、全部数量处理，不支持部分数量送检或部分数量填写结果，也不保留“加工完成”中间按钮和相关状态。

该整单规则只适用于标准和装配工单。委外加工使用独立分批 QC 规则，不进入普通送检、直接结果或返工复检入口。

工单完成时二选一：

1. 直接填写整单结果：工单结单且不能再次送检，物料留在当前车间作为在制品，可继续开下一道工艺，但不能跨到流程图下一节点。
2. 整单送检：工单进入质检中，QC 记录挂在原工单和检验批上。QC 合格后选择返回当前车间或放行下一节点；只有放行才完成宏观节点流转。不合格进入返工路径。装包节点不重复送检，整单完成后直接进入成品待入库。

同一车间内如粗 1、粗 2，粗 1 可以直接记录结果后开粗 2，也可以送检，是否送检由文员决定。

委外加工规则：

- 只能由业务部在生产计划确认或完成后创建；同一计划配件和稳定委外节点只有一张工单，任务数不可编辑。
- 供应商与加工工艺是工单自由文本快照，不引用或写入工人、工艺、单价主数据。
- 不记录供应商到货，不消耗或占用开单库存，不允许拆单。
- QC 每次直接录入一条完整结果；累计质检可超过任务数，累计合格不得超过任务数。
- 返工、报废、遗失为最终历史结果，不生成返工复检或后续供应商任务。
- 每批合格品只允许放行到流程图正式下一节点；全部任务数量合格并放行后自动结单。

### 10.4 多路节点与生产任务

- 装配、焊接多路节点的第一张工单直接消耗所需原配件或装配体；装包使用标准单路工单，整单完成后直接形成待入库成品。
- 同一逻辑物料来自多个实际库存批次时，开单必须明确分配各批次数量；库存批次是物理来源，不是 BOM 替代来源。
- 同一节点后续工艺使用该节点在制品，不能再次消耗原配件。
- 未产生结果且允许取消的工单，取消时准确恢复消耗物料；已有结果、送检、放行或后续引用时不能随意取消。
- 装配生产任务采用一直展开的目录结构：装配体为父项、输入配件为子项。
- 装配体父项不展示到货数，子配件不展示完成数和欠交数。
- 装配工艺列必须显示真实装配、焊接或装包车间，不能只显示焊接。
- 计划确认后，相关部门才能看到生产任务，不等待工单完成。
- 加工工艺列显示去掉“车间”后缀的车间名；一个物料涉及多个车间时拆成多行。
- 装配任务中同一装配体的加工工艺、任务数、完成数和备注使用合并单元格。
- 已撤销物料编号按 `-01/-02/-03` 的专用排序，不得恢复。
- 工作台左侧加工中卡片在前，完成和历史卡片在后；只有一个卡片时不能拉伸为异常高度。
- 主工作区合理使用视口，左右列表独立滚动；切换 Tab 不重置页面到页首。
- 生产情况抽屉只显示当前部门和当前工艺信息，不混入其他工艺。
- 进度条同时表达加工中黄色、完成绿色、送检紫色和异常红色；异常段位于完成段右侧，各段数量不能重复。
- 抽屉完整滑入滑出为 300ms，不使用透明渐变和变暗遮罩，不叠加多套位移动画。

### 10.5 库存、仓库与成品

库存唯一业务身份为：

> 产品 + 产品版本 + 配件/装配体 + 加工状态

加工状态记录实际工单工艺履历、最近一次 QC 去向和后续继续节点。例如同一粗光车间内先后完成“粗1”“粗2”，必须记录为两次实际工艺结果；直接完成、QC 送回、QC 放行和 QC 入仓不得被压缩成同一状态。流程图同名节点不再通过“粗光1完、粗光2完”一类坐标或出现顺序文字区分。

- `warehouse` 是仓库，查看 C01 主料仓和 C02 辅料仓的临时库存及本项目仓库操作记录；记录不伪装为外部 SQL Server 的全部流水。
- `finished` 是成品部，管理成品入库和订单发货。
- 两个部门页面与权限分开；成品部入库和发货使用两个 Tab。
- QC 录入合格结果后必须决定返回、放行或入库；非成品可选择入库，成品只放行到成品部。
- 当前节点尚未加工且未被工单占用的配件或装配体可由所在生产部门直接入库，不要求生产计划完成，也不重复 QC。
- 生产节点物料按实际完成节点状态入库，不限制为完成全部前置工艺。
- 多余成品进入成品部库存。
- QC 放行或装包工单直接完成到入库终点后生成一条待入库记录，来源分别记录 QC 批次或装包工单且严格互斥；成品部只能整批确认，待入库不增加实存。
- 成品库存只按产品和版本聚合，不保存订单、成品节点或加工状态身份，也不区分订单内与跨订单库存。
- 计划确认对成品库存建立占用但不扣减实存；计划取消时释放尚未发货的占用，客户发货才扣减实存。
- 订单结单不转移成品结余，剩余库存天然可供相同产品版本的其他订单或计划使用。
- 本项目只保存自身仓库操作记录；外部 SQL Server 可能由其他系统修改，本项目不维护其全量流水。
- 不保留旧库存结构、旧入库入口或兼容分支；开发阶段以 `zzerp.sql` 破坏性重建。

当前使用 `warehouse_stock` 临时表模拟外部 SQL Server 库存，后端库存网关隔离具体数据源；未来接入 SQL Server 时，planning、quality 和 production 不直接访问外部库存持久化。

### 10.6 部门、默认数据和业务页面

- 核心部门包括业务、工程、外协、生产、装配、QC、仓库和成品部。
- 普通账号使用稳定部门代码关联部门主数据；系统账号使用独立系统身份，不占用伪造的 `sys` 部门。部门改名不改变账号归属和权限判断。
- 权限仍以轻量文本集合存储并由身份模块统一解析，不在业务模块重复维护解析规则。
- 外协包括蚀字和电镀；默认外协单位曾确认为蚀字“新南伟”，电镀“智诚”“未来”，修改初始化数据前仍要核对当前 SQL。
- 装配部的装配和焊接车间支持多路连接；装包车间为单路节点，上游物料已通过 QC 时，装包完成后可直接到“入库”终点形成待入库成品。
- 工程部流程配置左侧列表中 BOM 配件单独组织，其余节点按部门分类，列表可独立滚动。
- QC 节点名称为“QC”并使用菱形；普通生产部门统一颜色，外协、装配和成品使用各自颜色。
- 当前开发期登录页展示全部账号，点击账号直接登录；未经新指示不恢复账号密码表单。
- 业务部进度明细表按客户切换，不提供输入搜索，字段包括厂编、产品名、订单日期、订单号、客编、订单数量、任务数、出货数量、欠货数量、交货日期和备注。
- 进度明细表产品名可点击并打开只读生产流程图，初次打开适应全部节点。

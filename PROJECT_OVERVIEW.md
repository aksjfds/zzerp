# zzerp v6 项目介绍

## 1. 项目定位

zzerp v6 是一套面向五金制造业务的模块化 ERP，覆盖从产品工程配置、客户订单、生产计划，到车间工单、质量检验、库存、成品发货和人员计价的完整业务链。

系统的核心目标不是单纯记录订单，而是把以下对象放在同一条可追踪的数据链中：

> 客户订单 → 产品版本 → BOM 与生产流程 → 生产计划 → 物料流转 → 工单与 QC → 库存与成品 → 发货与结单

当前项目处于开发阶段，数据库以 `zzerp.sql` 作为破坏性重建基线，不承担旧结构和旧数据兼容。

## 2. 技术架构

### 后端

- Python 3.12
- FastAPI
- SQLAlchemy
- PostgreSQL
- Uvicorn

后端采用模块化单体结构。业务模块拥有自己的数据和规则，通过公开 API 或部门能力门面协作；不同生产部门共享统一的生产库存、物料流转、工单和 QC 状态模型。

### 前端

- Vue 3
- TypeScript
- Vite
- Element Plus
- Vue Router

前端按业务功能拆分，每个功能目录管理自己的页面、组件和接口类型；跨功能复用内容放在 `shared`，部门入口通过部门注册表统一组织。

### 数据库与部署

- `zzerp.sql`：唯一的破坏性重建基线，统一维护完整表结构、声明式完整性约束、并发底线触发器和开发初始化数据。
- SQLAlchemy ORM：与 `zzerp.sql` 保持同一表、字段、约束和索引口径，用于应用访问，不作为第二套数据库结构来源。
- 后端领域模块负责工单类型、流程节点、送检资格、QC 路由和状态转换；数据库函数不解析流程图，也不维护完整业务状态机。
- 数据库触发器只保留并发数量、QC 结果与流水守恒、生产/库存流水及已完成 QC 历史不可变等底线，每个触发器保持单一职责。
- 初始化数据先创建部门，再创建账号、车间、工艺和默认人员；示例客户、产品、BOM、流程图、订单和示例人员集中在可删除区段。
- Docker Compose：本地容器化开发。
- Render：后端测试部署配置。
- Netlify：前端测试部署配置。
- Neon PostgreSQL：测试环境数据库。

## 3. 主要使用部门

### 业务部

负责客户订单、生产计划和订单进度明细。

- 创建、修改、确认、取消和查看客户订单；
- 订单确认后处理系统创建的草稿生产计划；
- 根据 BOM 净需求和库存参考填写普通配件计划数量；
- 查看订单、任务、出货和欠货情况；
- 在生产条件满足后手动完成生产计划。

### 工程部

负责产品、产品版本、BOM 和宏观生产流程图。

- 维护产品和客户基础关联；
- 配置产品版本；
- 配置 BOM 配件；
- 在流程图中配置车间、QC、采购、外协、装配和成品等节点；
- 维护装配关系和系统生成的稳定装配体身份；
- 判断产品是否已经配置完整并可用于创建订单。

工程部只配置宏观车间流程，不负责配置车间内具体要开的工艺工单。

### PMC

负责从计划角度查看生产执行情况，主要通过进度明细表掌握任务、完成和欠交信息。

### 生产部门

包括冲压、CNC、抛光及其他标准生产部门。各部门共享生产工作台，但只处理本部门负责的车间节点。

- 生产计划创建后查看本部门生产任务；
- 按实际需要选择工艺并开工单；
- 对整张工单填写加工结果或送检；
- 查看本部门当前物料和工单进度；
- 订单结单后处理停留在本部门的多余物料。

### 装配部

负责装配、焊接和装包等多路加工节点。

装配工单需要同时考虑多个输入配件或装配体的可用数量。生产任务以装配体为父项、所需物料为子项展示各输入物料的到货情况。

### 采购部

负责流程图中的外购物料节点、采购工单、经办人和到货处理。采购物料可以进入 QC 或按当前业务动作记录结果。

### 外协部

负责蚀字、电镀等外协工单以及对应外协单位。

### QC 部门

负责整张工单的质量检验。

QC 合格后决定物料返回当前车间继续加工，还是放行到流程图下一节点；不合格物料进入返工路径。QC 记录与原工单、检验批和物料流转保持关联。

### 仓库

部门代码为 `warehouse`，负责非成品配件和装配体的库存、生产计划出库及库存流水。

### 成品部

部门代码为 `finished`，负责成品入库和客户订单发货。入库与发货在不同页面 Tab 中处理。

## 4. 核心业务对象

### 产品与产品版本

产品保存厂编、产品名、客户等基础信息。产品版本冻结订单执行时使用的 BOM 和正式生产流程。客户订单一旦绑定产品版本，后续生产计划、工单和进度计算都应基于该版本。

### BOM

BOM 描述生产一个成品所需的普通配件及用量。业务部填写生产计划时只填写普通配件数量，装配体和成品由系统内部根据 BOM 与流程图计算和追踪。

同一逻辑配件不使用“自产/外购替代来源”模型；外购物料作为明确的独立 BOM 物料存在。

### 生产流程图

生产流程图描述物料在不同车间、采购、外协、装配、QC 和成品节点之间的宏观流转关系。

- 工程部使用可编辑流程图；
- 业务部及其他有权限部门使用只读进度流程图；
- 装配和焊接等节点支持多路输入；
- 装配体编号从 `-81` 开始由系统生成，并在产品版本内保持稳定。
- 正式流程图的节点 ID 是产品版本内的稳定业务身份，计划项、仓位、工单、QC、库存和流水均使用这一身份。
- 产品版本被订单、生产或库存历史引用后，BOM 和正式流程冻结；流程调整必须创建新版本，旧订单不会切换到新版本。
- 仅存在工艺单价配置时仍可调整节点坐标和显示内容，但被配置引用的节点不能删除、换号或改变类型、车间及 BOM 归属。

### 生产计划

客户订单确认后，系统生成草稿生产计划。业务部结合系统净需求和库存参考填写普通配件生产数量，再确认计划。

确认计划后：

- 库存被占用；
- 仓库可按计划出库；
- 计划内配件、装配体和成品分别进入生产跟踪。

草稿生产计划创建时，系统已按订单绑定版本生成路线任务投影，各生产部门立即看到本部门任务；确认计划负责占用库存并允许后续领料和执行，不负责首次发布任务可见性。

客户订单结单不会停止生产计划。只有计划内全部对象达到计划数量后，业务部才能手动完成生产计划。

### 车间工艺

车间节点内部的工艺由所属部门管理。工艺身份由产品、产品版本、物料、车间节点和工艺共同确定。

系统没有必做工艺清单，也没有固定工艺顺序。开工单时选择一个已有工艺，或输入一个新工艺；新工艺写入该车间的工艺库，单价允许暂时为空。

### 工单

一张工单只对应一个车间节点和一个工艺。同一工艺可以拆成多张工单，同一批物料也可以在同一车间连续执行多张不同工艺工单。

工单按整单处理，不支持部分数量送检或部分数量填写结果。完成时可以：

- 直接填写整单结果，物料留在当前车间，可继续开下一张工艺工单；
- 整单送检，由 QC 决定返回当前车间或放行到下一节点。

### 库存与物料流转

库存按产品、产品版本、物料节点和完成状态区分。完成状态表示物料完成到哪个流程节点。

库存系统分别记录：

- 当前库存；
- 生产计划占用；
- 仓库或成品部出库；
- 生产节点之间的物料移动；
- 订单结单后的多余物料入库；
- 成品入库、发货和流水。

## 5. 主要业务流程

### 产品投入使用

1. 工程部创建产品和版本。
2. 配置 BOM。
3. 配置并正式保存生产流程图。
4. 系统确认配置完整后，产品状态变为可用。
5. 业务部只能用可用产品版本创建订单。

### 订单到生产

1. 业务部创建并确认客户订单。
2. 系统创建草稿生产计划。
3. 业务部填写普通配件计划数量。
4. 系统校验 BOM 可满足的成品数量。
5. 草稿计划创建时发布生产任务可见性；业务部确认计划后系统占用库存并允许领料和执行。
6. 仓库按生产计划出库，或生产部门从初始物料开始生产。

### 工单到节点流转

1. 部门在当前车间节点选择物料和工艺开工单。
2. 工单按整单填写结果或整单送检。
3. 直接填写结果时，物料留在当前车间。
4. 送检时，QC 记录检验结果。
5. QC 合格后选择返回当前车间或放行下一节点。
6. 多路节点收齐所需输入后生成装配体或后续物料。
7. 最终成品进入成品部。

### 发货与结余

1. 成品部按客户订单发货。
2. 发货达到订单数量后，客户订单自动结单。
3. 生产计划仍可继续执行。
4. 订单结单后，停留在生产部门或 QC 的多余配件、装配体可以按完成节点存入仓库。
5. 多余成品进入成品部库存。
6. 计划内全部对象达到计划数量后，由业务部手动完成生产计划。

## 6. 项目目录

```text
zzerp/
├── AGENTS.md                 AI 与 Agent 协作规则、最终业务约束
├── MODIFICATION.md           大型整改方案、阶段状态和验证记录
├── PROJECT_OVERVIEW.md       项目定位、架构和业务介绍
├── README.md                 项目入口、开发与部署提示
├── main.py                   根级后端启动入口
├── zzerp.sql                 PostgreSQL 完整重建基线
├── package.json              根级开发脚本
├── backend/
│   ├── requirements.txt      后端运行依赖
│   ├── src/
│       ├── main.py           FastAPI 应用和路由装配
│       ├── auth_dependencies.py  登录身份依赖
│       ├── authorization.py  权限检查
│       ├── config.py         环境配置
│       ├── database.py       数据库会话基础设施
│       ├── departments/      部门门面与部门能力组合
│       ├── domain/           跨模块领域类型、错误和稳定常量
│       ├── modules/          按业务域划分的核心模块
│       ├── routers/          HTTP 路由入口
│       └── schemas/          请求与响应结构
├── frontend/
│   ├── package.json          前端脚本与依赖
│   └── src/
│       ├── api/              通用接口基础设施
│       ├── features/         按业务功能拆分的页面和组件
│       ├── permission/       权限常量与路由守卫
│       ├── router/           前端路由
│       ├── shared/           跨功能共享组件和工具
│       ├── stores/           全局状态
│       ├── styles/           全局样式
│       ├── types/            跨功能类型
│       └── views/            通用页面入口
└── scripts/
    ├── check_backend.py      应用导入、架构边界及 SQL/ORM 静态一致性检查
    ├── reset_database.py     开发数据库破坏性重建
```

## 7. 后端模块分工

`backend/src/modules` 中的主要业务模块包括：

- `identity`：账号、会话与身份认证；
- `organization`：部门、车间和工艺基础数据；
- `engineering`：产品、版本、BOM 和生产流程；
- `sales`：客户订单和业务进度；
- `planning`：生产计划、需求展开、PMC 和跨域进度读模型；
- `production_core`：生产物料、工单、节点流转和共享生产规则；
- `standard_execution`：标准生产部门执行能力；
- `assembly`：装配和多路物料执行；
- `purchasing`：采购工单和到货；
- `quality`：检验批、QC 操作、返回和放行；
- `inventory`：库存、占用、出入库、成品库存和流水；
- `workforce`：工人、计价和工资相关数据。

模块自己的 ORM、查询和事务实现位于对应模块目录。旧顶层 `models`、`repositories`、`services` 源码层已经删除，不属于当前架构。模块应通过公开接口协作，不应直接读取其他模块内部表结构来复制业务判断。

## 8. 前端功能分工

`frontend/src/features` 中的主要功能包括：

- `process-designer`：工程部产品、BOM 和流程图编辑；
- `customer-orders`：业务部订单、生产计划和进度明细；
- `customers`：客户查询和客户公共类型；
- `production`：生产任务、工作台、工单和生产情况；
- `inventory`：库存和库存流水；
- `departments`：部门页面注册和部门专属入口；
- `workers`：工人和工资相关页面；
- `admin`：管理与 PMC 聚合页面。

工程部流程图编辑器与其他部门的只读进度流程图具有不同职责。共享代码只能复用纯展示或纯计算能力，不能让只读交互修改编辑器行为。

跨 feature 调用必须经过目标 feature 的 `index.ts` 公共入口。`router` 和部门注册表负责具体页面装配；`shared` 只保存跨功能公共能力，不能依赖业务 feature。

`scripts/check_backend.py` 以只读方式核对应用能否导入、ORM 表所有权、跨模块 persistence、descriptor 依赖与公开 API、局部导入和模块循环，并比对 `zzerp.sql` 与 ORM 的表、字段、具名约束和索引名称；同时检查前端 `shared -> feature` 反向依赖和 feature 内部越界导入。该检查不连接数据库，也不替代数据库重建。

## 9. 文档边界

- 项目是什么、有哪些模块、业务如何流转：阅读本文件。
- AI/Agent 能做什么、修改时必须遵守什么：阅读 [`AGENTS.md`](AGENTS.md)。
- 大型整改为什么做、执行到哪一阶段：阅读 [`MODIFICATION.md`](MODIFICATION.md)。
- 开发入口和环境提示：阅读 [`README.md`](README.md)。
- 数据库真实结构：以 [`zzerp.sql`](zzerp.sql) 和当前后端模型共同核对，不能只依赖说明文档。

## 10. 已确认的详细业务与界面规则

本节记录当前项目已经确认的详细业务规则和业务界面规则。

### 10.1 产品、BOM、版本与流程图

- 工程部产品列表只标注“可用/不可用”。业务部不能选择不可用产品创建订单，页面直接显示“不可用”，不暴露工程内部缺项原因。
- 客户订单绑定明确的产品版本；生产计划、BOM 展开、流程图、工单和进度始终使用该版本，不能自动切换到产品最新版本。
- 同一客户订单禁止重复选择相同产品版本。
- 同一逻辑配件不支持自产/外购双来源；外购时在 BOM 中使用明确物料，例如“脚钉（外购）”。
- 装配体编号按正式流程图装配顺序从 `-81` 开始生成，坐标顺序为从左到右、从上到下。
- 装配体名称按参与物料顺序组合，并保持稳定身份，不能因重新渲染或临时改名生成新身份。
- 工程部负责产品版本、BOM 和宏观流程图。流程节点表示车间，节点名就是车间名；工程部不配置车间内具体工艺。
- 同一车间节点内部可以连续开多张工艺工单，不需要为每道工艺创建流程节点。
- 装配、焊接等多路节点允许多个上游输入。
- 流程图编辑器不再限制“跨部门必须经过 QC”或“发货上游必须是 QC”；实际物料跨节点仍由 QC 放行控制。

工程部流程图编辑器与其他部门的只读进度流程图分开维护。业务部版本是其他有权限部门只读流程图的基准，修改只读版本时同步其他部门，但不得改变工程部编辑器。

流程图当前交互规则：

- 禁用鼠标滚轮平移；工程部使用滚轮键拖动画布、左键框选节点。
- 节点拖动时自由移动，松开后吸附网格；当前网格为 X 轴 500px、Y 轴 250px，修改前仍需核对真实代码。
- “适应”只调整视口以显示全部节点，不重新布局。
- 节点大小、字体大小和连线粗细调整需要持久化；箭头大小不随线宽变化。
- 节点文字不可拖选、不可溢出，不显示输入光标。
- 只读节点详情使用 Popover，不常驻展示“入/出”。
- 连线默认不穿过节点，优先减少拐弯；需要两次拐弯时在较长轴距离的中点附近转折，不能贴近起点或终点。
- 节点移动后按同一规则重新计算连线。
- 自动连接按节点真实空间顺序逐个连接，不能跳过 BOM 起点或错误直连首尾节点。

### 10.2 客户订单与生产计划

- 业务部“客户订单”和“生产计划”为两个独立 Tab。
- 创建或保存订单成功后离开编辑界面。
- 确认客户订单后，系统创建草稿生产计划。
- 已确认但尚未创建或启动计划的订单取消后仍可查看和编辑；所有取消订单保留查看入口。
- 发货达到订单需求数量后客户订单自动结单。
- 订单结单不停止、也不自动完成生产计划，计划可以继续生产。
- 计划内全部配件、装配体和成品达到计划数量后，业务部手动完成生产计划。

生产计划填写规则：

- 业务部只填写 BOM 普通配件计划数量，装配体和成品不作为可填写明细。
- 系统内部可以保留装配体和成品计划项用于计算与执行。
- 普通配件计划数量必须大于等于系统净需求。
- 系统按 BOM 用量校验配件可满足的成品数量不低于订单需求。
- 库存仅供填写时参考，按产品分组展示配件、装配体和成品。
- 每个产品只提供一个库存换算入口，用 Popover 展示装配体和成品折算后的数量，不为每条库存项单独放按钮。
- 计划确认时占用可用库存；取消尚未出库计划时解除占用，已从仓库或成品部出库的计划暂时禁止取消。
- 仓库人员选择生产计划出库，出库物料进入对应生产流程。

数量口径：

- “任务数”只表示生产计划数量，不使用客户订单数量回退。
- 业务部进度明细表任务数读取生产计划内部成品项的计划生产数量。
- 生产部门删除订单数量相关字段和值，只使用生产计划任务数。
- 生产任务表展示 `任务数量 / 到货数量`、完成数量和欠交数量；欠交数量 = 任务数量 - 完成数量。
- 加工完成不能清除已经发生的到货数；完成数使用清晰的亮绿色。

### 10.3 车间工艺、工单与 QC

工艺业务身份为：

> 产品 + 产品版本 + 配件/装配体 + 流程节点（车间） + 工艺

- 工艺与单价由节点所属部门管理。
- 不存在必做工艺或固定工艺顺序，文员按实际情况开单。
- 一张工单只选择一个工艺；同一工艺可以拆成多张工单。
- 可以选择已有工艺或输入新工艺。新工艺与新工单在同一事务写入，新工艺进入车间工艺库，单价允许为空。
- 新工艺不修改任何产品版本必做清单，因为该概念不存在。
- 工艺允许修改；修改已完成工单涉及的工艺时必须保持历史工资明细一致。
- 已被工单引用的工艺禁止物理删除，未引用工艺可删除；不设置停用属性。
- 空单价不阻止开工单，但工资结算必须明确处理，不能静默使用错误价格。

工单按整张工单、全部数量处理，不支持部分数量送检或部分数量填写结果，也不保留“加工完成”中间按钮和相关状态。

工单完成时二选一：

1. 直接填写整单结果：工单结单且不能再次送检，物料留在当前车间作为在制品，可继续开下一道工艺，但不能跨到流程图下一节点。
2. 整单送检：工单进入质检中，QC 记录挂在原工单和检验批上。QC 合格后选择返回当前车间或放行下一节点；只有放行才完成宏观节点流转。不合格进入返工路径。

同一车间内如粗 1、粗 2，粗 1 可以直接记录结果后开粗 2，也可以送检，是否送检由文员决定。

### 10.4 多路节点与生产任务

- 装配、焊接等多路节点的第一张工单直接消耗所需原配件或装配体。
- 同一逻辑物料来自多个实际库存批次时，开单必须明确分配各批次数量；库存批次是物理来源，不是 BOM 替代来源。
- 同一节点后续工艺使用该节点在制品，不能再次消耗原配件。
- 未产生结果且允许取消的工单，取消时准确恢复消耗物料；已有结果、送检、放行或后续引用时不能随意取消。
- 装配生产任务采用一直展开的目录结构：装配体为父项、输入配件为子项。
- 装配体父项不展示到货数，子配件不展示完成数和欠交数。
- 装配工艺列必须显示真实装配、焊接或装包车间，不能只显示焊接。
- 计划创建后，相关部门立即看到生产任务，不等待工单完成。
- 加工工艺列显示去掉“车间”后缀的车间名；一个物料涉及多个车间时拆成多行。
- 装配任务中同一装配体的加工工艺、任务数、完成数和备注使用合并单元格。
- 已撤销物料编号按 `-01/-02/-03` 的专用排序，不得恢复。
- 工作台左侧加工中卡片在前，完成和历史卡片在后；只有一个卡片时不能拉伸为异常高度。
- 主工作区合理使用视口，左右列表独立滚动；切换 Tab 不重置页面到页首。
- 生产情况抽屉只显示当前部门和当前工艺信息，不混入其他工艺。
- 进度条同时表达加工中黄色、完成绿色、送检紫色和异常红色；异常段位于完成段右侧，各段数量不能重复。
- 抽屉完整滑入滑出为 300ms，不使用透明渐变和变暗遮罩，不叠加多套位移动画。

### 10.5 库存、仓库、成品与结余

库存唯一业务身份为：

> 产品 + 产品版本 + 配件/装配体/成品节点 + 完成状态

完成状态表示已完成的流程节点，例如粗光完、焊接完、装配完。物料位于节点 A 时，A 完与上一节点 B 完必须分开记录。

- `warehouse` 是仓库，管理非成品配件和装配体库存、计划出库与流水。
- `finished` 是成品部，管理成品入库和订单发货。
- 两个部门页面与权限分开；成品部入库和发货使用两个 Tab。
- 订单结单后，多余配件或装配体由当前生产部门或 QC 在本部门库存 Tab 存入仓库。
- 多余物料可按任意已完成节点状态入库，不限制为完成全部前置工艺。
- 多余成品进入成品部库存。
- 结余确认后自动入库，不再存在“历史结余待入库”或再次手工入库操作。
- 库存流水记录真实入库、占用、出库、发货和结余，不能从当前库存反推。
- 不通过旧数据兼容恢复已删除结余；开发阶段通过更新 `zzerp.sql` 或用户明确执行的数据命令处理。

此前讨论过用临时表模拟外部 SQL Server 仓库，但用户随后明确仓库相关改造暂缓。未经再次确认，不按该讨论重构库存模块。

### 10.6 部门、默认数据和业务页面

- 核心部门包括业务、工程、PMC、采购、外协、生产、装配、QC、仓库和成品部。
- 普通账号使用稳定部门代码关联部门主数据；系统账号使用独立系统身份，不占用伪造的 `sys` 部门。部门改名不改变账号归属和权限判断。
- 权限仍以轻量文本集合存储并由身份模块统一解析，不在业务模块重复维护解析规则。
- 外协包括蚀字和电镀；默认外协单位曾确认为蚀字“新南伟”，电镀“智诚”“未来”，修改初始化数据前仍要核对当前 SQL。
- 采购部经办人示例为“赵哥”。
- 装配部包含焊接能力，焊接和装配均支持多路连接。
- 工程部流程配置左侧列表中 BOM 配件单独组织，其余节点按部门分类，列表可独立滚动。
- QC 节点名称为“QC”并使用菱形；普通生产部门统一颜色，外协、采购、装配和成品使用各自颜色。
- 当前开发期登录页展示全部账号，点击账号直接登录；未经新指示不恢复账号密码表单。
- 业务部进度明细表按客户切换，不提供输入搜索，字段包括厂编、产品名、订单日期、订单号、客编、订单数量、任务数、出货数量、欠货数量、交货日期和备注。
- 进度明细表产品名可点击并打开只读生产流程图，初次打开适应全部节点。
- PMC 删除“配件生产进度”和“客户订单生产情况”旧 Tab，使用“进度明细表”Tab。

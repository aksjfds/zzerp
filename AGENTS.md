# AGENTS.md

# 项目上下文（Project Context）

## 项目简介

项目名称：zzerp v6

这是一个面向五金制造业务的 Web ERP 项目，采用前后端分离的模块化单体架构。

项目目标：

-   管理客户、产品、产品版本与 BOM。
-   配置产品的宏观生产流程图和稳定装配体身份。
-   将客户订单转换为生产计划，并按产品版本展开物料需求。
-   管理车间工艺、生产工单、采购、外协、装配、焊接和 QC 流转。
-   跟踪生产任务、物料到货、加工完成、异常、欠交和订单进度。
-   管理配件、装配体与成品的库存、占用、出入库、发货和库存流水。
-   管理部门、账号、工人、工艺单价及工资相关数据。

## 技术栈

主要技术：

-   编程语言：Python 3.12、TypeScript
-   后端框架：FastAPI、SQLAlchemy、Uvicorn
-   前端框架：Vue 3、Vite、Element Plus、Vue Router
-   数据库：PostgreSQL
-   缓存：当前未使用独立缓存服务
-   消息队列：当前未使用消息队列
-   云服务：Render、Netlify、Neon PostgreSQL（测试部署）
-   部署方式：Docker Compose 本地开发；Render 后端与 Netlify 前端测试部署

## 核心业务规则

Agent 修改代码前必须理解：

1.  客户订单绑定明确的产品版本；该订单的生产计划、BOM 展开、流程图、生产任务和工单必须始终使用绑定版本，不能自动切换到产品最新版本。
2.  工程部负责产品版本、BOM 和宏观生产流程图。流程节点表示车间，节点名就是车间名；车间内具体工艺由节点所属部门管理。
3.  客户订单确认后系统创建草稿生产计划。业务部只填写普通配件的计划数量，系统按 BOM 用量验证这些配件可支持的成品数量不低于订单需求。
4.  “任务数”表示生产计划数量，不是客户订单数量。生产部门只使用生产计划任务数口径。
5.  不存在必做工艺或固定工艺顺序。一张工单只对应一个车间节点和一个工艺，同一工艺可以拆成多张工单。
6.  工单按整张工单、全部数量处理，不支持部分送检或部分录入结果。直接填写结果后物料留在当前车间；送检后由 QC 决定返回当前车间或放行到下一流程节点。
7.  装配、焊接等多路节点的第一张工单消耗所需输入物料；同一节点的后续工艺使用该节点在制品，不能重复消耗原配件。
8.  库存按“产品 + 产品版本 + 配件/装配体/成品节点 + 完成状态”区分。完成状态表示已完成的流程节点。
9.  客户订单发货达到订单数量后自动结单，但生产计划不会自动停止或完成；计划达到完成条件后由业务部手动完成。
10.  `zzerp.sql` 是开发阶段数据库的唯一破坏性重建基线。项目不维护旧表、旧字段或旧数据兼容逻辑。
11.  正式流程图的节点 ID 是产品版本内的稳定业务身份。版本被订单、计划、生产、库存或流水引用后，不允许原地修改 BOM 或正式流程；需要调整时必须创建新版本。仅被工艺单价引用的节点允许调整显示内容和坐标，但不能删除、换号或改变节点类型、车间及 BOM 归属。
12.  普通账号必须引用 `department.department_code`；系统账号通过明确的 `is_system` 身份存在，不伪造 `sys` 部门。部门代码是稳定业务代码，修改部门显示名称不得影响账号权限。
13.  当前权限集合仍以逗号分隔文本持久化，但只能由 `identity` 模块统一解析；未经明确需求不扩展为复杂权限系统，也不得在其他模块重复拆分权限字符串。

更完整的项目介绍、部门职责和业务流程见 `PROJECT_OVERVIEW.md`。

禁止：

-   修改核心业务逻辑但未确认影响范围
-   删除已有业务流程
-   恢复已经删除的必做工艺、替代来源或旧数据兼容逻辑
-   让前端、后端和数据库分别维护同一业务概念的不同口径

------------------------------------------------------------------------

# 项目结构说明（Project Structure）

## 项目目录

    /
    ├── AGENTS.md
    ├── MODIFICATION.md
    ├── PROJECT_OVERVIEW.md
    ├── README.md
    ├── main.py
    ├── zzerp.sql
    ├── package.json
    ├── backend/
    │   ├── requirements.txt
    │   ├── src/
    │       ├── main.py
    │       ├── auth_dependencies.py
    │       ├── authorization.py
    │       ├── config.py
    │       ├── database.py
    │       ├── departments/
    │       ├── domain/
    │       ├── modules/
    │       ├── routers/
    │       └── schemas/
    ├── frontend/
    │   ├── package.json
    │   └── src/
    │       ├── api/
    │       ├── features/
    │       ├── permission/
    │       ├── router/
    │       ├── shared/
    │       ├── stores/
    │       ├── styles/
    │       ├── types/
    │       └── views/
    ├── scripts/
    ├── Dockerfile
    ├── docker-compose.yaml
    ├── render.yaml
    └── netlify.toml

## 目录职责

### 根目录

负责：

-   `main.py`：加载 `backend/src/main.py` 的后端启动入口。
-   `zzerp.sql`：PostgreSQL 完整重建基线，包含表、约束、触发器、索引和初始化数据。
-   `package.json`：前后端开发、检查和数据库辅助脚本入口。
-   `MODIFICATION.md`：大型整改方案、阶段状态和验证记录。
-   `PROJECT_OVERVIEW.md`：项目定位、部门职责、业务流程和详细业务规则。
-   `README.md`：开发环境、验证和部署入口说明。

禁止：

-   在根级启动文件中堆积业务逻辑
-   新增与现有入口重复的脚本或配置

### backend/src/routers/

负责：

-   定义 FastAPI HTTP 路由。
-   接收请求并完成接口层参数衔接。
-   调用对应业务模块的公开 API。
-   返回符合 schemas 定义的响应。

禁止：

-   编写核心业务规则。
-   直接复制模块内部查询和状态判断。

### backend/src/modules/

负责：

-   按业务域实现核心规则和应用流程。
-   管理工程、销售、计划、生产、采购、装配、QC、库存、成品、组织和人员等模块。
-   通过公开 API 与其他模块协作。

主要模块：

-   `identity`：账号、会话与身份认证。
-   `organization`：部门、车间与工艺基础数据。
-   `engineering`：产品、版本、BOM 和生产流程图。
-   `sales`：客户订单和业务进度。
-   `planning`：生产计划、需求展开、PMC 和跨域进度读模型。
-   `production_core`：共享生产物料、工单、节点流转和状态规则。
-   `standard_execution`：标准生产部门执行。
-   `assembly`：装配、焊接和多路物料执行。
-   `purchasing`：采购工单与到货。
-   `quality`：检验批、QC 结果、返回与放行。
-   `inventory`：库存、占用、出入库、成品库存与流水。
-   `workforce`：工人、工艺单价和工资数据。

禁止：

-   直接依赖其他业务模块的内部实现。
-   在多个模块中重复维护同一状态机或数量算法。

### backend/src/departments/

负责：

-   组合各部门拥有的业务能力。
-   为冲压、CNC、抛光、外协、采购、装配、QC、仓库和成品部提供部门门面。
-   复用统一生产核心，同时隔离部门权限和入口。

顶层 `services/`、`repositories/` 和 `models/` 源码层已经删除，不是当前架构。业务实现、查询和 ORM 定义均归属各自 `modules/<name>/`；不得重新建立这些旧顶层源码层。

### backend/src/schemas/

负责：

-   定义后端请求与响应数据结构。
-   约束接口字段、类型和返回口径。

### backend/src/domain/

负责：

-   保存跨模块稳定领域类型、错误和常量。
-   为多个模块提供不依赖具体实现的共同语言。

禁止：

-   放入单一模块私有业务逻辑。

### frontend/src/features/

负责：

-   按业务功能组织页面、组件、接口和状态。
-   管理工程部、业务部、生产、库存、成品、部门和人员等功能。

主要功能：

-   `process-designer`：产品、BOM 和工程部流程图编辑器。
-   `customer-orders`：客户订单、生产计划和业务进度。
-   `customers`：客户查询及客户公共类型。
-   `production`：生产任务、工作台、工单和生产情况。
-   `inventory`：库存与流水。
-   `departments`：部门页面注册与部门入口。
-   `workers`：工人、工艺单价和工资页面。
-   `admin`：管理与 PMC 聚合页面。

禁止：

-   跨 feature 直接修改对方内部状态。
-   把后端业务规则复制成独立的前端判断体系。

跨 feature 协作必须从目标 feature 的 `index.ts` 公共入口导入。路由和部门注册表是明确的组合根，可以装配具体页面；`shared` 不能依赖任何业务 feature。

### frontend/src/shared/

负责：

-   跨功能共享的纯展示组件、表格能力、物料组件和只读流程图能力。
-   不包含具体部门独有的业务流程。

工程部流程图编辑器与其他部门只读流程图职责不同。只能复用纯计算或纯展示能力，不能让共享修改改变工程部编辑行为。

### frontend/src/api/、types/、stores/

负责：

-   `api`：通用请求基础设施。
-   `types`：跨功能共享类型。
-   `stores`：全局状态。

功能专属接口、类型和状态应优先放在对应 feature 内，避免全局目录持续膨胀。

### frontend/src/permission/、router/

负责：

-   权限常量、路由守卫和页面访问控制。
-   前端路由注册和部门页面入口。

### scripts/

负责：

-   `check_backend.py`：只读检查应用导入、模块依赖与所有权、SQL/ORM 名称一致性和前端 feature 边界。
-   `reset_database.py`：开发数据库破坏性重建。

未经用户当前请求明确授权，Agent 不得执行这些脚本。

# 编码规范（Code Style）

## 通用原则

代码必须：

-   简洁
-   可维护
-   易理解

优先：

-   小范围修改
-   复用已有代码
-   遵循已有项目模式

避免：

-   过度设计
-   无必要抽象
-   大规模重构

## 命名规范

变量、函数、类名称必须：

-   表达实际业务含义
-   避免无意义缩写
-   保持一致性

## 函数规范

函数应该：

-   单一职责
-   保持较短
-   降低嵌套复杂度

禁止：

-   一个函数处理多个业务领域
-   隐藏重要副作用

## 注释规范

只添加必要注释：

允许：

-   复杂算法说明
-   特殊业务规则说明
-   非直观实现原因

禁止：

-   重复解释代码
-   添加无意义注释

------------------------------------------------------------------------

# 架构约束（Architecture Constraints）

## 架构设计原则

必须遵循：

-   低耦合
-   高内聚
-   清晰职责
-   明确边界

设计要求：

-   模块之间必须通过明确接口进行通信
-   禁止直接依赖其他模块内部实现
-   修改单个模块时，应尽量减少对其他模块的影响
-   优先通过抽象接口降低模块之间的依赖
-   新增功能时必须考虑长期维护成本，避免形成技术债

## 后端依赖方向

必须遵循：

HTTP Router → `modules/<name>/api.py` → 模块内部实现 / `persistence.py` → Database

部门入口通过 `departments/<name>/api.py` 组合能力。跨模块协作通过双方 descriptor 声明的 `api.py` 或窄接口 `*_api.py` 完成；表所有权以 `modules/ownership.py` 为准。

## 数据库职责边界

数据库负责声明式关系约束、必须依赖行锁的并发数量底线、历史不可变和账实守恒；后端领域模块负责权限、业务路径、工单与 QC 状态转换、流程图解析和面向用户的错误信息。

必须：

-   优先使用外键、组合外键、唯一约束和简单检查约束。
-   生产流水、库存流水和已完成 QC 历史保持数据库级不可变保护。
-   触发器保持单一职责，并具备对应查询索引。

禁止：

-   在数据库函数中解析生产流程图或选择业务节点。
-   在数据库触发器和后端重复维护完整业务状态机。
-   为降低耦合删除必要的并发、数量守恒或历史保护底线。

## Router 层

负责：

-   请求处理
-   参数验证
-   响应格式处理

禁止：

-   编写核心业务逻辑
-   直接操作数据库

## 业务模块

负责：

-   模块拥有的核心规则、事务操作和查询。
-   模块自己的 ORM 定义与数据访问。
-   对 Router 暴露 `api.py`，对已声明协作者暴露必要的窄接口。

禁止：

-   直接导入其他模块的 `persistence.py`。
-   构造或修改其他模块拥有的 ORM 记录。
-   通过新增顶层 Service、Repository 或 Model 绕过模块所有权。

## 模块依赖规则

必须：

-   遵循已有模块依赖方向
-   保持职责边界清晰
-   跨 feature 前端调用目标 feature 的公共入口

禁止：

-   循环依赖
-   跨模块访问内部实现
-   共享前端代码反向依赖业务 feature

------------------------------------------------------------------------

# 修改规则（Change Rules）

## 大型整改流程

涉及多个业务模块、多个技术层级、数据库结构、公共接口或需要分阶段完成的修改，均视为大型整改。

大型整改必须遵循：

1.  修改代码前，先阅读当前真实代码并分析实际问题。
2.  在根目录 `MODIFICATION.md` 中写明整改背景、目标、非目标、业务基线、实施阶段、执行顺序、影响范围、风险和代码审查方式。
3.  完成方案记录后，才能按照文档中的顺序开始修改。
4.  实施过程中同步更新每个阶段的状态、重要发现、实际修改和未完成事项。
5.  如果实际情况要求改变方案、顺序或影响范围，必须先更新 `MODIFICATION.md`；涉及业务范围扩大或重要业务选择时，必须先向用户确认。
6.  整改完成后，在 `MODIFICATION.md` 记录最终结果，以及代码规范、模块耦合和稳定性审查结论。

禁止：

-   未记录方案就直接开始大型整改
-   实际修改脱离 `MODIFICATION.md` 中的已记录方案
-   为追求进度跳过影响分析、风险说明或代码审查记录
-   把普通小型修复强行扩展成未获授权的大型整改

## 方案执行完成约束

用户要求“执行方案”时，目标是满足方案的全部目标和完成条件，而不是仅执行方案中已经列出的修改动作。

必须遵循：

1.  检查或执行过程中发现属于本轮目标的问题，必须继续解决；不得仅记录为遗留问题后宣布完成。
2.  “问题已记录”“留待下一轮”“建议后续整改”均不等于问题已经解决。
3.  如果原方案遗漏了实现已确认目标所必需的工作，必须先更新 `MODIFICATION.md`，然后继续执行；不得擅自缩小目标或完成标准。
4.  只有在方案执行前已经明确列入非目标的问题，才能在完成后作为已知问题保留。
5.  未经用户确认，不得把本轮目标范围内的问题转移到“下一轮整改”。
6.  文档阶段状态、实际代码状态和最终回复必须保持一致。

阶段状态只允许使用：

-   `待执行`：尚未开始。
-   `进行中`：已经开始，但实施范围或完成条件尚未全部满足。
-   `已完成`：实施范围和完成条件已经全部满足，范围内不存在未解决问题。
-   `受阻`：缺少用户决定、权限或外部条件，无法继续执行。

存在任何本轮范围内未解决问题时：

-   当前阶段只能标记为 `进行中` 或 `受阻`。
-   整体方案不得标记为 `已完成`。
-   最终回复不得声称阶段或任务已经完成。
-   不得用“已记录”代替修复，也不得用新的整改轮次规避当前完成条件。

每个阶段标记为 `已完成` 前，必须逐项核对并记录：

1.  阶段实施范围是否全部落实。
2.  阶段完成条件是否全部满足。
3.  整体整改目标是否仍然满足。
4.  是否符合本文件的代码规范、低耦合和高稳定完成条件。
5.  每项完成条件是否存在对应的实际代码或只读审查证据。

任何一项缺少证据，或者审查结论仍包含范围内的未解决问题，阶段均不得标记为 `已完成`。

## Agent 操作权限限制

Agent 允许对项目目录、文件和文件内容执行操作，并允许执行不会修改项目、系统或外部状态的只读检查命令。

允许：

-   添加文件或目录
-   删除文件或目录
-   修改文件内容
-   读取文件内容
-   查看项目目录和文件清单
-   搜索文件内容、符号、引用和依赖关系
-   查看指定文件、文件片段和文件元数据
-   查看 Git 状态、差异、日志和提交信息
-   执行不写入文件、不连接外部服务的静态检查与项目审查命令

禁止：

-   执行会修改项目文件、系统环境或外部状态的命令
-   未经明确授权运行项目脚本
-   主动执行构建或格式化命令；只有用户当前请求明确要求时才可执行
-   启动、停止或重启服务
-   安装、升级或删除依赖
-   连接、查询、修改或重建数据库
-   执行网络请求或操作外部服务
-   执行 Git 提交、切换、合并、变基、重置、清理或其他 Git 写操作
-   操作项目目录以外的系统资源

只读检查命令必须满足：

-   仅用于理解、检查或审查当前项目
-   不产生缓存、构建产物、临时项目文件或其他持久化副作用
-   不通过脚本间接执行被禁止的操作
-   无法确认命令是否有副作用时，不得执行，必须先向用户确认

## 修改前要求

Agent 必须：

1.  阅读相关代码。
2.  理解现有实现。
3.  遵循已有项目规范。

禁止：

-   未阅读代码直接修改
-   根据文件名称猜测功能
-   创建重复实现

## 修改原则

默认原则：

只进行完成任务所需的必要修改。

必须：

-   优先复用已有代码
-   保持修改范围可控
-   遵循现有架构约束
-   保持代码规范一致

禁止：

-   为解决单个报错添加临时代码
-   添加无实际业务价值的代码
-   通过硬编码绕过问题
-   堆积垃圾代码或技术债
-   顺便进行无关重构
-   修改无关文件

## 代码质量要求

新增或修改代码必须：

-   具备明确职责
-   保持可维护性
-   符合项目编码规范
-   避免增加不必要复杂度

------------------------------------------------------------------------

# Debug / Bug Fix 流程

## 复现问题

必须确认：

-   当前错误行为
-   预期行为
-   触发条件

## 定位根因

检查：

-   代码执行路径
-   数据流
-   最近修改
-   相关依赖

目标：

解决根本原因，而不是隐藏错误。

## 实施修复

要求：

-   修改范围最小化
-   遵循现有架构
-   不引入临时代码
-   不通过绕过逻辑解决问题

禁止：

-   为消除报错添加无意义代码
-   添加无法解释用途的逻辑
-   通过硬编码掩盖问题

------------------------------------------------------------------------

# Code Review 标准

代码修改的完成判断只依据代码规范、低耦合和高稳定审查。

## 代码规范

检查：

-   是否符合项目编码规范
-   是否容易理解和维护
-   命名是否准确表达业务含义
-   函数和组件是否保持单一职责
-   是否存在重复或无价值代码

拒绝：

-   临时解决方案
-   垃圾代码
-   无必要复杂设计
-   难以维护实现

## 低耦合

确认：

-   模块职责和所有权是否清晰
-   是否遵循既定依赖方向和公共接口
-   是否存在循环依赖、跨模块内部访问或重复状态机
-   单个模块修改是否尽量减少对其他模块的影响

## 高稳定

确认：

-   修改是否基于当前真实代码和已确认业务规则
-   是否保持现有业务行为、接口口径和数据不变量
-   异常和边界情况是否有明确处理，不静默回退
-   是否避免隐藏副作用、临时代码、硬编码和旧数据兼容分支
-   是否存在明显的导入错误、类型冲突、约束冲突或资源生命周期问题

------------------------------------------------------------------------

# 完成条件（Definition of Done）

任务完成只需同时满足以下三类代码审查标准。

## 代码规范

-   [ ] 符合项目编码规范
-   [ ] 修改范围合理
-   [ ] 没有无关改动
-   [ ] 没有重复或无价值代码

## 低耦合

-   [ ] 遵循架构约束
-   [ ] 保持模块职责清晰
-   [ ] 只通过明确公共接口协作
-   [ ] 没有新增循环依赖或跨模块内部访问

## 高稳定

-   [ ] 保持已确认业务逻辑和数据口径
-   [ ] 异常、边界和副作用处理明确
-   [ ] 没有明显的导入、类型、约束或生命周期冲突
-   [ ] 没有临时绕过、静默回退或旧数据兼容逻辑

## 最终报告

完成任务后必须说明：

1.  修改了什么
2.  为什么修改
3.  代码规范、低耦合和高稳定审查结论

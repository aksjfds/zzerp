# AGENTS.md

# 项目上下文（Project Context）

## 项目简介

项目名称：zzerp v6

这是一个面向五金制造业务的 Web ERP 项目，采用前后端分离的模块化单体架构。

项目目标：

-   管理客户、产品、产品版本与 BOM。
-   配置产品的宏观生产流程图和稳定装配体身份。
-   将客户订单转换为生产计划，并按产品版本展开物料需求。
-   管理车间工艺、生产工单、采购、外协、装配、焊接和 QC 流转。
-   跟踪生产任务、物料到货、加工完成、异常、欠交和订单进度。
-   管理配件、装配体与成品的库存、占用、出入库、发货和库存流水。
-   管理部门、账号、工人、工艺单价及工资相关数据。

## 技术栈

主要技术：

-   编程语言：Python 3.12、TypeScript
-   后端框架：FastAPI、SQLAlchemy、Uvicorn
-   前端框架：Vue 3、Vite、Element Plus、Vue Router
-   数据库：PostgreSQL
-   缓存：当前未使用独立缓存服务
-   消息队列：当前未使用消息队列
-   云服务：Render、Netlify、Neon PostgreSQL（测试部署）
-   部署方式：Docker Compose 本地开发；Render 后端与 Netlify 前端测试部署

## 核心业务规则

Agent 修改代码前必须理解：

1.  客户订单绑定明确的产品版本；该订单的生产计划、BOM 展开、流程图、生产任务和工单必须始终使用绑定版本，不能自动切换到产品最新版本。
2.  工程部负责产品版本、BOM 和宏观生产流程图。流程节点表示车间，节点名就是车间名；车间内具体工艺由节点所属部门管理。
3.  客户订单确认后系统创建草稿生产计划。业务部只填写普通配件的计划数量，系统按 BOM 用量验证这些配件可支持的成品数量不低于订单需求。
4.  “任务数”表示生产计划数量，不是客户订单数量。生产部门只使用生产计划任务数口径。
5.  不存在必做工艺或固定工艺顺序。一张工单只对应一个车间节点和一个工艺，同一工艺可以拆成多张工单。
6.  工单按整张工单、全部数量处理，不支持部分送检或部分录入结果。直接填写结果后物料留在当前车间；送检后由 QC 决定返回当前车间或放行到下一流程节点。
7.  装配、焊接等多路节点的第一张工单消耗所需输入物料；同一节点的后续工艺使用该节点在制品，不能重复消耗原配件。
8.  库存按“产品 + 产品版本 + 配件/装配体/成品节点 + 完成状态”区分。完成状态表示已完成的流程节点。
9.  客户订单发货达到订单数量后自动结单，但生产计划不会自动停止或完成；计划达到完成条件后由业务部手动完成。
10.  `zzerp.sql` 是开发阶段数据库的唯一破坏性重建基线。项目不维护旧表、旧字段或旧数据兼容逻辑。

更完整的项目介绍、部门职责和业务流程见 `PROJECT_OVERVIEW.md`。

禁止：

-   修改核心业务逻辑但未确认影响范围
-   删除已有业务流程
-   恢复已经删除的必做工艺、替代来源或旧数据兼容逻辑
-   让前端、后端和数据库分别维护同一业务概念的不同口径

------------------------------------------------------------------------

# 项目结构说明（Project Structure）

## 项目目录

    /
    ├── AGENTS.md
    ├── PROJECT_OVERVIEW.md
    ├── README.md
    ├── main.py
    ├── zzerp.sql
    ├── package.json
    ├── backend/
    │   ├── requirements.txt
    │   ├── requirements-dev.txt
    │   └── src/
    │       ├── main.py
    │       ├── departments/
    │       ├── domain/
    │       ├── models/
    │       ├── modules/
    │       ├── repositories/
    │       ├── routers/
    │       ├── schemas/
    │       └── services/
    ├── frontend/
    │   ├── package.json
    │   └── src/
    │       ├── api/
    │       ├── features/
    │       ├── motion/
    │       ├── permission/
    │       ├── router/
    │       ├── shared/
    │       ├── stores/
    │       ├── styles/
    │       ├── types/
    │       └── views/
    ├── scripts/
    ├── Dockerfile
    ├── docker-compose.yaml
    ├── render.yaml
    └── netlify.toml

## 目录职责

### 根目录

负责：

-   `main.py`：加载 `backend/src/main.py` 的后端启动入口。
-   `zzerp.sql`：PostgreSQL 完整重建基线，包含表、约束、触发器、索引和初始化数据。
-   `package.json`：前后端开发、检查和数据库辅助脚本入口。
-   `PROJECT_OVERVIEW.md`：项目定位、部门职责、业务流程和详细业务规则。
-   `README.md`：开发环境、验证和部署入口说明。

禁止：

-   在根级启动文件中堆积业务逻辑
-   新增与现有入口重复的脚本或配置

### backend/src/routers/

负责：

-   定义 FastAPI HTTP 路由。
-   接收请求并完成接口层参数衔接。
-   调用对应业务模块的公开 API。
-   返回符合 schemas 定义的响应。

禁止：

-   编写核心业务规则。
-   直接复制模块内部查询和状态判断。

### backend/src/modules/

负责：

-   按业务域实现核心规则和应用流程。
-   管理工程、销售、计划、生产、采购、装配、QC、库存、成品、组织和人员等模块。
-   通过公开 API 与其他模块协作。

主要模块：

-   `engineering`：产品、版本、BOM 和生产流程图。
-   `sales`：客户订单和业务进度。
-   `planning`：生产计划与需求展开。
-   `production_core`：共享生产物料、工单、节点流转和状态规则。
-   `standard_execution`：标准生产部门执行。
-   `assembly`：装配、焊接和多路物料执行。
-   `purchasing`：采购工单与到货。
-   `quality`、`qc`：检验批、QC 结果和放行。
-   `inventory`、`warehouse`：库存、占用、出入库和流水。
-   `output`：成品入库与发货。
-   `pmc`：PMC 进度视图。
-   `organization`、`identity`：组织、账号和权限。
-   `workforce`：工人、工艺单价和工资数据。

禁止：

-   直接依赖其他业务模块的内部实现。
-   在多个模块中重复维护同一状态机或数量算法。

### backend/src/departments/

负责：

-   组合各部门拥有的业务能力。
-   为冲压、CNC、抛光、外协、采购、装配、QC、仓库和成品部提供部门门面。
-   复用统一生产核心，同时隔离部门权限和入口。

### backend/src/services/

负责：

-   跨模块应用流程编排。
-   组织不能由单一业务模块独立完成的操作。

禁止：

-   绕过模块公开 API 直接复制内部规则。
-   把数据访问细节与跨模块流程混在一起。

### backend/src/repositories/

负责：

-   数据查询与持久化。
-   封装稳定的数据访问操作。

禁止：

-   编写业务规则和页面展示判断。

### backend/src/models/

负责：

-   定义 SQLAlchemy 数据模型和表关系。
-   与 `zzerp.sql` 保持结构和约束语义一致。

### backend/src/schemas/

负责：

-   定义后端请求与响应数据结构。
-   约束接口字段、类型和返回口径。

### backend/src/domain/

负责：

-   保存跨模块稳定领域类型、错误和常量。
-   为多个模块提供不依赖具体实现的共同语言。

禁止：

-   放入单一模块私有业务逻辑。

### frontend/src/features/

负责：

-   按业务功能组织页面、组件、接口和状态。
-   管理工程部、业务部、生产、库存、成品、部门和人员等功能。

主要功能：

-   `process-designer`：产品、BOM 和工程部流程图编辑器。
-   `customer-orders`：客户订单、生产计划和业务进度。
-   `production`：生产任务、工作台、工单和生产情况。
-   `material-execution`、`material-planning`：物料执行和计划。
-   `inventory`：库存与流水。
-   `output`：成品入库与发货。
-   `departments`：部门页面注册与部门入口。
-   `workers`：工人、工艺单价和工资页面。
-   `admin`：管理与 PMC 聚合页面。

禁止：

-   跨 feature 直接修改对方内部状态。
-   把后端业务规则复制成独立的前端判断体系。

### frontend/src/shared/

负责：

-   跨功能共享的纯展示组件、表格能力、物料组件和只读流程图能力。
-   不包含具体部门独有的业务流程。

工程部流程图编辑器与其他部门只读流程图职责不同。只能复用纯计算或纯展示能力，不能让共享修改改变工程部编辑行为。

### frontend/src/api/、types/、stores/

负责：

-   `api`：通用请求基础设施。
-   `types`：跨功能共享类型。
-   `stores`：全局状态。

功能专属接口、类型和状态应优先放在对应 feature 内，避免全局目录持续膨胀。

### frontend/src/permission/、router/

负责：

-   权限常量、路由守卫和页面访问控制。
-   前端路由注册和部门页面入口。

### scripts/

负责：

-   `check_backend.py`：后端静态一致性检查。
-   `reset_database.py`：开发数据库破坏性重建。
-   `validate_full_flow.py`：生产计划、库存、生产、发货和结单的全流程验证。

未经用户当前请求明确授权，Agent 不得执行这些脚本。

------------------------------------------------------------------------

# 编码规范（Code Style）

## 通用原则

代码必须：

-   简洁
-   可维护
-   可测试
-   易理解

优先：

-   小范围修改
-   复用已有代码
-   遵循已有项目模式

避免：

-   过度设计
-   无必要抽象
-   大规模重构

## 命名规范

变量、函数、类名称必须：

-   表达实际业务含义
-   避免无意义缩写
-   保持一致性

## 函数规范

函数应该：

-   单一职责
-   保持较短
-   降低嵌套复杂度

禁止：

-   一个函数处理多个业务领域
-   隐藏重要副作用

## 注释规范

只添加必要注释：

允许：

-   复杂算法说明
-   特殊业务规则说明
-   非直观实现原因

禁止：

-   重复解释代码
-   添加无意义注释

------------------------------------------------------------------------

# 架构约束（Architecture Constraints）

## 架构设计原则

必须遵循：

-   低耦合
-   高内聚
-   清晰职责
-   明确边界

设计要求：

-   模块之间必须通过明确接口进行通信
-   禁止直接依赖其他模块内部实现
-   修改单个模块时，应尽量减少对其他模块的影响
-   优先通过抽象接口降低模块之间的依赖
-   新增功能时必须考虑长期维护成本，避免形成技术债

## 分层规则

必须遵循：

Controller → Service → Repository → Database

## Controller 层

负责：

-   请求处理
-   参数验证
-   响应格式处理

禁止：

-   编写核心业务逻辑
-   直接操作数据库

## Service 层

负责：

-   核心业务逻辑
-   业务流程处理

禁止：

-   直接处理底层数据访问细节

## Repository 层

负责：

-   数据查询
-   数据存储
-   外部数据访问

禁止：

-   包含业务判断逻辑

## 模块依赖规则

必须：

-   遵循已有模块依赖方向
-   保持职责边界清晰

禁止：

-   循环依赖
-   跨模块访问内部实现

------------------------------------------------------------------------

# 修改规则（Change Rules）

## Agent 操作权限限制

Agent 只允许对项目目录、文件和文件内容执行以下操作：

允许：

-   添加文件或目录
-   删除文件或目录
-   修改文件内容
-   读取文件内容

禁止：

-   执行任何命令
-   运行脚本
-   执行构建操作
-   执行测试命令
-   操作项目目录以外的系统资源

## 修改前要求

Agent 必须：

1.  阅读相关代码。
2.  理解现有实现。
3.  遵循已有项目规范。

禁止：

-   未阅读代码直接修改
-   根据文件名称猜测功能
-   创建重复实现

## 修改原则

默认原则：

只进行完成任务所需的必要修改。

必须：

-   优先复用已有代码
-   保持修改范围可控
-   遵循现有架构约束
-   保持代码规范一致

禁止：

-   为解决单个报错添加临时代码
-   添加无实际业务价值的代码
-   通过硬编码绕过问题
-   堆积垃圾代码或技术债
-   顺便进行无关重构
-   修改无关文件

## 代码质量要求

新增或修改代码必须：

-   具备明确职责
-   保持可维护性
-   符合项目编码规范
-   避免增加不必要复杂度

------------------------------------------------------------------------

# Debug / Bug Fix 流程

## 复现问题

必须确认：

-   当前错误行为
-   预期行为
-   触发条件

## 定位根因

检查：

-   代码执行路径
-   数据流
-   最近修改
-   相关依赖

目标：

解决根本原因，而不是隐藏错误。

## 实施修复

要求：

-   修改范围最小化
-   遵循现有架构
-   不引入临时代码
-   不通过绕过逻辑解决问题

禁止：

-   为消除报错添加无意义代码
-   添加无法解释用途的逻辑
-   通过硬编码掩盖问题

------------------------------------------------------------------------

# Code Review 标准

## 功能正确性

检查：

-   是否满足需求
-   是否符合业务规则
-   是否处理异常情况

## 代码质量

检查：

-   是否符合项目编码规范
-   是否容易理解和维护
-   是否存在重复或无价值代码

拒绝：

-   临时解决方案
-   垃圾代码
-   无必要复杂设计
-   难以维护实现

## 架构检查

确认：

-   是否遵循架构约束
-   是否保持模块职责清晰
-   是否违反依赖规则

------------------------------------------------------------------------

# 完成条件（Definition of Done）

## 代码

-   [ ] 符合项目编码规范
-   [ ] 修改范围合理
-   [ ] 没有无关改动
-   [ ] 没有重复或无价值代码

## 架构

-   [ ] 遵循架构约束
-   [ ] 保持模块职责清晰
-   [ ] 没有破坏已有设计

## 最终报告

完成任务后必须说明：

1.  修改了什么
2.  为什么修改
